# Diagnostic Report: Worker Log Analysis — 2026-06-04

**Task:** 260604-279
**Generated:** 2026-06-04 04:40 UTC
**Scope:** Docker compose worker logs, Temporal state, SurrealDB health, API document status

---

## Previously Fixed Issues — Verification

### I-01: ProcessingLogger `$rid` Syntax Bug

| Field | Value |
|-------|-------|
| **Status** | **RESOLVED** |
| **Evidence** | 0 occurrences of "ProcessingLogger" in 1000 worker log lines |
| **Confidence** | High — if the bug were still active, every `store_results`/`update_status` call would log the `$rid` error |

The fix (committed in `260603-wqw`) successfully prevents the `$rid` string-literal bug in the processing logger. No `$rid`-related errors appear in the current worker logs.

---

### I-03: SurrealDB In-Memory Storage

| Field | Value |
|-------|-------|
| **Status** | **RESOLVED** (persistence active, but pre-fix data lost) |
| **Evidence** | SurrealDB log: `Starting kvs store at absolute path rocksdb:/data` |
| **Side effect** | The previously-completed document `bd99cf797a1f45b0bc48c65779aec10d` was stored in the old in-memory instance and is **lost**. It must be re-uploaded. |

The SurrealDB container was restarted at `03:46:27 UTC` with `--path /data` for RocksDB persistence. This is confirmed working. However, the restart created a new database — the old in-memory data (from before the fix) is gone. This is expected behavior: persistence starts from the point the `--path /data` flag was added.

---

### 260604-1px: `reference_type` Validation Crash

| Field | Value |
|-------|-------|
| **Status** | **FIX DEPLOYED — stale Temporal replay** |
| **Fix deployed?** | **YES** — `docker compose exec worker grep` confirms line 1597 in `/app/src/eth_pipeline/activities.py`: `if ref_type not in ("espacio", "tiempo", "humanos", "objetos"):` |
| **Still occurring?** | **YES, in logs** — but only as a Temporal replay of pre-fix workflow history |
| **Root cause** | The workflow `doc-bc04801b2dfc49379a55767108eae2c9` was created at `03:53 UTC` (pre-fix). The `que_paso` error was stored in its workflow history. The fix was deployed at `~04:04 UTC`. Temporal replays the stored history — the error is historical, not a live code path. |

**Verification:** New documents uploaded after the fix will NOT hit this error. The validation code on line 1597 catches invalid `reference_type` values before they reach SurrealDB. The only reason the error still appears in logs is Temporal's replay mechanism replaying the old workflow.

---

## Current Errors Found

### Error 1: `que_paso` reference_type (×3 failed activations)

| Field | Detail |
|-------|--------|
| **Category** | **Stale Temporal replay** — NOT a live code bug |
| **Workflow ID** | `doc-bc04801b2dfc49379a55767108eae2c9` |
| **Run ID** | `019e90c3-fb40-74e9-834a-55683cb61385` |
| **Document** | `48- ESMA II fundamentos.pdf` |
| **Exact error** | `RuntimeError: Found 'que_paso' for field \`reference_type\`, with record \`reference:l11uonkw48t3at66kpng\`, but field must conform to: $value = 'espacio' OR $value = 'tiempo' OR $value = 'humanos' OR $value = 'objetos'` |
| **Workflow state** | Running (stuck in loop), 66 state transitions, 61 history events, 48 min runtime |
| **Blocks new processing?** | **No** — only affects this one workflow. New uploads will use the fixed code. |
| **Document data** | 184 references extracted before crash, 66 events, 37 chunks, 743K words |
| **Processing logs** | 2 error entries, 1 warning ("No document_chunk records found"), 1 info |

**Cause:** The LLM returned `reference_type="que_paso"` for one reference during the pre-fix run. The `store_extraction_results_activity` did not have validation at that time, so SurrealDB rejected it with a schema assertion failure. The workflow failed and Temporal stored the failure in its history. Every subsequent replay re-raises the same stored error.

**Evidence this is NOT a live bug:**
1. The validation code is confirmed present in the running container (line 1597)
2. The workflow was created at `03:53 UTC` — before the fix was deployed at `~04:04 UTC`
3. The error is raised at `workflows.py:204` (`raise RuntimeError(store_result["error"])`) — it is reading a stored activity result, not executing the activity anew

---

### Error Count Summary

| Category | Count | Details |
|----------|-------|---------|
| **Live bug** | 0 | No errors on current code paths |
| **Stale replay** | 1 | `doc-bc04801b2dfc49379a55767108eae2c9` — 3 failed activations, same error |
| **Resolved** | 2 | I-01 (ProcessingLogger), I-03 (SurrealDB persistence) |
| **Observation** | 1 | Previously-completed document `bd99cf...` lost due to SurrealDB storage migration |

**Total error lines in worker logs:** 8 (all from the same stale replay)

---

## Pipeline Health Summary

### Services Running

| Service | Status | Uptime | Ports |
|---------|--------|--------|-------|
| `api` | Up (healthy) | 23 min | 1985→8001 |
| `worker` | Up | 23 min | — |
| `surrealdb` | Up (healthy) | 55 min | 8000 |
| `temporal-server` | Up (healthy) | 2 hrs | 7233, 8233 |
| `temporal-ui` | Up | 2 hrs | 8080 |
| `minio` | Up (healthy) | 2 hrs | 9000-9001 |
| `cloudflared` | Up | 2 hrs | — |

**All 7 services are running and healthy.**

### Workflows

| Status | Count | Workflow ID | Document |
|--------|-------|-------------|----------|
| Running (stuck) | 1 | `doc-bc04801b2dfc49379a55767108eae2c9` | 48- ESMA II fundamentos.pdf |
| Completed | 1 | `doc-bd99cf797a1f45b0bc48c65779aec10d` | (data lost — SurrealDB migration) |

### Documents in Database

| Count | Status |
|-------|--------|
| 1 | Failed (`bc04801b2dfc49379a55767108eae2c9`) |
| 0 | Completed |
| 1 | Total |

### SurrealDB Persistence

| Property | Value |
|----------|-------|
| Storage engine | RocksDB |
| Data path | `/data` (mounted volume) |
| Version | 3.1.2 |
| Errors | None |

---

## Recommended Actions

### P0 — Immediate: Terminate stuck Temporal workflow

The stuck workflow `doc-bc04801b2dfc49379a55767108eae2c9` is replaying the same error in a loop, generating noise in the worker logs. It will never succeed because the error is stored in its history.

```bash
docker compose exec temporal-server temporal workflow terminate \
  --workflow-id doc-bc04801b2dfc49379a55767108eae2c9 \
  --reason "Stale replay: pre-fix que_paso reference_type error in stored history"
```

**Effect:** Stops the loop in worker logs immediately. The document data in SurrealDB (184 references) remains — it can be deleted for a clean re-upload.

---

### P0 — Immediate: Re-upload the failed document

After terminating the workflow, delete and re-upload `48- ESMA II fundamentos.pdf` so it processes with the fixed code.

```bash
# 1. Delete the existing document (and all its references, events, logs)
curl -X DELETE http://localhost:1985/documents/bc04801b2dfc49379a55767108eae2c9

# 2. Re-upload the PDF
curl -X POST http://localhost:1985/documents \
  -F "file=@/path/to/48- ESMA II fundamentos.pdf"
```

**Expected outcome:** The document will process cleanly. The fix on line 1597 will skip any `que_paso` reference types and log a warning instead of crashing. The workflow will complete successfully.

---

### P1 — Recover lost document

The document `bd99cf797a1f45b0bc48c65779aec10d` was processed 2 hours ago but its data was lost when SurrealDB switched from in-memory to persistent storage. Re-upload it.

```bash
curl -X POST http://localhost:1985/documents \
  -F "file=@/path/to/(original document for bd99cf797a1f45b0bc48c65779aec10d)"
```

---

### P2 — Optimize: Trim Temporal history retention

The stuck workflow has 66 state transitions and 61 history events. Consider configuring Temporal namespace retention to avoid long replay chains:

```bash
# View current retention
docker compose exec temporal-server temporal namespace describe default
```

This is non-urgent — the current workflow count is manageable.

---

## Summary

| Metric | Value |
|--------|-------|
| Live code bugs | **0** |
| Stale Temporal replays | **1** (pre-fix history, fix deployed) |
| Verified resolved fixes | **3/3** (I-01, I-03, 260604-1px) |
| Services healthy | **7/7** |
| Documents in DB | **1** (failed, needs re-upload) |
| Actions needed | Terminate stuck workflow + re-upload document |

**Conclusion:** The pipeline is healthy. All three previous fixes are verified as deployed and functional. The only error in the worker logs is a stale Temporal replay of a pre-fix workflow — the fix code is active in the container and will prevent this error on all new documents. Terminate the stuck workflow and re-upload the document to confirm.
