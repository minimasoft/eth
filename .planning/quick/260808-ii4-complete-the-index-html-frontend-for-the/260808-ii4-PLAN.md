---
quick_id: 260808-ii4
slug: complete-the-index-html-frontend-for-the
date: 2026-08-08
description: Complete the index.html frontend for the provider/model refactor
tasks: 1
---

# Quick Task 260808-ii4: Complete index.html provider/model frontend

Goal: Finish the 4 pending index.html edits so the UI matches the already-implemented
DB-backed provider/model backend. Backend changes are already done and sitting uncommitted
on this branch — include them as part of the atomic refactor commit.

## Task 1 — Apply all 4 index.html edits

File: `src/eth_pipeline/static/index.html`

1. **Header gears button** — Add a `<div class="header-actions">` inside `<header>` (after
   the `<h1>` at line ~1046) containing an icon button that navigates to `/ui/providers`.
   The header is a blue bar (background #2563eb). Add the `.header-actions` /
   `.header-actions .btn-icon` CSS block next to the existing `header h1` rule (~line 35).
   Style the button in white so it is visible on blue. Wire a click handler in JS
   (e.g. `document.getElementById('providers-gear-btn').addEventListener('click', ...)`
   that sets `window.location.href = '/ui/providers'`).

2. **Remove LLM Configs tab/panel** — Remove the `<button ... id="tab-btn-providers" data-tab="providers">LLM Configs</button>`
   line (1053), the entire `<section id="tab-providers">...</section>` panel (1352–1358),
   the `providers` entry in the `sections` object (line 1382), and the providers branch in
   `onTabClick` (lines 1420–1423). The tab list is read via `document.querySelectorAll('nav button')`
   so removing the button from the DOM is sufficient.

3. **Upload provider fan-out** — Fetch providers on load (`GET /api/providers`) and render a
   checkbox list above the upload actions (in the `#tab-upload` section, near
   `#upload-actions` at line 1066; add a container div e.g. `#provider-select`). Selected
   provider ids are appended as repeated `provider_ids` fields on the FormData in the
   `uploadBtn` click handler (line ~1498). Keep working with no selection (default provider
   is used server-side). Update `renderFileList`/reset logic if needed so the select state
   is preserved/reset appropriately. The backend creates one document row per selected
   provider, so a single upload may return a single `document_id` (first provider) — keep the
   existing per-file success/error handling intact.

4. **Documents "Modelo" column** — Add a `<th class="col-model">Modelo</th>` to the
   documents table header (after "Palabras"/before or after "Tokens", near line 1108) and a
   matching `<td>` in the renderDocuments row template (line ~1719) showing
   `item.provider_name` and `item.model` (fallback to `—` when absent).

## Constraints

- Do NOT modify backend files that are already implemented (providers.py, api routes, etc.)
  unless a genuine bug requires it.
- Preserve existing Spanish UI labels and code style (escapeHtml where interpolating user data).
- Commit all changes (including the pending backend edits already on the tree) as one atomic
  refactor commit.

## Verify

- `node --check` is not applicable to inline HTML; instead: confirm the file has balanced
  `</section>`, no lingering references to `tab-btn-providers` / `tab-providers` /
  `sections.providers`, and the new ids (`providers-gear-btn`, `provider-select`) are defined.
  A quick `rg "tab-providers|tab-btn-providers|sections.providers"` on index.html should
  return nothing.