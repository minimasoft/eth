---
phase: quick
plan: 260905-to0
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/static/index.html
  - tests/test_timeline_fetch_all.py
autonomous: true
requirements: [QUICK-260905-TO0]
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Cronología (timeline) view renders ALL events matching current filters/search, not just the first 100"
    - "The timeline note shows the total event count with no per-page qualifier"
    - "Table view pagination behavior is unchanged"
  artifacts:
    - src/eth_pipeline/static/index.html (fetch-all logic for timeline mode)
    - tests/test_timeline_fetch_all.py (regression test)
  key_links:
    - fetchEventos() timeline branch → sequential /events page fetches (per_page=100, page=1..pages) → merged items → renderEventosTimeline()
---

<objective>
Fix the Cronología (timeline) view in the Eventos tab so it renders ALL events (currently capped at 100 of e.g. 212).

Purpose: The timeline is meant to show every event across the full date range; the current single-page fetch silently drops events beyond 100.

Root cause (verified):
- `GET /events` (`src/eth_pipeline/api/routes/events_v2.py:26`) caps `per_page` at `le=100`.
- `fetchEventos()` (`src/eth_pipeline/static/index.html:2731`) requests `per_page=100` when `eventosViewMode === 'timeline'` and renders only that single page; the note at line ~2859 renders a parenthetical showing the current page count vs total.

Output: Timeline mode fetches every page before rendering; regression test guards the behavior.
</objective>

<execution_context>
@/home/u/src/eth/.opencode/gsd-core/workflows/execute-plan.md
@/home/u/src/eth/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@src/eth_pipeline/static/index.html (lines 2724-2917: fetchEventos, renderEventos, renderEventosTimeline, view-toggle handler)
@src/eth_pipeline/api/routes/events_v2.py (lines 23-33: per_page Query(20, ge=1, le=100), response shape: items/total/page/pages)

Project rules (AGENTS.md): run tests only via `./test.sh` (unit inner loop: `./test.sh --unit`). Never run pytest ad-hoc on the host except `--unit` mode. New schema changes need Alembic revisions — NOT applicable here (no schema changes).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timeline mode fetches all pages before rendering</name>
  <files>src/eth_pipeline/static/index.html</files>
  <action>
In `src/eth_pipeline/static/index.html`, make timeline mode render the complete event set by looping over `/events` pages client-side. Do NOT raise the server-side `le=100` cap in events_v2.py — the unbounded-per-page approach is a DoS vector; the client loop keeps each request bounded while still fetching everything.

Changes, all inside the existing Eventos script block (single file, vanilla JS, keep the ES5-ish style used by neighboring code):

1. Add a `fetchAllEventos()` async helper next to `fetchEventos()` (line ~2724). Behavior:
   - Guard against concurrent fetches the same way `fetchEventos` does (reuse the existing `eventosLoadingFlag` + `deferredLoading('eventos', ...)` pattern so the spinner and flag semantics stay identical).
   - Build `URLSearchParams` exactly like the timeline branch of `fetchEventos` does today: `page`, `per_page` set to the API's maximum page size (the numeric literal `'100'`), `sort=time_start`, `order=asc`, plus `search` and `document` params when the corresponding state variables are set. Server sort is stable, so appending items page-by-page preserves time ordering.
   - Fetch page 1, read `data.total` and `data.pages` from the response, accumulate `data.items` into an array, then loop fetching pages 2..`data.pages` sequentially (sequential, not parallel — keeps request pressure at one page at a time) accumulating items.
   - Reuse the exact error-handling shape of `fetchEventos`'s catch block: same JSON `detail` extraction, same `loader.done()`, same empty-state fallback, same `showBanner(...)` call.
   - After the loop, construct a merged payload `{ items: allItems, total: <total from last response>, page: 1, pages: 1, per_page: <max page size> }`, assign it to `eventosData`, and call `renderEventosTimeline(mergedPayload)`.

2. In `fetchEventos()`, branch at the top: when `eventosViewMode === 'timeline'`, delegate to `fetchAllEventos()` and return. Keep the existing single-fetch path (per_page '20') untouched for table mode so table pagination behaves exactly as before.

3. In `renderEventosTimeline()` (line ~2821), update the note text (the `tl-note` div at lines ~2858-2859): drop the parenthetical that displays current-page vs total (the two-word Spanish qualifier meaning current page, followed by the counts) and instead render the total plainly, e.g. `Una línea por modelo. Cada punto es un evento posicionado por su fecha de inicio (N eventos).` using `data.total`. The timeline already hides `eventosPagination`, so no pagination UI changes are needed.

Do not touch `renderEventos()` (table path), the view-toggle handler's page reset, or search debounce logic.
  </action>
  <verify>
    <automated>grep -n "fetchAllEventos" src/eth_pipeline/static/index.html && grep -n "data.pages" src/eth_pipeline/static/index.html && ! grep -n "página actual" src/eth_pipeline/static/index.html && grep -n "le=100" src/eth_pipeline/api/routes/events_v2.py</automated>
  </verify>
  <done>
- Timeline mode issues sequential /events requests for page 1..pages at per_page=100 and renders the merged item set (212 events → 212 dots/undated entries).
- The tl-note shows the total event count with no page qualifier.
- Table mode still fetches per_page=20 with pagination intact.
- Server per_page cap unchanged (le=100).
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression test guarding timeline fetch-all behavior</name>
  <files>tests/test_timeline_fetch_all.py</files>
  <action>
Create `tests/test_timeline_fetch_all.py` — a pure unit test (no DB fixtures, so it auto-qualifies for `./test.sh --unit`) in the style of the existing static-asset guard tests. It reads `src/eth_pipeline/static/index.html` from the repo root (resolve the path relative to the test file via `pathlib.Path(__file__).resolve().parents[1]`) and asserts:

1. A `fetchAllEventos` function is defined.
2. The fetch-all helper loops over pages: source contains both `data.pages` and an accumulation push into an items array within the helper's vicinity.
3. `fetchEventos` delegates to the helper when the view mode is timeline (the branch condition referencing `eventosViewMode` and the helper name appear together in `fetchEventos`'s body).
4. The timeline note no longer contains the old current-page qualifier string (assert the two-word Spanish phrase for current page is absent from the whole file — it was only used by the timeline note).
5. The table path still exists: `renderEventos(` is called from `fetchEventos`'s non-timeline branch.
6. The API cap is still in place: read `src/eth_pipeline/api/routes/events_v2.py` and assert it contains `le=100`.

Use plain `assert` with descriptive messages (match the assert-with-message style of `tests/test_workflow_timeouts.py`). Mark the module with a docstring explaining it guards the Cronología fetch-all fix (quick task 260905-to0). Do not add any pytest markers — conftest auto-marks only stateful tests as integration, and this one has none.
  </action>
  <verify>
    <automated>./test.sh --unit tests/test_timeline_fetch_all.py</automated>
  </verify>
  <done>
`./test.sh --unit tests/test_timeline_fetch_all.py` passes (all 6 assertions green), proving the fetch-all wiring exists and the old page-qualifier note is gone.
  </done>
</task>

</tasks>

<verification>
- `./test.sh --unit` passes (includes the new regression test; no integration regressions expected — no Python behavior changed).
- `./test.sh` full suite green (confirms nothing else broke).
</verification>

<success_criteria>
- Cronología view displays every event matching the active search/filter (e.g. 212 of 212), via sequential page fetching at the API's max page size.
- Note text reports the total event count without any page qualifier.
- Table view, search, filters, and pagination are behaviorally unchanged.
- Regression test committed and passing in unit mode.
</success_criteria>

<output>
Create `.planning/quick/260905-to0-fix-the-timeline-cronologia-view-it-shou/260905-to0-SUMMARY.md` when done
</output>
