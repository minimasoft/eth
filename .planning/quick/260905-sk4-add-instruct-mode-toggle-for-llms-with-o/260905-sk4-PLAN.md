---
phase: quick-260905-sk4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/alembic/versions/0005_llm_mode_instruct_params.py
  - src/eth_pipeline/llm.py
  - src/eth_pipeline/providers.py
  - src/eth_pipeline/api/models.py
  - src/eth_pipeline/api/routes/providers.py
  - src/eth_pipeline/api/routes/documents.py
  - src/eth_pipeline/activities/extract_events_v7.py
  - src/eth_pipeline/activities/store_events_v7.py
  - src/eth_pipeline/static/index.html
  - src/eth_pipeline/static/providers.html
  - tests/test_llm_mode.py
autonomous: true
requirements: []
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Uploading a document with mode instruct creates document rows with llm_mode='instruct' and the LLM call uses instruct sampling params (temp/top_p/top_k) instead of the thinking defaults"
    - "Default upload path (no mode selected) behaves exactly as today — llm_mode='thinking' and current hardcoded sampling params are used"
    - "When instruct mode ran, every tracking surface shows it: llm_usage rows, llm_call_log rows, processing log metadata, and event_v2.model all carry the '[I]' suffix on the model string"
    - "Instruct sampling params are per-provider columns with sensible defaults; existing providers and the env-backed default provider keep working with zero migration data"
    - "After a successful send from the Cargar tab, the UI switches to the Documentos tab"
  artifacts:
    - src/eth_pipeline/alembic/versions/0005_llm_mode_instruct_params.py
    - tests/test_llm_mode.py
  key_links:
    - "document.llm_mode (written by upload endpoint) → read by extract_events_v7_activity to pick sampling params and build the '[I]' tracking model string"
    - "llm_provider.instruct_* columns → resolve_provider() → activity sampling overrides → OpenRouterProvider payload"
    - "index.html upload form llm_mode select → /documents/upload Form field → document INSERT"
---

<objective>
Add an instruct-mode option to the LLM extraction pipeline: a per-send toggle stored on the document, its own sampling parameters (per-provider columns + safe defaults), visible "[I]" tracking on the model string everywhere usage/calls/events are recorded, and a UI that jumps to the Documentos tab after sending.

Purpose: reasoning ("thinking") models overthink mechanical JSON extraction; instruct mode with lower sampling params should be selectable per document send and distinguishable in tracking, at minimal change surface.

Output: Alembic revision 0005, backend wiring across providers/API/activities, UI toggle + navigation, unit tests for the new pure helpers.
</objective>

<execution_context>
@/home/u/src/eth/.opencode/gsd-core/workflows/execute-plan.md
@/home/u/src/eth/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@src/eth_pipeline/llm.py
@src/eth_pipeline/providers.py
@src/eth_pipeline/api/routes/documents.py (lines 200-330: upload endpoint INSERT)
@src/eth_pipeline/activities/extract_events_v7.py
@src/eth_pipeline/activities/store_events_v7.py (lines 85-140: document SELECT + event INSERT)
@src/eth_pipeline/alembic/versions/0003_llm_providers_document_model.py (revision format reference)
@src/eth_pipeline/static/index.html (lines 1251-1265 upload section; lines 1763-1811 upload click handler; line 1614 switchTab)
@src/eth_pipeline/static/providers.html (lines 636-669 add-provider form submit)

**CRITICAL — uncommitted WIP in llm.py:** `git diff` shows one uncommitted hunk in
`src/eth_pipeline/llm.py` (a prompt-text tweak inside `EVENT_EXTRACTION_V7_SYSTEM_PROMPT`,
around lines 157-158). This is unrelated user work-in-progress:
- Do NOT revert it; do NOT `git stash` it.
- When committing llm.py, stage ONLY this task's hunks (defaults/helpers/__init__/_build_v7_payload
  regions) — e.g. build a filtered patch of the task hunks and `git apply --cached`, or
  equivalent selective staging. The WIP hunk (system-prompt string) must remain unstaged and
  uncommitted. All other files can be staged whole.
- Edits to llm.py are in different regions than the WIP hunk, so hunk-level separation is clean.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 0005 + instruct sampling config in llm.py/providers</name>
  <files>src/eth_pipeline/alembic/versions/0005_llm_mode_instruct_params.py, src/eth_pipeline/llm.py, src/eth_pipeline/providers.py, src/eth_pipeline/api/models.py, src/eth_pipeline/api/routes/providers.py, tests/test_llm_mode.py</files>
  <behavior>
    - Test: resolve_sampling("thinking", None) returns None (use provider's current hardcoded payload params)
    - Test: resolve_sampling("instruct", None) returns {"temperature": 0.7, "top_p": 0.9, "top_k": 40} (module defaults)
    - Test: resolve_sampling("instruct", {"instruct_temperature": 0.3, "instruct_top_p": None, "instruct_top_k": 20}) returns {"temperature": 0.3, "top_p": 0.9, "top_k": 20} — per-provider values win, NULL falls back to defaults
    - Test: tracking_model_name("deepseek/v4", "thinking") == "deepseek/v4"; tracking_model_name("deepseek/v4", "instruct") == "deepseek/v4 [I]"
    - Test: _build_v7_payload with sampling={"temperature": 0.3, "top_p": 0.8, "top_k": 20} emits those values; without sampling it emits temperature 1.0, top_p 0.95, top_k 20 (current behavior unchanged)
    - Test: unknown mode string raises ValueError
  </behavior>
  <action>
    1. Create Alembic revision `0005` (down_revision `'0004'`, same file style as 0003). Upgrade:
       - `ALTER TABLE llm_provider ADD COLUMN instruct_temperature DOUBLE PRECISION`, `instruct_top_p DOUBLE PRECISION`, `instruct_top_k INTEGER` — all nullable, no server default (NULL = "use module defaults").
       - `ALTER TABLE document ADD COLUMN llm_mode TEXT NOT NULL DEFAULT 'thinking'`.
       Downgrade drops all four columns in reverse order. Do NOT touch `schema.sql` — these are migration-owned objects (AGENTS.md rule).
    2. In `src/eth_pipeline/llm.py` (preserving the uncommitted WIP hunk per context note):
       - Add module constants: `INSTRUCT_TEMPERATURE = 0.7`, `INSTRUCT_TOP_P = 0.9`, `INSTRUCT_TOP_K = 40`.
       - Add two pure module-level helpers (unit-testable, no I/O):
         `resolve_sampling(mode: str, provider_cfg: dict | None) -> dict | None` — returns None for mode "thinking"; for "instruct" returns a dict with keys temperature/top_p/top_k taking provider_cfg["instruct_temperature"/"instruct_top_p"/"instruct_top_k"] when not None, else the module constants; raises ValueError for any other mode. Accept mode case-insensitively (strip + lower).
         `tracking_model_name(model: str, mode: str) -> str` — appends " [I]" only when mode == "instruct" (after strip/lower), else returns the model unchanged.
       - `OpenRouterProvider.__init__` gains optional `sampling: dict | None = None` param (stored as `self._sampling`). In `_build_v7_payload`, when `self._sampling` is set, use its temperature/top_p/top_k in the payload instead of the current hardcoded 1.0/0.95/20. `presence_penalty` and `max_tokens` stay as-is in both modes. Thinking-mode payload must be byte-identical to today when sampling is None.
       - Do NOT pass LLM config through Temporal activity args anywhere — the activity already resolves from DB (AGENTS.md).
    3. In `src/eth_pipeline/providers.py`: add the three instruct columns to every SELECT column list (list_providers, get_provider, resolve_provider, resolve_provider_by_name); expose them in `_row_to_public`; `add_provider` gains optional keyword args `instruct_temperature: float | None = None`, `instruct_top_p: float | None = None`, `instruct_top_k: int | None = None` and includes them in the INSERT. Basic validation: values must be in range (temperature 0–2, top_p 0–1, top_k >= 1) else raise ValueError.
    4. In `src/eth_pipeline/api/models.py`: `ProviderCreate` and `ProviderItem` gain the three optional fields (`instruct_temperature: float | None = None`, etc.).
    5. In `src/eth_pipeline/api/routes/providers.py`: `create_provider` forwards the three new fields to `add_provider`.
    6. Write `tests/test_llm_mode.py` with the unit tests from the behavior block (host-runnable, no DB — mark nothing integration). Resolve provider_cfg rows to plain dicts in tests.
  </action>
  <verify>
    <automated>./test.sh --unit -- tests/test_llm_mode.py -q</automated>
  </verify>
  <done>Alembic 0005 upgrades/downgrades cleanly (verified in Task 2's full test run); pure helpers pass all unit tests on host; thinking-mode LLM payload unchanged; providers API accepts and returns instruct params.</done>
</task>

<task type="auto">
  <name>Task 2: Wire llm_mode through upload API, extraction activity, and event provenance</name>
  <files>src/eth_pipeline/api/routes/documents.py, src/eth_pipeline/activities/extract_events_v7.py, src/eth_pipeline/activities/store_events_v7.py, src/eth_pipeline/api/models.py</files>
  <action>
    1. `src/eth_pipeline/api/routes/documents.py`:
       - `upload_document` gains `llm_mode: Annotated[str, Form()] = "thinking"`. Normalize (strip/lower) and validate against {"thinking", "instruct"} — reject with HTTP 400 otherwise. Pass the normalized value into the document INSERT (add `llm_mode` to the column list and one more bind param).
       - JSON endpoint `create_document` (`POST /documents`): `DocumentInput` gains `llm_mode: str = "thinking"`; validate the same way (HTTP 422 via ValueError→HTTPException or explicit check → 400); include in its INSERT.
    2. `src/eth_pipeline/activities/extract_events_v7.py`:
       - Extend the document SELECT to `SELECT provider_id, model, llm_mode FROM document WHERE id = $1`; `mode = (doc.get("llm_mode") or "thinking")`.
       - Compute `sampling = resolve_sampling(mode, provider_cfg)` (import from `eth_pipeline.llm`) and pass `sampling=sampling` to `OpenRouterProvider(**provider_kwargs, sampling=sampling)` when not None (or always — None is the no-op).
       - Compute `tracking_model = tracking_model_name(model, mode)` and use it for: `record_llm_usage(model=...)`, `record_llm_call_log(model=...)`, the processing-log metadata dict, and the activity logger lines. The actual API call keeps the clean `model`.
       - Note in code: sampling/mode are fetched from the DB inside the activity — never added to activity args (Temporal history hygiene).
    3. `src/eth_pipeline/activities/store_events_v7.py`:
       - Extend the document SELECT to include `llm_mode`; set `event_model = tracking_model_name(doc.get("model") or "", doc.get("llm_mode") or "thinking")` (guard None model) so `event_v2.model` carries the "[I]" suffix for instruct runs — this automatically surfaces in the Eventos tab Modelo column, comparisons, and model filters with zero UI change.
    4. `src/eth_pipeline/api/models.py`: `DocumentInput` gains `llm_mode` (from step 1).
  </action>
  <verify>
    <automated>./test.sh</automated>
  </verify>
  <done>Full suite passes in the disposable test stack (migration 0005 applies during schema-init); an upload with llm_mode=instruct persists the flag; instruct runs record "[I]"-suffixed model in llm_usage, llm_call_log, and event_v2; thinking runs are byte-identical to current behavior.</done>
</task>

<task type="auto">
  <name>Task 3: UI — mode toggle on upload, navigate to Documentos after send, instruct params in providers form</name>
  <files>src/eth_pipeline/static/index.html, src/eth_pipeline/static/providers.html</files>
  <action>
    1. `src/eth_pipeline/static/index.html` upload section (tab-upload, near the `provider-select` div):
       - Add a small inline "Modo:" `<select id="llm-mode">` with two options: value "thinking" labeled "Thinking (predeterminado)" (selected by default) and value "instruct" labeled "Instruct". Always visible once files are chosen (toggle its display together with `upload-actions`, or leave always visible inside the upload area — match existing styling conventions; keep it simple, reuse `.provider-select` styling pattern).
       - In the upload click handler (~line 1776): after building FormData, append `formData.append('llm_mode', document.getElementById('llm-mode').value || 'thinking')`.
       - After the per-file loop, if at least one file uploaded successfully (`allSuccess` or any success), call `switchTab('documents')` followed by `fetchDocuments()` so the user lands on the Documentos tab after sending. Keep the existing success banners.
    2. `src/eth_pipeline/static/providers.html` add-provider form:
       - Add three optional numeric inputs (label them in Spanish, e.g. "Temperatura instruct", "Top P instruct", "Top K instruct", each with placeholder showing the default: 0.7 / 0.9 / 40). Empty = omit (send null).
       - Include them in the POST /api/providers JSON body as `instruct_temperature` / `instruct_top_p` / `instruct_top_k` (null when blank).
    3. No other UI changes: the "[I]" suffix flows into Eventos/Registros tables automatically via event_v2.model and llm_call_log.model from Task 2.
  </action>
  <verify>
    <automated>./test.sh --unit -q && grep -c "llm-mode" src/eth_pipeline/static/index.html | grep -qv '^0$' && grep -c "instruct_top_k" src/eth_pipeline/static/providers.html | grep -qv '^0$'</automated>
  </verify>
  <done>Upload form sends llm_mode (default thinking); after a successful send the app shows the Documentos tab; providers form accepts optional instruct sampling params; static pages still load (no JS syntax errors — sanity-check by serving and loading /ui in a browser if the stack is up, else via node --check on extracted script is unnecessary; rely on manual smoke with ./run.sh when available).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→upload API | llm_mode form field is client-controlled input reaching the DB |
| browser→providers API | instruct_* numeric fields are client-controlled input reaching the DB |
| DB→activity | llm_mode and instruct params read back and injected into LLM API payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-SK4-01 | Tampering | upload `llm_mode` Form field | low | mitigate | Server-side allowlist {thinking, instruct} after strip/lower; 400 on anything else; DB column defaulted server-side |
| T-SK4-02 | Tampering | providers `instruct_*` numeric fields | low | mitigate | Range validation in add_provider (temp 0–2, top_p 0–1, top_k ≥ 1); typed as float/int in Pydantic; NULL fallback to constants |
| T-SK4-03 | Information disclosure | sampling params in Temporal history | low | accept | Params are non-secret sampling knobs resolved from DB inside the activity; no keys/models of record cross activity args (AGENTS.md rule upheld) |
| T-SK4-04 | Denial of service | instruct payloads to reasoning-mode endpoints | low | accept | Sampling params are provider-relayed standard OpenAI-compatible fields; failure surfaces as existing HTTP error path |
</threat_model>

<verification>
- `./test.sh` full suite green (includes migration application via schema-init and all existing v7 workflow/activity tests).
- `./test.sh --unit -- tests/test_llm_mode.py -q` green for the pure helpers.
- `git log --oneline` shows task commits; `git status` afterwards still shows the llm.py prompt-text WIP hunk as modified-uncommitted (never committed, never stashed away permanently).
</verification>

<success_criteria>
- Toggle exists per send (default thinking), instruct mode uses its own temp/top_p/top_k (per-provider override + module defaults).
- Instruct runs are identifiable in llm_usage, llm_call_log, processing log, and event provenance via the "[I]" model-string suffix.
- UI navigates to Documentos tab after sending a document.
- Thinking-mode behavior (payload params, tracking strings) is unchanged.
</success_criteria>

<output>
Create `.planning/quick/260905-sk4-add-instruct-mode-toggle-for-llms-with-o/260905-sk4-SUMMARY.md` when done
</output>
