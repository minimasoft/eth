---
phase: quick-260604-h1i
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/activities.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Worker processes documents through resolve_entities_with_search_activity without NameError"
    - "Stuck workflow doc-560521dc96614e1bbadd1ab37a505791 stops looping"
  artifacts:
    - path: "src/eth_pipeline/activities.py"
      provides: "resolve_entities_with_search_activity with doc_ref defined"
      contains: "doc_ref = str(doc_rid)"
  key_links:
    - from: "src/eth_pipeline/activities.py:687"
      to: "doc_ref variable"
      via: "defined at line 582"
      pattern: "doc_ref = str\\(doc_rid\\)"
---

<objective>
Fix the `NameError: name 'doc_ref' is not defined` crash in `resolve_entities_with_search_activity`, then rebuild and restart the worker to clear the stuck workflow loop.

Purpose: 48 occurrences of this error in the past 7 minutes since the last worker restart (2026-06-04T15:09 UTC). The stuck workflow `doc-560521dc96614e1bbadd1ab37a505791` is in an infinite retry loop, consuming resources.

Output: A single-line fix in activities.py, a rebuilt worker container, and a terminated stuck workflow.
</objective>

<execution_context>
@/home/u/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/u/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
<!-- Only what the executor needs to understand the bug and fix -->

@src/eth_pipeline/activities.py:579-581  # Where doc_rid is defined
@src/eth_pipeline/activities.py:685-689  # Where doc_ref is used but undefined

**Root cause:** `resolve_entities_with_search_activity` (line 544) defines `doc_rid = RecordID("document", document_id)` on line 581, but on line 687 references the undefined name `doc_ref` in the string interpolation `f"SELECT text_content FROM {doc_ref}"`. All other activities in this file define both a `doc_rid` (RecordID object for parameterized queries) and a `doc_ref` (string like `"document:{id}"` for inline SURREALQL interpolation). This function only defined `doc_rid`.

**Secondary finding:** A different workflow (`doc-f0e612...`) has a legit PDF error (empty/scanned PDF with no text layer) — this is a user-document issue, not a code bug. The `Activity not found on completion` warnings are stale callbacks for already-completed workflows — harmless.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix undefined doc_ref variable in resolve_entities_with_search_activity</name>
  <files>src/eth_pipeline/activities.py</files>
  <action>
After line 581 (`doc_rid = RecordID("document", document_id)`), add line 582:
`doc_ref = str(doc_rid)`

This produces the string `"document:{document_id}"` needed for the inline SURREALQL interpolation on line 687 (`f"SELECT text_content FROM {doc_ref}"`). Using `str(doc_rid)` instead of an f-string keeps the code DRY — RecordID already knows its format.

This pattern matches every other activity in this file (e.g., `get_document_text_activity` at line 129-130, `update_document_status_activity` at line 1421, `extract_text_activity` at line 1782, etc.) — all define both `doc_rid` and `doc_ref`.
  </action>
  <verify>
    <automated>grep -n "doc_ref" /home/u/src/eth/src/eth_pipeline/activities.py | head -5 && python3 -c "from eth_pipeline.activities import resolve_entities_with_search_activity; print('import OK')"</automated>
  </verify>
  <done>Line after `doc_rid = RecordID(...)` now contains `doc_ref = str(doc_rid)`, and `python3 -c "import eth_pipeline.activities"` succeeds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild worker container, restart, and terminate stuck workflow</name>
  <files>docker-compose.yml</files>
  <action>
Rebuild and restart the worker container with the fix, then terminate the stuck workflow:

1. Rebuild worker image: `docker compose build worker`
2. Recreate worker with new image: `docker compose up -d worker` (this stops the old one and starts the fixed one)
3. Wait 3s for worker to register with Temporal: `sleep 3`
4. Terminate the stuck workflow: `docker compose exec temporal-server temporal workflow terminate --workflow-id doc-560521dc96614e1bbadd1ab37a505791`
5. Verify no new errors appear in the next 10 seconds: `sleep 10 && docker compose logs worker --tail 20 2>&1 | grep -c "name 'doc_ref' is not defined"`

Expected: step 5 returns 0 (no new instances of the error).
  </action>
  <verify>
    <automated>docker compose logs worker --tail 20 2>&1 | grep -c "name 'doc_ref' is not defined" | xargs -I{} sh -c '[ {} -eq 0 ] && echo "PASS: zero doc_ref errors" || echo "FAIL: {} doc_ref errors remain"'</automated>
  </verify>
  <done>Worker container is built from the fixed code, running, and `docker compose logs worker --tail 20` contains zero instances of `name 'doc_ref' is not defined`. The stuck workflow `doc-560521dc96614e1bbadd1ab37a505791` is terminated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| (none) | This is a bugfix — no new surface or data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h1i-01 | None | activities.py:687 | fix | Add missing `doc_ref` variable definition |
| T-h1i-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
After both tasks complete:
- `grep "name 'doc_ref' is not defined"` from worker logs returns 0 matches
- Worker container is running with the rebuilt image
- The stuck workflow `doc-560521dc96614e1bbadd1ab37a505791` is terminated in Temporal
</verification>

<success_criteria>
- [ ] `doc_ref = str(doc_rid)` added after line 581 in activities.py
- [ ] Worker rebuilt and restarted
- [ ] Zero new `NameError: name 'doc_ref' is not defined` in worker logs
- [ ] Stuck workflow terminated
- [ ] Commit created with the fix
</success_criteria>

<output>
Create `.planning/quick/260604-h1i-review-docker-compose-worker-logs-and-fi/260604-h1i-SUMMARY.md` when done
</output>
