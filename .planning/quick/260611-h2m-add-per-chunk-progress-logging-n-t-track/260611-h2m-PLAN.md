---
phase: quick/260611-h2m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/llm.py
  - src/eth_pipeline/activities/extract_events_v7.py
  - src/eth_pipeline/workflows.py
  - tests/test_extract_events_v7.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Activity logs include per-chunk progress (N/T) before and after the LLM call"
    - "System prompt instructs literal event/crime extraction with OCR artifact awareness and research disclaimer"
    - "User prompt in _build_v7_payload includes OCR artifact note and disclaimer"
    - "Workflow passes total_chunks to the activity"
    - "Tests pass with updated signature and semantic 3rd-arg usage"
  artifacts:
    - path: src/eth_pipeline/llm.py
      provides: "Updated EVENT_EXTRACTION_V7_SYSTEM_PROMPT + _build_v7_payload user message"
    - path: src/eth_pipeline/activities/extract_events_v7.py
      provides: "Per-chunk N/T logging before/after LLM call, total_chunks param"
    - path: src/eth_pipeline/workflows.py
      provides: "total_chunks=len(chunks) passed to activity call"
    - path: tests/test_extract_events_v7.py
      provides: "Tests use None for prior_events when irrelevant, pass with new signature"
  key_links:
    - from: workflows.py:123
      to: extract_events_v7_activity()
      via: "args=[document_id, chunk_idx, prior_events, total_chunks=len(chunks)]"
    - from: extract_events_v7_activity():68
      to: provider.extract_events_v7()
      via: "log before/after LLM call with chunk N/T"
---

<objective>
Add per-chunk progress logging with N/T tracking, log before/after the LLM call, improve the system/user prompts for more literal event/crime extraction with OCR artifact notes and a research disclaimer.

**Purpose:** Make pipeline progress visible in logs during long multi-chunk document extraction, and improve extraction quality by guiding the model to extract literally from the text, handle OCR artifacts gracefully, and clarify the research-only nature of the extraction.

**Output:** Updated llm.py (prompts), extract_events_v7.py (logging + signature), workflows.py (total_chunks param), tests (adapted to new signature).
</objective>

<execution_context>
This is a quick-mode task. All changes are in known files with established patterns.
No discovery needed — Level 0 (pure internal work, established patterns only).
</execution_context>

<context>
src/eth_pipeline/llm.py              # EVENT_EXTRACTION_V7_SYSTEM_PROMPT (L331-344), _build_v7_payload (L919-955)
src/eth_pipeline/activities/extract_events_v7.py  # extract_events_v7_activity (full file, 132 lines)
src/eth_pipeline/workflows.py        # DocumentProcessingV7Workflow.run (chunk loop L105-153)
tests/test_extract_events_v7.py      # Tests (221 lines) — all call with str as 3rd arg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve system prompt and user message for literal extraction, OCR awareness, and disclaimer</name>
  <files>src/eth_pipeline/llm.py</files>
  <action>
    **In `EVENT_EXTRACTION_V7_SYSTEM_PROMPT` (L331-344):**
    Append after the existing content three new paragraphs (separated by `\n\n`):

    1. **Literal event/crime extraction instruction:**
       ```
       "Instrucción específica de extracción: Extrae eventos y crímenes específicos descritos "
       "literalmente en el texto — cada incidente, violación, ataque, detención, desaparición, "
       "u otro hecho con entidad propia debe convertirse en un evento separado. No resumas, no "
       "generalices. Si el texto describe múltiples incidentes distintos, cada uno debe ser su "
       "propio objeto en la lista 'events'. El título del evento debe reflejar directamente el "
       "hecho descrito (ej. 'Detención de Juan Pérez el 15 de marzo' no 'Hechos de violencia').\n\n"
       ```

    2. **OCR artifact awareness:**
       ```
       "NOTA sobre artefactos de OCR: El texto que analizas proviene de reconocimiento óptico "
       "de caracteres (OCR) sobre documentos escaneados. Puede contener caracteres extraviados, "
       "palabras mal reconocidas, saltos de línea incorrectos, o fragmentos de formato. Extrae "
       "la información disponible incluso si el texto circundante tiene errores de OCR. No "
       "inventes ni corrijas información que no esté claramente presente en el texto original.\n\n"
       ```

    3. **Disclaimer:**
       ```
       "AVISO: Esta es una extracción automatizada de datos para fines de investigación "
       "académica y documentación histórica. Los datos extraídos no constituyen asesoría "
       "legal ni tienen validez jurídica vinculante. Este proyecto no tiene fines de lucro "
       "ni objetivos político-partidistas — su único propósito es la preservación de la "
       "memoria histórica y la facilitación de investigación académica sobre derechos humanos."
       ```

    **In `_build_v7_payload` (L919-955):**
    After the schema instruction (L933-936), add a short user-facing note about OCR artifacts before the chunk text:
    ```
    user_parts.append(
        "(Nota: Este texto puede contener artefactos de OCR. "
        "Extrae solo la información que esté claramente presente en el texto.)\n\n"
    )
    ```
    Insert right before the `f"{text}"` line (currently L935). Also add a brief disclaimer to the user message:
    ```
    user_parts.append(
        "AVISO: Esta es una extracción automatizada para fines de investigación académica "
        "y no tiene validez jurídica vinculante.\n\n"
    )
    ```
    Insert right after the OCR artifact note, before the `f"{text}"` line.

    **The resulting user_parts flow should be:**
    1. (if prior_events) prior events context
    2. JSON schema instruction
    3. OCR artifact note
    4. Research disclaimer
    5. The chunk text

    **Do NOT** touch any other part of the file — no schema changes, no temperature changes, no max_tokens changes.
  </action>
  <verify>
    <automated>
      grep -c 'artefactos de OCR' src/eth_pipeline/llm.py > /dev/null 2>&1 \
      && grep -c 'valor jurídico' src/eth_pipeline/llm.py > /dev/null 2>&1 \
      && grep -c 'crimen específico\|crímenes\|incidente' src/eth_pipeline/llm.py > /dev/null 2>&1 \
      && echo "PASS" || echo "FAIL — prompt updates missing"
    </automated>
  </verify>
  <done>
    EVENT_EXTRACTION_V7_SYSTEM_PROMPT has 3 new paragraphs (literal extraction, OCR artifacts, disclaimer). _build_v7_payload user message includes OCR artifact note and research disclaimer. No other changes in llm.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add total_chunks param, before/after LLM call logging with N/T, update workflow and tests</name>
  <files>
    src/eth_pipeline/activities/extract_events_v7.py
    src/eth_pipeline/workflows.py
    tests/test_extract_events_v7.py
  </files>
  <action>
    **In `extract_events_v7.py`:**

    1. **Add `total_chunks` parameter** to the activity function signature:
       ```python
       async def extract_events_v7_activity(
           document_id: str,
           chunk_index: int,
           prior_events: list[dict] | None = None,
           total_chunks: int = 0,
       ) -> dict:
       ```

    2. **Build a `chunk_progress` string** for reuse in log messages (right after getting chunk_text):
       ```python
       chunk_progress = f"[chunk {chunk_index + 1}/{total_chunks}]" if total_chunks > 0 else f"[chunk {chunk_index}]"
       ```

    3. **Update start log (L56-65)** — add `chunk_progress` and `total_chunks` to both log calls:
       - `activity.logger.info`: change message to include the N/T progress, e.g. `"extract_events_v7_activity called {chunk_progress}..."`; add `total_chunks` to the extra data.
       - `_log.log`: change the message string to include chunk_progress.

    4. **Add log before LLM call** — right before L68 (`chunk_result, usage = await provider.extract_events_v7(...)`):
       ```python
       activity.logger.info(
           "Sending prompt to LLM for v7 extraction %s [document_id=%s]",
           chunk_progress, document_id,
       )
       await _log.log(document_id, "extract_events_v7", "info",
                      f"Sending prompt to LLM {chunk_progress}",
                      {"chunk_index": chunk_index, "total_chunks": total_chunks})
       ```

    5. **Add log after LLM call returns successfully** — right after L70 (after `chunk_result, usage = ...`), before the any-error check:
       ```python
       activity.logger.info(
           "LLM response received for v7 extraction %s [document_id=%s]",
           chunk_progress, document_id,
       )
       await _log.log(document_id, "extract_events_v7", "info",
                      f"LLM response received {chunk_progress}",
                      {"chunk_index": chunk_index, "total_chunks": total_chunks})
       ```

       These two log calls bookend the actual HTTP call to OpenRouter so Temporal logs show clearly what happened before vs after the call.

    6. **Update the completion log (L123-131)** — add chunk_progress:
       Change message to `f"V7 extraction completed {chunk_progress}: {len(result['events'])} events"`.

    **In `workflows.py`:**

    At L121-123, add `total_chunks=len(chunks)` to the activity call args:
    ```python
    extract_result = await workflow.execute_activity(
        extract_events_v7_activity,
        args=[document_id, chunk_idx, prior_events],
        kwargs={"total_chunks": len(chunks)},
        ...
    )
    ```
    Use `kwargs` dict (not expanding into `args`) since `total_chunks` is a keyword arg with default.

    **In `tests/test_extract_events_v7.py`:**

    For every test that calls `extract_events_v7_activity(...)` with a string as the 3rd argument (which maps to `prior_events` despite being a str):
    - Change the 3rd positional arg from `"some text"` / `"document text"` / `"sensitive text"` / `"test text"` / `"some text"` to `None`.
    - These strings were semantically incorrect — they happened to not crash because `json.dumps("string")` works, but the parameter is typed as `list[dict] | None`.

    Affected lines:
    - L22: `"doc-001", 0, "some text"` → `"doc-001", 0, None`
    - L76: `"doc-002", 1, "document text"` → `"doc-002", 1, None`
    - L100: `"doc-003", 0, "sensitive text"` → `"doc-003", 0, None`
    - L122: `"doc-004", 0, "some text"` → `"doc-004", 0, None`
    - L162-164: `"doc-005", 2, "test text"` → `"doc-005", 2, None`
    - L206-208 (`test_no_refusals_on_corpus`): `f"corpus-{os.path.basename(txt_path)}", idx, chunk` — here `chunk` is actual text content. This call was incorrect: the activity fetches the text from DB, so passing text as `prior_events` is wrong. This test bypasses the activity's DB-fetch pattern for spike testing. Keep the call as-is since this test works differently (it provides text through `prior_events` parameter), but add a comment explaining this: `# Bypass: passes chunk as prior_events for direct-injection spike testing`.
  </action>
  <verify>
    <automated>
      grep -c 'total_chunks' src/eth_pipeline/activities/extract_events_v7.py > /dev/null 2>&1 \
      && grep -c 'Sending prompt to LLM' src/eth_pipeline/activities/extract_events_v7.py > /dev/null 2>&1 \
      && grep -c 'LLM response received' src/eth_pipeline/activities/extract_events_v7.py > /dev/null 2>&1 \
      && grep -c '"total_chunks"' src/eth_pipeline/workflows.py > /dev/null 2>&1 \
      && grep -c 'prior_events.*None' tests/test_extract_events_v7.py > /dev/null 2>&1 \
      && echo "PASS" || echo "FAIL — check individual grep results"
    </automated>
  </verify>
  <done>
    Activity has total_chunks param. Before/after LLM call logs include chunk N/T. Workflow passes total_chunks=len(chunks). Tests use None for prior_events where semantically correct. All existing tests pass with `python -m pytest tests/test_extract_events_v7.py -x -v`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Activity code→LLM API | Prompt text crosses network boundary to external model provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h2m-01 | Tampering | npm/pip installs | mitigate | No new packages — all changes are in existing source files only |
| T-h2m-02 | Information Disclosure | Prompt text logging | accept | Chunk text already logged at activity start; logging N/T progress adds no new PII risk |
| T-h2m-SC | Tampering | pip installs | mitigate | No new packages — zero package installs in this task |
</threat_model>

<verification>
```bash
# Run all existing tests to confirm no regressions
python -m pytest tests/test_extract_events_v7.py -x -v 2>&1
```
</verification>

<success_criteria>
1. Four key log messages appear in sequence for each chunk: activity called → sending prompt → response received → extraction completed — all with N/T progress
2. System prompt includes literal extraction instruction, OCR artifact note, and research disclaimer
3. User message includes OCR artifact note and research disclaimer
4. Workflow passes total_chunks via kwargs
5. All tests pass with semantically correct arguments
</success_criteria>

<output>
Create SUMMARY after execution.
</output>
