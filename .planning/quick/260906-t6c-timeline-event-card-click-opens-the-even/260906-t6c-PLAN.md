---
phase: quick-260906-t6c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/static/index.html
  - src/eth_pipeline/static/linea-tiempo.js
  - tests/test_hash_navigation.py
autonomous: true
requirements: [QUICK-T6C]
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Clicking an event card (or undated item) in the Línea de tiempo tab opens the event detail in the right half of a 50-50 horizontal split inside the same tab — the timeline stays visible on the left.
    - Clicking the SAME event card again closes the detail; a small × button at the top-right of the detail pane also closes it; the existing "Volver" button still works.
    - The open card is visually highlighted while its detail is open.
    - The timeline view is deep-linkable with an open event: #s=lineatiempo&event=<id> restores split view with that detail (reload and fresh tab); browser back to #s=lineatiempo closes the detail.
    - Switching to any other tab closes the timeline detail and returns the shared detail panel to the eventos tab, where existing behavior (table rows, Cronología cards, #s=eventos&event=) is unchanged.
    - Closing the timeline detail never triggers an eventos refetch.
  artifacts:
    - src/eth_pipeline/static/index.html (split markup + CSS, context-aware detail state machine, hash wiring)
    - src/eth_pipeline/static/linea-tiempo.js (toggle routing, selection highlight, lt2-selected CSS)
    - tests/test_hash_navigation.py (new static-structure guards)
  key_links:
    - linea-tiempo.js click handler → window.toggleEventDetailFromTimeline(id) → openEventDetail(id, 'lineatiempo') / closeTimelineEventDetail()
    - #event-detail-panel reparenting: original parent (tab-eventos) ↔ #lineatiempo-split-detail (listeners survive appendChild)
    - syncHash/applyHash ↔ lineatiempoEventId (event=<id> written only while detail open in lineatiempo tab)
---

<objective>
In the Línea de tiempo (vertical timeline) tab, clicking an event card opens the event detail in a 50-50 horizontal split — timeline left, detail right — reusing the existing event-detail panel and rendering, closable by re-clicking the same card or a × button, with #s=lineatiempo&event=<id> deep links.

Purpose: today the card click calls showEventDetail, but the detail panel lives inside the eventos tab, so nothing visible happens from the timeline tab.

Output: modified index.html + linea-tiempo.js, extended static tests, all unit tests green.
</objective>

<execution_context>
@/home/u/src/eth/.opencode/gsd-core/workflows/execute-plan.md
@/home/u/src/eth/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/u/src/eth/AGENTS.md
@/home/u/src/eth/src/eth_pipeline/static/linea-tiempo.js
@/home/u/src/eth/tests/test_hash_navigation.py

Key index.html locations (read once, edit surgically):
- tab-lineatiempo section: lines 1491-1494 (lineatiempo-loading + lineatiempo-container only)
- #event-detail-panel: line 1392 (direct child of section#tab-eventos, after #eventos-container; "Volver" back btn line 1394)
- Hash state machine: parseHash ~1625, syncHash ~1642 (eventos branch pushes event= at ~1648), applyHash ~1657 (event option handled only for state.tab === 'eventos', regex /^[A-Za-z0-9-]+$/ at ~1676)
- switchTab ~1687, onTabClick ~1721 (lineatiempo branch calls renderLineaTiempo + restoreLineaTiempoScroll), hashchange listener ~1740
- Event detail block: currentEventId/currentEventDetail ~3309, showEventDetail ~3330 (sets currentEventId, syncHash, hides eventosContainer, shows panel, fetch+renderEventDetail), hideEventDetail ~3444 (syncHash, hides panel, shows eventosContainer, fetchEventos), back-btn binding ~3453
- Universal refresh dispatch: ~1749 (lineatiempo → window.refreshLineaTiempo())
- HARD CONSTRAINT: tests/test_hash_navigation.py::test_no_lt2_leakage_into_index_html — the string "lt2-" must NOT appear anywhere in index.html. All timeline-card CSS/JS (including the selected-card highlight) stays in linea-tiempo.js; new index.html markup/CSS uses lineatiempo-split* / lineatiempo-detail* names only.
- showEventDetail existing callers (eventos table rows, Cronología cards, applyHash) keep working unchanged.
- test.sh rules: static-structure tests run on host via `./test.sh --unit ...`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 50-50 split markup/CSS + context-aware detail state machine in index.html</name>
  <files>src/eth_pipeline/static/index.html</files>
  <action>
1. Restructure the tab-lineatiempo section (lines 1491-1494) into a split wrapper: `<div id="lineatiempo-split">` containing `<div id="lineatiempo-split-timeline">` (wrap the existing lineatiempo-loading + lineatiempo-container inside it) and `<div id="lineatiempo-split-detail">` (empty host, plus the close button: `<button id="lineatiempo-detail-close" class="btn-icon" type="button" aria-label="Cerrar detalle">&times;</button>`).
2. Add CSS (do NOT use the lt2- prefix anywhere): `#lineatiempo-split { display:flex; gap:16px; align-items:flex-start; }`; default `#lineatiempo-split-detail { display:none; }`; when `#lineatiempo-split.split-open`: detail pane becomes `display:block; flex:1 1 50%; min-width:0; position:relative; background:#fff; border:1px solid #e2e8f0; border-radius:8px; padding:16px;` and the timeline pane `flex:1 1 50%; min-width:0;` (the lt2-scroller inside already handles horizontal overflow at half width). Add a media query (max-width ~900px) switching the split to `flex-direction:column` so it stacks on narrow screens. Position #lineatiempo-detail-close absolute top-right inside the detail pane, only visible under .split-open.
3. Detail state machine — refactor around the existing showEventDetail/hideEventDetail (line ~3330/~3444) without changing their external contract:
   - Add `let detailContext = 'eventos';` and `let lineatiempoEventId = null;` next to currentEventId (~3309). Capture `const eventDetailOriginalParent = eventDetailPanel.parentElement;` once at init.
   - Extract the fetch/render body of showEventDetail (everything after the panel/container toggles) into `async function loadEventDetail(eventId)`; both contexts call it.
   - `function openEventDetail(eventId, context)`: set detailContext/currentEventId; if context === 'lineatiempo': set lineatiempoEventId = eventId, append eventDetailPanel into #lineatiempo-split-detail, add 'split-open' to #lineatiempo-split, show the panel (do NOT touch eventosContainer), call window.markLineaTiempoSelection(eventId) if defined, syncHash(), then loadEventDetail(eventId). If context === 'eventos': defensively restore the panel to eventDetailOriginalParent and remove 'split-open' if it was left in the timeline pane, then keep the exact current showEventDetail behavior (hide eventosContainer, show panel, syncHash, loadEventDetail). Redefine `showEventDetail(eventId)` as `openEventDetail(eventId, 'eventos')` so every existing caller is unchanged.
   - `function closeTimelineEventDetail(opts)`: clear lineatiempoEventId, detailContext='eventos', currentEventId, currentEventDetail; hide panel; move eventDetailPanel back to eventDetailOriginalParent; remove 'split-open'; call window.markLineaTiempoSelection(null) if defined; call syncHash() unless opts.skipSync. It must NOT call fetchEventos().
   - `hideEventDetail()`: route by context — if detailContext === 'lineatiempo', call closeTimelineEventDetail() and return; otherwise keep the exact current eventos behavior (including fetchEventos()).
   - `function toggleEventDetailFromTimeline(eventId)`: if currentTab === 'lineatiempo' && lineatiempoEventId === eventId → closeTimelineEventDetail(); else openEventDetail(eventId, 'lineatiempo'). Expose as window.toggleEventDetailFromTimeline.
   - Wire #lineatiempo-detail-close click → hideEventDetail() (routes by context).
4. Hash wiring: in syncHash, add a lineatiempo branch mirroring the eventos one — `if (currentTab === 'lineatiempo' && lineatiempoEventId) parts.push('event=' + lineatiempoEventId);`. In applyHash, after onTabClick(state.tab): if state.tab === 'lineatiempo' and a valid event option (/^[A-Za-z0-9-]+$/ — reuse the same regex the eventos branch uses) → openEventDetail(opts.event, 'lineatiempo'); if state.tab === 'lineatiempo' with NO valid event option and lineatiempoEventId is set → closeTimelineEventDetail({ skipSync: true }) so browser back from the detail closes it (the final syncHash() at the end of applyHash writes the canonical hash). In onTabClick, first line: if tabName !== 'lineatiempo' && lineatiempoEventId → closeTimelineEventDetail({ skipSync: true }) — switching tabs always closes the timeline detail and returns the panel to the eventos tab.
5. In the universal refresh dispatch (~1749), for lineatiempo after window.refreshLineaTiempo(): re-apply the highlight — `if (lineatiempoEventId && window.markLineaTiempoSelection) window.markLineaTiempoSelection(lineatiempoEventId);` (render() rebuilds the DOM and drops it).
6. Hash values flow only into comparisons / openEventDetail(id) (which already encodeURIComponent's its fetch path) — never into innerHTML (T-S7D-01 pattern carries over).
  </action>
  <verify>
    <automated>./test.sh --unit tests/test_hash_navigation.py tests/test_linea_tiempo.py</automated>
  </verify>
  <done>Split markup/CSS exist; openEventDetail/closeTimelineEventDetail/toggleEventDetailFromTimeline wired; syncHash+applyHash handle #s=lineatiempo&event=<id> (open on option, close on absent); switching tabs closes the detail; the shared panel returns to the eventos tab with existing behavior intact; unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: linea-tiempo.js — toggle routing, same-card close, selected-card highlight</name>
  <files>src/eth_pipeline/static/linea-tiempo.js</files>
  <action>
1. In the document-level click handler (~line 469): route BOTH branches (.lt2-event rectangles and .lt2-undated-item rows) through the index.html toggle instead of calling showEventDetail directly — if `typeof window.toggleEventDetailFromTimeline === 'function'` call `window.toggleEventDetailFromTimeline(id)`, else fall back to the old `showEventDetail(id)` guard. This gives same-card-click-to-close for free (index.html owns the lineatiempoEventId comparison) and keeps the file header's "relies on globals defined by index.html" contract.
2. Add `window.markLineaTiempoSelection = function (eventId)`: query #lineatiempo-container for `.lt2-event, .lt2-undated-item` and toggle the class `lt2-selected` on each node whose data-event-id matches eventId (null clears all). Query the container fresh each call — render() rebuilds it.
3. In ensureStyles() CSS add a clear selected style, e.g. `.lt2-event.lt2-selected, .lt2-undated-item.lt2-selected { box-shadow: 0 0 0 3px #2563eb; }` (outline works too — just make the open card obviously highlighted).
4. Update the file header comment's exposed-hooks list: add window.markLineaTiempoSelection (called from index.html on open/close/refresh) and note that card clicks route through window.toggleEventDetailFromTimeline.
  </action>
  <verify>
    <automated>./test.sh --unit tests/test_hash_navigation.py tests/test_linea_tiempo.py</automated>
  </verify>
  <done>Card and undated-item clicks toggle the detail (same card closes it); the open card carries lt2-selected until closed or deselected; the highlight survives a timeline refresh; node --check on linea-tiempo.js passes.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tests/test_hash_navigation.py with timeline split guards</name>
  <files>tests/test_hash_navigation.py</files>
  <action>
Add a "Línea de tiempo event detail split" section mirroring the existing static-assertion style (source string/regex checks, no DB):
1. index.html contains ids lineatiempo-split, lineatiempo-split-timeline, lineatiempo-split-detail, lineatiempo-detail-close (with aria-label "Cerrar detalle"); the close button and detail pane live inside the split markup region.
2. CSS guards: a `#lineatiempo-split` flex rule exists; a `.split-open` rule sets `flex: 1 1 50%` (the 50-50 contract); a media-query stacking rule exists (grep for `flex-direction: column` within the lineatiempo-split CSS block or a max-width media query near it).
3. Script guards (via _inline_app_script): `function openEventDetail`, `function closeTimelineEventDetail`, `function toggleEventDetailFromTimeline`, `window.toggleEventDetailFromTimeline`, `lineatiempoEventId`, and `eventDetailOriginalParent` (panel reparenting) exist; syncHash references lineatiempoEventId; the applyHash body contains the event regex `/^[A-Za-z0-9-]+$/` (already asserted for eventos — assert it also guards the lineatiempo branch, e.g. openEventDetail is called inside applyHash) and references closeTimelineEventDetail (back-to-close); onTabClick body references closeTimelineEventDetail (close on tab switch); #lineatiempo-detail-close click is bound to hideEventDetail; hideEventDetail still calls fetchEventos only on its eventos path (e.g. closeTimelineEventDetail body does NOT contain fetchEventos).
4. linea-tiempo.js guards: the click handler calls window.toggleEventDetailFromTimeline (and no longer calls showEventDetail directly, except as the guarded fallback), window.markLineaTiempoSelection is defined and toggles lt2-selected, and `.lt2-selected` appears in the ensureStyles CSS.
5. Re-run-existing guarantee: the no-lt2-leakage test and node --check syntax gate must still pass — do not introduce "lt2-" into index.html.
  </action>
  <verify>
    <automated>./test.sh --unit tests/test_hash_navigation.py tests/test_linea_tiempo.py</automated>
  </verify>
  <done>All new guards pass alongside the existing hash-navigation, linea-tiempo, and no-lt2-leakage tests; full unit suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash → app state | location.hash (event=<id>) is user-controlled input crossing into view state |
| API → detail panel | /events/<id> response rendered into the shared panel (pre-existing boundary, reused) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-T6C-01 | Tampering/Elevation | applyHash event option (lineatiempo branch) | low | mitigate | Same /^[A-Za-z0-9-]+$/ regex as the eventos branch; the id only flows into openEventDetail → encodeURIComponent fetch path, never into innerHTML |
| T-T6C-02 | Tampering | panel reparenting (appendChild) | low | accept | Moves an existing trusted DOM node; no new HTML is built; listeners are preserved, not re-created |
| T-T6C-SC | Tampering | npm/pip installs | high | mitigate | No new packages added by this plan; stdlib/pytest only |
</threat_model>

<verification>
- `./test.sh --unit tests/test_hash_navigation.py tests/test_linea_tiempo.py` green (includes node --check syntax gate and the no-lt2-leakage guard).
- Manual (dev stack, ./run.sh): open Línea de tiempo → click a card → 50-50 split with detail right, timeline left, card highlighted; click the SAME card → detail closes; × button closes; "Volver" closes; click card, switch to Eventos → detail closed, eventos detail behavior unchanged; open detail, copy `#s=lineatiempo&event=<id>` into a fresh tab → split restored with that event; browser back from the detail closes it; open detail, hit the universal refresh button → timeline refetches and highlight returns.
</verification>

<success_criteria>
All must_haves truths observable in the running UI; all unit tests pass; no regression in existing static-structure guards (test_linea_tiempo.py) or existing eventos detail behavior; files committed per task.
</success_criteria>

<output>
Create /home/u/src/eth/.planning/quick/260906-t6c-timeline-event-card-click-opens-the-even/260906-t6c-SUMMARY.md when done
</output>
