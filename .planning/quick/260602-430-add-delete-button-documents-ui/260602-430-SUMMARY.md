---
status: completed
---

## Summary

Added a delete button (SVG trash icon) to each row in the documents table view.

### Changes

- Added `.btn-icon` CSS class with hover-to-red styling for the delete button
- Added `.col-actions` CSS class for the Actions column
- Added `<th>Actions</th>` column header to the documents table
- Added SVG trash icon delete button to each document row in `renderDocuments()`
- Added event delegation click handler on `docTbody` for `.btn-delete` buttons
- Delete flow: confirmation dialog → disable button → `DELETE /documents/{id}` → re-fetch on success
- Error handling: re-enables button and shows error banner on failure
