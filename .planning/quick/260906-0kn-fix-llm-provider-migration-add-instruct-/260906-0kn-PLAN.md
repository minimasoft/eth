---
phase: quick
plan: 260906-0kn
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/llm.py
autonomous: true
requirements: [QUICK-260906-0KN]
user_setup: []

must_haves:
  truths:
    - Alembic head 0005 is applied to the dev database (alembic_version = '0005')
    - llm_provider has instruct_temperature, instruct_top_p, instruct_top_k columns; document has llm_mode
    - Existing llm_provider rows are intact (api_key preserved, no data loss)
    - GET /ui/providers no longer errors with 'column "instruct_temperature" does not exist'
    - The pending llm.py prompt change is committed
  artifacts:
    - Git commit containing src/eth_pipeline/llm.py change
  key_links:
    - Alembic revision 0005 → dev DB schema (docker compose run --rm api uv run alembic upgrade head)
    - providers.py SELECT → new instruct_* columns present
---

<objective>
Apply the existing Alembic revision 0005 (instruct params + llm_mode) to the dev database so the providers API stops erroring, and commit the pending llm.py prompt change that belongs to the same change set.

Purpose: The dev DB is stamped at 0004 while the code SELECTs instruct_temperature/instruct_top_p/instruct_top_k from llm_provider, so GET /ui/providers fails. No new revision — 0005 already exists and is correct.

Output: Dev DB upgraded to 0005, verified; one atomic commit of src/eth_pipeline/llm.py.
</objective>

<context>
@src/eth_pipeline/alembic/versions/0005_llm_mode_instruct_params.py
@AGENTS.md (Database & migrations + Gotchas sections)

Facts verified by the orchestrator:
- Dev DB alembic_version = '0004'; migration 0005 adds three nullable columns to llm_provider and llm_mode (String, server_default 'thinking') to document.
- Columns are nullable; NULL means "use module defaults" (INSTRUCT_* constants in src/eth_pipeline/llm.py) — no backfill needed, existing api_key values untouched.
- Do NOT touch schema.sql (pre-0001 baseline only). Do NOT create a new revision.
- Alembic runs in containers per AGENTS.md; dev stack is currently up.
- One uncommitted change: src/eth_pipeline/llm.py (one-line addition to EVENT_EXTRACTION_V7_SYSTEM_PROMPT warning against overthinking) — commit it here.
- Postgres service: `postgres` (user/password/db: eth).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the pending llm.py prompt change</name>
  <files>src/eth_pipeline/llm.py</files>
  <action>Run `git diff src/eth_pipeline/llm.py` to confirm the only change is the one-line addition to EVENT_EXTRACTION_V7_SYSTEM_PROMPT warning against overthinking. Check `git status` for stray files and stage ONLY src/eth_pipeline/llm.py. Commit with message: `fix(llm): warn against overthinking in v7 extraction system prompt`. Do not include .planning/ files or anything else in this commit.</action>
  <verify>
    <automated>git status --short src/eth_pipeline/llm.py | grep -c . && exit 1 || true; git log -1 --stat --oneline | grep -q 'llm.py' && echo OK</automated>
  </verify>
  <done>src/eth_pipeline/llm.py change is committed alone; worktree shows no modification to llm.py; no unrelated files in the commit.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Alembic 0005 to dev database and verify schema</name>
  <files>src/eth_pipeline/alembic/versions/0005_llm_mode_instruct_params.py (already exists — apply only, no edits)</files>
  <action>Per AGENTS.md, run the upgrade in a container: `docker compose run --rm api uv run alembic upgrade head`. Then verify inside the dev postgres container (read-only checks, no raw-SQL "repairs"):
1. `docker compose exec postgres psql -U eth -d eth -tAc "SELECT version_num FROM alembic_version"` → must print `0005`.
2. `docker compose exec postgres psql -U eth -d eth -tAc "SELECT column_name FROM information_schema.columns WHERE table_name='llm_provider' AND column_name IN ('instruct_temperature','instruct_top_p','instruct_top_k')"` → must return all three.
3. `docker compose exec postgres psql -U eth -d eth -tAc "SELECT count(*) FROM information_schema.columns WHERE table_name='document' AND column_name='llm_mode'"` → must print `1`.
4. Confirm existing rows intact: `docker compose exec postgres psql -U eth -d eth -tAc "SELECT count(*), count(api_key) FROM llm_provider"` — row count unchanged from before upgrade (note the pre-upgrade count first) and api_key count equals row count.

If anything fails, do NOT "repair" by re-running raw SQL (AGENTS.md) — report the actual alembic error instead.</action>
  <verify>
    <automated>docker compose run --rm api uv run alembic current | grep -q '0005' && echo SCHEMA_OK</automated>
  </verify>
  <done>alembic_version = 0005 on the dev DB; all three instruct_* columns exist on llm_provider; llm_mode exists on document; llm_provider row and api_key counts unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify providers API route works end-to-end</name>
  <files>[]</files>
  <action>Confirm the original symptom is gone using the running dev stack (no DB writes):
1. Direct DB sanity (mirror of the failing SELECT in src/eth_pipeline/providers.py): `docker compose exec postgres psql -U eth -d eth -tAc "SELECT id, name, instruct_temperature, instruct_top_p, instruct_top_k FROM llm_provider LIMIT 3"` — must return rows with NULL instruct params (expected; NULL = module defaults per llm.py INSTRUCT_* constants).
2. Hit the live endpoint via the running api container: `docker compose exec api python -c "import urllib.request; r=urllib.request.urlopen('http://localhost:8000/ui/providers'); print(r.status)"` (adjust port/path if the api container's internal bind differs — check `rg -n "uvicorn|port" docker-compose.yml` first). Must return HTTP 200 with a JSON body, no 'column "instruct_temperature" does not exist' error.
3. If api container networking makes the check awkward, fall back to `docker compose logs api --tail 50` after hitting the URL from the host at the published port and confirm no SQLAlchemy UndefinedColumn errors.</action>
  <verify>
    <automated>docker compose exec postgres psql -U eth -d eth -tAc "SELECT 1 FROM llm_provider LIMIT 1" >/dev/null && docker compose logs api --tail 50 | grep -ci 'instruct_temperature.*does not exist' | grep -qx '0' && echo API_OK</automated>
  </verify>
  <done>GET /ui/providers returns 200 (or an auth response as designed) with no UndefinedColumn/instruct_temperature errors in api logs; instruct params read back as NULL on existing rows.</done>
</task>

</tasks>

<verification>
- `docker compose run --rm api uv run alembic current` reports 0005
- information_schema confirms instruct_temperature/instruct_top_p/instruct_top_k on llm_provider and llm_mode on document
- llm_provider row/api_key counts unchanged from pre-upgrade
- GET /ui/providers succeeds; api logs clean of UndefinedColumn errors
- git log shows exactly one new code commit touching src/eth_pipeline/llm.py; schema.sql untouched; no new Alembic revision created
</verification>

<success_criteria>
Dev database is on Alembic revision 0005 with the instruct params live, existing provider records and API keys intact, the providers UI route functional again, and the llm.py prompt change committed atomically.
</success_criteria>

<output>
On completion, update .planning/STATE.md (Quick Tasks Completed table) and create
.planning/quick/260906-0kn-fix-llm-provider-migration-add-instruct-/260906-0kn-SUMMARY.md
</output>
