---
phase: quick-260906-ql3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/static/index.html
  - src/eth_pipeline/static/providers.html
  - src/eth_pipeline/api/routes/comparisons.py
  - src/eth_pipeline/api/routes/documents.py
  - src/eth_pipeline/api/__init__.py
  - src/eth_pipeline/api/models.py
  - tests/test_v7_workflow.py
  - tests/test_passcodes.py
autonomous: true
requirements: [QUICK-01, QUICK-02, QUICK-03, QUICK-04, QUICK-05]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Events view (Eventos table chips, eventos timeline lanes, event detail modal) shows the DB-assigned model colors from GET /events/colors — same TABLEAU20 palette and per-model assignment the Línea de tiempo tab uses (QUICK-01)
    - No comparison UI, no /comparisons API route, and no comparison JS remains in the app (QUICK-02)
    - Uploading a document creates exactly one document row — the fan-out loop is gone (QUICK-03)
    - The upload picker offers exactly one model selection (plus mode), options labeled by model name, with a default option that uses the env-backed default provider (QUICK-03)
    - The page title is "¿Qué? ¿Cuándo? ¿Dónde?" (QUICK-04)
    - Browsers issue no /favicon.ico request — no 404 in server logs (QUICK-05)
  artifacts:
    - src/eth_pipeline/static/index.html (modified: DB-backed colors, comparison removed, single-select upload, title, favicon link)
    - src/eth_pipeline/static/providers.html (favicon link)
    - src/eth_pipeline/api/routes/documents.py (single provider upload)
    - tests/test_v7_workflow.py (fanout test rewritten to single-provider)
  key_links:
    - GET /events/colors → modelChip color resolution in index.html (must be keyed by item.model, TABLEAU20, hash fallback)
    - POST /documents/upload provider_id form field → single INSERT INTO document
---

<objective>
Frontend cleanup quick task: (1) adopt the DB-backed model colors (tableau20 via /events/colors, as designed in the Línea de tiempo view) in the events view; (2) remove the cross-model comparison feature and the multi-provider fan-out; (3) reduce document loading to a single model+mode pick with a default option labeled by model name; (4) retitle the page "¿Qué? ¿Cuándo? ¿Dónde?"; (5) add a no-op favicon so no 404s.

Purpose: Eventos tab currently invents its own palette instead of the DB-assigned colors, and the comparison/fan-out features are being retired.
Output: Updated static UI, slimmed upload endpoint, comparison route deleted, tests updated.
</objective>

<execution_context>
@/home/u/src/eth/.opencode/gsd-core/workflows/execute-plan.md
@/home/u/src/eth/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@brief Upload UI is inline JS inside a single-file app; backend is FastAPI serving static/ under /ui.
@src/eth_pipeline/static/index.html  # 4216 lines — ALL frontend changes live here (inline <script>)
@src/eth_pipeline/static/linea-tiempo.js  # lines 18, 95-129: the "as designed" color system to replicate (TABLEAU20, hashModel, colorFor, fetchColors via GET /events/colors)
@src/eth_pipeline/api/routes/documents.py  # upload_document (lines 200-376): provider_ids fan-out to remove
@src/eth_pipeline/api/routes/events_v2.py  # GET /events/colors contract (model → color_index)
@tests/test_v7_workflow.py  # test_fanout_creates_two_documents_with_distinct_models (line 243) — rewrite
@tests/test_passcodes.py  # line 209 registers comparisons route — remove entry
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-backed model colors in the events view (QUICK-01)</name>
  <files>src/eth_pipeline/static/index.html</files>
  <action>
In the inline script of index.html, replace the local 8-color MODEL_PALETTE / modelPalette(key) system with the same color system linea-tiempo.js uses (per QUICK-01):

1. Add a TABLEAU20 constant (20 RGB triples) identical to linea-tiempo.js line 18, plus the same hashModel(model) string-hash (h = h*31 + charCode, masked to 0x7fffffff) and rgb(index) helpers.
2. Add a module-level cache `modelColorIndex` (model string → color_index | null) populated once by fetching GET /events/colors with fetchWithC, exactly like fetchColors() in linea-tiempo.js (map[item.model] = item.color_index when non-null; store null marker otherwise). Fetch it lazily before first chip render — e.g. kick it off at script init alongside other initial loads, and have the color resolver await-or-default so a failed fetch degrades to the hash fallback instead of breaking rendering.
3. Rewrite modelPalette/modelChip so the resolved color is keyed by the MODEL NAME ONLY (item.model; fall back to item.provider_name when model is empty — no more "provider · model" composite key in modelKey for color purposes). Keep modelKey for lane grouping if the eventos timeline lanes use it, but colors must come from the DB map.
4. Chip styling: keep the existing .model-chip look (colored dot + tinted pill). Derive the three shades from the assigned TABLEAU20 color: text/border/dot = the full rgb() color; background = rgba(same, 0.08); pill border = rgba(same, 0.25). Update the eventos timeline lane swatches/points (renderEventosTimeline uses modelPalette around lines 3074 and 3117) to use the same resolved color.
5. modelChip is used by the events table (line ~2945), eventos timeline (3074, 3117), event detail modal (3317) and comparison rendering (3583) — Task 2 deletes the comparison call site; leave the others working through the new resolver.
6. Delete MODEL_PALETTE and the old hash-based modelPalette once no call sites remain.

Do NOT touch linea-tiempo.js — it is already correct.
  </action>
  <verify>
    <automated>grep -c "events/colors" src/eth_pipeline/static/index.html | grep -qv '^0$' && ! grep -q "MODEL_PALETTE" src/eth_pipeline/static/index.html && ! grep -q "TABLEAU20" src/eth_pipeline/static/linea-tiempo.js.disabled 2>/dev/null; grep -c "TABLEAU20" src/eth_pipeline/static/index.html | grep -qv '^0$'</automated>
  </verify>
  <done>index.html contains a TABLEAU20 palette, an /events/colors fetch cached into a model→color_index map, and no MODEL_PALETTE constant; modelChip and eventos timeline colors resolve from the DB index with hash fallback keyed by model name.</done>
</task>

<task type="auto">
  <name>Task 2: Remove comparison feature and upload fan-out (QUICK-02, QUICK-03)</name>
  <files>src/eth_pipeline/static/index.html, src/eth_pipeline/api/routes/comparisons.py, src/eth_pipeline/api/__init__.py, src/eth_pipeline/api/models.py, src/eth_pipeline/api/routes/documents.py, tests/test_v7_workflow.py, tests/test_passcodes.py</files>
  <action>
Backend — remove the comparison route (per QUICK-02):
1. Delete src/eth_pipeline/api/routes/comparisons.py entirely.
2. In api/__init__.py remove the comparisons_router import (line ~86) and the app.include_router call (line ~96).
3. In api/models.py remove the ComparisonDocument, ComparisonEvent, ComparisonResponse classes and their names from the __all__-style export list (around lines 651-723 and the export block near 798).

Backend — remove fan-out from upload (per QUICK-03):
4. In documents.py upload_document: change the form signature from `provider_ids: Annotated[list[str], Form()] = []` to a single optional `provider_id: Annotated[str | None, Form()] = None`. Replace the fan-out list (`unique_ids`/`selected` loop, lines ~236-240) with a single `_resolve_provider(provider_id)` call — None resolves to the env-backed default provider, which is the existing behavior of _resolve_provider. Replace the multi-row INSERT loop (line ~314 `for _provider in selected:`) with one INSERT of the resolved provider's id/model. Keep the response model unchanged (document_ids still returned as a one-element list, source_id still generated) so no schema migration or client breakage beyond this endpoint's contract.
5. Update the log line at ~358 to drop the fanout count.

Frontend — remove comparison UI:
6. In index.html delete: the `tab-btn-compare` button (line ~1311), the whole `tab-compare` section (lines ~1564-1600 area), the `compare` entry in the tabs element map (line ~1700), the "Comparison view" CSS block (line ~1235) and all `.cmp-*` CSS rules, the cross-model comparison JS block (openComparison, fetchComparison, clusterComparisonEvents, renderComparison — lines ~3399-3621 including the cmp-refresh-btn listener), the src-chip / btn-compare event-delegation wiring (lines ~2744-2749), and the src-chip / btn-compare rendering inside the documents table row builder (lines ~2166-2175).

Tests:
7. Rewrite tests/test_v7_workflow.py test_fanout_creates_two_documents_with_distinct_models (line 243) into a single-provider test: upload with one `provider_id` form field (and one case with no provider field → default) asserts exactly ONE document row is created and its model matches the selected provider. Keep the multipart helper style already used in that test.
8. In tests/test_passcodes.py remove the comparisons route tuple (line 209) from the passcode-protected route list.
  </action>
  <verify>
    <automated>./test.sh tests/test_v7_workflow.py tests/test_passcodes.py && ! grep -rqi "comparison\|comparaci" src/eth_pipeline/api/routes/ src/eth_pipeline/api/__init__.py && ! grep -q "provider_ids" src/eth_pipeline/api/routes/documents.py tests/test_v7_workflow.py && ! grep -q "btn-compare\|tab-compare\|openComparison\|cmp-refresh" src/eth_pipeline/static/index.html</automated>
  </verify>
  <done>GET /comparisons/* no longer exists (404 from router absence), the upload endpoint inserts exactly one document row per file, no comparison UI/JS/CSS remains in index.html, and the updated workflow + passcode tests pass in the disposable test stack.</done>
</task>

<task type="auto">
  <name>Task 3: Single model picker (by model name, with default), title, favicon (QUICK-03, QUICK-04, QUICK-05)</name>
  <files>src/eth_pipeline/static/index.html, src/eth_pipeline/static/providers.html</files>
  <action>
Single model picker (per QUICK-03):
1. In index.html replace the checkbox list built by renderUploadProviders (lines ~1807-1831) with a single-select `<select id="upload-provider">` inside the existing #provider-select div. Options: first an explicit default option (label "Predeterminado" — or the default provider's model name if present in the /api/providers payload, e.g. "Predeterminado (gpt-x)"; value empty string), then one option per non-default provider labeled by the provider's MODEL NAME (p.model), falling back to p.name only if model is empty — never label by provider name. Exactly one option can be chosen (native select semantics).
2. Update getSelectedProviderIds/upload click handler (~lines 1802-1805, 1880-1881): when the select has a value, append a single `provider_id` form field; when the default option is chosen, send no provider_id at all (backend falls back to the env default provider). Remove the old checkbox-based helper.
3. Keep the "Modo:" select (llm-mode) as-is — it is already a single choice; only confirm it remains visible with the new picker.

Title (per QUICK-04):
4. Change the `<title>` at index.html line 6 to `¿Qué? ¿Cuándo? ¿Dónde?` (with the Spanish opening question marks, exactly).

Favicon (per QUICK-05):
5. Add to the <head> of BOTH index.html and providers.html, before the title: a `<link rel="icon">` pointing at an inline data URI SVG that is a plain white 16×16 rect (transparent background acceptable), e.g. `data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16'%3E%3Crect width='16' height='16' fill='%23ffffff'/%3E%3C/svg%3E`. The data-URI link tag stops browsers from requesting /favicon.ico, which is what currently 404s — no new file, no route needed.
  </action>
  <verify>
    <automated>./test.sh --unit && grep -q "<title>¿Qué? ¿Cuándo? ¿Dónde?</title>" src/eth_pipeline/static/index.html && grep -c 'rel="icon"' src/eth_pipeline/static/index.html src/eth_pipeline/static/providers.html | grep -qv ':0$' && grep -q 'id="upload-provider"' src/eth_pipeline/static/index.html && ! grep -q 'name="provider_ids"' src/eth_pipeline/static/index.html</automated>
  </verify>
  <done>Upload card shows one select labeled by model names with a Predeterminado option; title reads ¿Qué? ¿Cuándo? ¿Dónde?; both HTML pages carry the inline favicon link; unit suite passes.</done>
</task>

</tasks>

<verification>
- `./test.sh tests/test_v7_workflow.py tests/test_passcodes.py` — upload single-row behavior and passcode route list
- `./test.sh --unit` — fast host sanity
- `./test.sh` full suite before finishing (the upload endpoint changed)
- Grep gates: no comparison/fanout identifiers remain in src/ (excluding `localeCompare` false positives — match on `comparison`, `comparaci`, `btn-compare`, `tab-compare`, `openComparison`, `provider_ids`); index.html has TABLEAU20 + /events/colors fetch and no MODEL_PALETTE
- Manual (optional, dev stack via ./run.sh): upload a doc with default option → one row in Documentos; Eventos tab chips match Línea de tiempo column colors; browser console/network shows no /favicon.ico request
</verification>

<success_criteria>
All five cleanup items delivered: events view uses DB-assigned model colors, comparison and fan-out features fully removed, document loading is single model+mode with a default option labeled by model name, title is ¿Qué? ¿Cuándo? ¿Dónde?, and no favicon 404. Test suite green.
</success_criteria>

<output>
Create .planning/quick/260906-ql3-cleanup-1-model-colors-in-events-view-re/260906-ql3-SUMMARY.md when done
</output>
