---
phase: quick-260906-kj0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/api/routes/documents.py
  - src/eth_pipeline/api/routes/events_v2.py
  - src/eth_pipeline/api/routes/geo.py
  - src/eth_pipeline/api/routes/providers.py
  - src/eth_pipeline/api/routes/comparisons.py
  - src/eth_pipeline/passcodes.py
  - tests/test_passcodes.py
  - src/eth_pipeline/static/index.html
  - src/eth_pipeline/static/providers.html
autonomous: true
estimate:
  tokens: 40000
  raw_tokens: 28000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Every data-returning GET endpoint returns 422 (missing passcode), 403 (empty/wrong/wrong-level passcode), or 200 (valid C passcode) — no data path exists without a valid C code
    - The documents view in the UI shows the C passcode modal on first load and fires no data fetch before the modal resolves
    - All UI read fetches (documents, events, geo, providers, comparisons, chunks, logs, llm-calls, tokens) send the stored C passcode
    - Level A/B codes are rejected on read endpoints (levels do not imply each other)
    - GET /health and GET /api/passcode/check remain open (healthcheck + bootstrap)
---

<objective>
Close the passcode enforcement gap left by 260906-jd7: all read endpoints currently serve data without any passcode. Apply the existing `require_passcode("C")` decorator to every data-returning read endpoint (missing param → FastAPI 422, wrong code → generic 403, per the jd7 established pattern), and wire the UI to send the stored C passcode on every read fetch, fixing the startup ordering so the initial documents fetch waits for the C modal.

Purpose: The app is exposed via a Cloudflare tunnel; jd7 only gated mutations, so documents, events, extraction data, providers, and comparison results were publicly readable.
Output: Decorated read endpoints, updated tests, gated read fetches in index.html and providers.html.
</objective>

<context>
@.planning/quick/260906-jd7-implement-basic-passcode-permissions-for/260906-jd7-SUMMARY.md
@src/eth_pipeline/passcodes.py
@tests/test_passcodes.py
@AGENTS.md

Existing pattern (reuse exactly — do not invent a second mechanism):
- `require_passcode(level)` in `src/eth_pipeline/passcodes.py`: wrapper adds keyword-only `passcode: str = Query(...)`, generic 403 for empty/wrong codes. Decorator goes BENEATH the route decorator (`@router.get(...)` on top, `@require_passcode("C")` below), as already done for the six mutating endpoints.
- Levels are independent: A never satisfies B, and neither satisfies C. A valid A or B code on a C endpoint must 403.
- UI localStorage keys: `eth_passcode_C` (index.html line ~4049 `PASSCODE_KEYS`; same helpers `getStoredPasscode`/`storePasscode`/`clearStoredPasscode`/`ensurePasscode`/`checkPasscode` exist in BOTH index.html and providers.html — duplicate definitions, keep that structure, edit each file's own helpers).

Known UI startup bug (index.html bottom block, ~lines 4144-4154): the C modal runs as a fire-and-forget async IIFE while `fetchDocuments()` is called unconditionally right after — the initial fetch does NOT wait for the modal. With server-side enforcement this fetch gets 422 and the table stays empty.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decorate all 11 read endpoints with require_passcode("C") + tests</name>
  <files>src/eth_pipeline/api/routes/documents.py, src/eth_pipeline/api/routes/events_v2.py, src/eth_pipeline/api/routes/geo.py, src/eth_pipeline/api/routes/providers.py, src/eth_pipeline/api/routes/comparisons.py, src/eth_pipeline/passcodes.py, tests/test_passcodes.py</files>
  <behavior>
    - GET /documents, /documents/{id}, /documents/{id}/chunks/{part_index}, /documents/{id}/logs, /documents/{id}/llm-calls, /documents/{id}/tokens, /events, /events/{event_id}, /geo/events, /api/providers, /comparisons/{source_id} with NO passcode param → 422
    - Same endpoints with empty (`?passcode=`) or wrong code → 403 with generic detail "Passcode required."
    - Same endpoints with a valid A or B code (not C) → 403 (levels do not imply each other)
    - Same endpoints with valid C code → handler runs (200/404 depending on data)
    - GET /health and GET /api/passcode/check → still reachable WITHOUT any passcode
  </behavior>
  <action>
    Extend tests/test_passcodes.py first (RED): add unit tests for the C-on-reads contract above, following the existing TestClient patterns in that file (mock DB dependencies the same way existing tests do; a 404 from a mocked-DB miss still proves the decorator passed). Then decorate (GREEN), placing `@require_passcode("C")` directly beneath each `@router.get(...)` in:
    - documents.py: `get_api_info` (GET /), `list_documents`, `get_document`, `get_chunk_text`, `get_document_logs`, `get_document_llm_calls`, `get_document_tokens`
    - events_v2.py: `list_events` (GET /events), `get_event` (GET /events/{event_id})
    - geo.py: the GET /geo/events handler
    - providers.py: `list_providers` (GET /api/providers)
    - comparisons.py: the GET /comparisons/{source_id} handler
    Import `require_passcode` in files that don't already import it (events_v2.py, geo.py, comparisons.py — check each).

    Deliberate exceptions — do NOT decorate:
    - GET /health: docker-compose.yml line 131 healthcheck polls http://localhost:8001/health with no passcode; protecting it marks the container unhealthy and can block dependent services. It returns no pipeline data.
    - GET /api/passcode/check: bootstrap/validation endpoint; the UI needs it to validate a C code before any read is possible. It returns only a level letter for the submitted code.

    Update the `passcodes.py` module docstring: the line describing level C as "reads need nothing" is now wrong — C is the read level required by all data-returning GETs. Also update the "reads stay open" framing in the opening paragraph.
  </action>
  <verify>
    <automated>./test.sh --unit</automated>
  </verify>
  <done>All new read-enforcement tests pass; full unit suite green (was 83 passed / 4 skipped); all 11 read endpoints decorated; /health and /api/passcode/check untouched.</done>
</task>

<task type="auto">
  <name>Task 2: index.html — send C passcode on all read fetches + fix startup ordering</name>
  <files>src/eth_pipeline/static/index.html</files>
  <action>
    1. Add a small helper next to the existing passcode helpers (e.g. `function readUrl(url)`) that appends `passcode=` with the stored C code (`getStoredPasscode('C')`, URL-encoded) to a URL, handling both URLs with and without an existing query string. If no C code is stored, still append the empty value so the server 403s and the caller can re-prompt.
    2. Apply it to every data-reading fetch in index.html: fetch('/api/providers') (~line 1814, provider/model dropdown), fetchDocuments (~2086), document logs (~2397), tokens (~2488), llm-calls (~2538), events list (~2804 and ~2850), documents per_page=100 loop (~3187), event detail (~3266), comparisons detail (~3407), chunk text (~3745 and the next-chunk fetch ~3784), geo events (~3993).
    3. Handle 403 on reads: when a read fetch receives 403, clear the stored C code (`clearStoredPasscode('C')`), await `ensurePasscode('C')`, and retry once. Implement this in a shared wrapper rather than copy-pasting per call site where practical (e.g. a `fetchWithC(url)` async helper returning the Response, so all read call sites collapse to one line).
    4. Fix the startup ordering (the confirmed gap): replace the fire-and-forget IIFE + unconditional `fetchDocuments()` with a gated flow — if a C code is stored, `fetchDocuments()` proceeds; otherwise await `ensurePasscode('C')` and only call `fetchDocuments()` after it resolves (skip the fetch if the user cancels — leave the table empty, matching providers.html cancel behavior of not loading). The C modal must appear on first load of the documents view before any data hits the table.
    5. Keep all existing behavior for A/B (upload, deletes, gear) untouched.
  </action>
  <verify>
    <automated>grep -c "fetchWithC\|readUrl" src/eth_pipeline/static/index.html && grep -n "fetch('/documents\|fetch('/events\|fetch('/geo/events\|fetch('/comparisons\|fetch('/api/providers'" src/eth_pipeline/static/index.html | grep -v passcode || true</automated>
  </verify>
  <done>Every read fetch in index.html sends the C passcode; on first visit with no stored C code the modal blocks all initial data loading (no fetch fires before the modal resolves); after entering a valid C code the documents table loads; wrong code re-prompts; cancel leaves the view empty.</done>
</task>

<task type="auto">
  <name>Task 3: providers.html — C gate for the provider list fetch</name>
  <files>src/eth_pipeline/static/providers.html</files>
  <action>
    1. In `loadProviders()` (~line 529), send the stored C passcode as a query param using the file's own passcode helpers (same `readUrl`-style helper as Task 2, using providers.html's local `getStoredPasscode`).
    2. Gate the list load on C: before calling loadProviders at startup, ensure a valid C code — if `getStoredPasscode('C')` is empty, await `ensurePasscode('C')` (providers.html already has identical modal helpers, ~line 739-820); on cancel, redirect to /ui (consistent with the existing A-gate cancel behavior). Note the page's A gate does NOT satisfy C — both may prompt on first visit; C should be requested for the list load and A remains for create/test actions.
    3. On 403 from the list fetch, clear stored C, re-prompt via ensurePasscode('C'), and retry once (mirror index.html behavior).
  </action>
  <verify>
    <automated>grep -n "loadProviders" src/eth_pipeline/static/providers.html | head -5 && grep -c "getStoredPasscode('C')\|ensurePasscode('C')" src/eth_pipeline/static/providers.html</automated>
  </verify>
  <done>GET /api/providers from providers.html always carries a C passcode; first visit without stored C prompts the C modal before the list renders; cancel redirects to /ui; create/test/delete flows unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Cloudflare tunnel → API | Untrusted readers hit all GET endpoints; reads previously served pipeline data (documents, events, LLM logs, costs) with no gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-KJ0-01 | Information Disclosure | all 11 data-returning GET endpoints | high | mitigate | `require_passcode("C")` on each; missing → 422, wrong → uniform generic 403, no level leakage (reuse jd7 mechanism verbatim) |
| T-KJ0-02 | Spoofing | client-supplied passcode param | low | mitigate | Constant-time `hmac.compare_digest` (existing); A/B codes rejected on C endpoints (levels independent) |
| T-KJ0-03 | Denial of Service | GET /health left open | low | accept | Unauthenticated health probe exposes only liveness; required by docker-compose healthcheck (line 131) |
| T-KJ0-04 | Information Disclosure | GET /api/passcode/check left open | low | accept | Returns only a level letter for a submitted code; needed as bootstrap to validate C before first read; jd7 verified no level enumeration leak |
| T-KJ0-05 | Tampering | localStorage-stored C code | low | accept | Same trust model as jd7's stored A/B codes; server re-validates every request |
</threat_model>

<verification>
1. `./test.sh --unit` — full suite green including new read-enforcement tests.
2. Live (dev stack, requires `docker compose build api && docker compose up -d api` since the api image bakes code — per jd7 deviation note 4):
   - `curl -i http://localhost:8001/documents` → 422; `curl -i "http://localhost:8001/documents?passcode="` → 403; `curl -i "http://localhost:8001/documents?passcode=AAAAA"` → 403 (A is not C); `curl -i "http://localhost:8001/documents?passcode=CCCCC"` → 200.
   - Spot-check one endpoint per router: /events, /geo/events, /api/providers, /comparisons/{id} → same 422/403/200 pattern.
   - `curl -i http://localhost:8001/health` → 200 (still open).
3. Browser: clear localStorage, open /ui/ → C modal appears before any data loads; enter C code → documents table fills; open /ui/providers → list renders (C prompted if not already stored); entering the A code on the C modal is rejected.
</verification>

<success_criteria>
- No data-returning endpoint is reachable without a valid C passcode (422/403 enforced server-side).
- The UI never renders data before the C passcode is confirmed, and every read fetch sends the stored C code.
- Existing mutating-endpoint behavior and all jd7 tests remain intact.
</success_criteria>

<output>
Update the quick task directory with 260906-kj0-SUMMARY.md when done; record commits and any deviations (especially if startup ordering requires a different approach than the fire-and-forget IIFE replacement).
</output>
