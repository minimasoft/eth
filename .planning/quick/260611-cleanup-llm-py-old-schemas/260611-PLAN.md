# Quick Task 260611: Clean up dead code in llm.py

## Task
Remove all dead code from `src/eth_pipeline/llm.py` — old v1 event extraction schema
(`EVENT_EXTRACTION_SCHEMA`), entity resolution schema + prompt
(`ENTITY_RESOLUTION_SCHEMA`, `ENTITY_RESOLUTION_SYSTEM_PROMPT`), the
`LLMProvider` protocol class, old `extract_events()` and `resolve_references()`
methods and their `_build_payload()` / `_build_resolution_payload()` helpers,
and the top-level convenience functions `extract_events()` and
`resolve_references()`.

## Files Modified
- `src/eth_pipeline/llm.py`
