---
phase: quick-260906-l1o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/static/index.html
  - src/eth_pipeline/static/providers.html
autonomous: true
requirements: [L1O-01, L1O-02]
estimate:
  tokens: 25000
  raw_tokens: 12500
  tasks: 2
  confidence: med
must_haves:
  truths:
    - The passcode modal input is a text input that browser password managers do not offer to save/autofill, on both /ui/ and /ui/providers
    - A Logout button sits left of the settings gear in the index.html header; clicking it clears all stored passcodes and returns the UI to the unauthenticated state (C modal re-shown, gated controls hidden)
    - providers.html offers a consistent logout that clears all stored passcodes and lands on /ui (which re-prompts for C)
  artifacts:
    - src/eth_pipeline/static/index.html (modal input attrs + logout button/handler)
    - src/eth_pipeline/static/providers.html (modal input attrs + logout button/handler)
  key_links:
    - logout handler → clearStoredPasscode(A/B/C) → window.location.reload() → startup C gate re-prompts and applyPasscodeVisibility() hides gated controls
    - providers.html logout → clearStoredPasscode(A/B/C) → window.location.href = '/ui'
---

<objective>
Two passcode UX fixes on top of 260906-jd7/260906-kj0:
1. Stop browser password managers from triggering on the passcode modal input (both index.html and providers.html).
2. Add a Logout button that clears all saved passcodes and returns the UI to the unauthenticated state.

Purpose: the modal input currently uses type="password", so Chrome/Firefox badger the user with save-password popups and autofill on every prompt; and there is no way to switch users/levels without manually clearing localStorage.

Output: modified src/eth_pipeline/static/index.html and src/eth_pipeline/static/providers.html, committed.
</objective>

<execution_context>
@/home/u/src/eth/.opencode/gsd-core/workflows/execute-plan.md
@/home/u/src/eth/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260906-kj0-fix-passcode-enforcement-gap-all-read-en/260906-kj0-SUMMARY.md
@.planning/quick/260906-jd7-implement-basic-passcode-permissions-for/260906-jd7-SUMMARY.md

Key facts from prior summaries (verified in source):
- The passcode modal is NOT static HTML — it is built by the JS function `promptPasscode(level)`, present identically in both files (index.html ~lines 4092-4137, providers.html ~lines 781-826). The input is created with `input.type = 'password'`, `input.className = 'passcode-input'`, `aria-label 'Contraseña nivel ' + level`. There is no wrapping `<form>`.
- localStorage keys: `PASSCODE_KEYS = { A: 'eth_passcode_A', B: 'eth_passcode_B', C: 'eth_passcode_C' }` with helper `clearStoredPasscode(level)` already defined in both files.
- index.html header (lines 1293-1300): `<div class="header-actions">` containing only `#providers-gear-btn` (a `.btn-icon` with an inline SVG). CSS for `.header-actions .btn-icon` exists at lines 38-58. `applyPasscodeVisibility()` (index.html ~line 4153) hides `#tab-btn-upload`, `#tab-upload`, and all `.btn-delete` when neither A nor B is stored. The startup gated IIFE (~line 4170) runs `ensurePasscode('C')` then `loadUploadProviders(); fetchDocuments();`.
- providers.html header (lines 440-443): `<h1>` + `<a href="/ui" class="back-link">← Volver al panel</a>`. No gear, no `.btn-icon` CSS in this file; `.btn-outline` (line 86) and `.back-link` (line 37) are available. Startup IIFE (~line 841) redirects to `/ui` on failed/cancelled A or C gate.

Constraints:
- Keep the existing modal styling (level-lettered label, passcode.png, .passcode-input CSS) — only change input attributes.
- No comments added to code unless matching existing style (both files use sparse `//` comments for section headers and non-obvious rationale; match that density).
- Tests run only via `./test.sh --unit` (per AGENTS.md).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the passcode modal input invisible to password managers (both pages)</name>
  <files>src/eth_pipeline/static/index.html, src/eth_pipeline/static/providers.html</files>
  <action>
In `promptPasscode(level)` in BOTH index.html and providers.html (the duplicated definitions — edit both), change the input element creation so password managers never trigger:

- Change `input.type = 'password'` to `input.type = 'text'`.
- Add `input.setAttribute('autocomplete', 'off')`.
- Add `input.setAttribute('name', 'passcode-value')` — a name that does not match password-manager heuristics (avoid 'password', 'pass', 'contraseña' as a name; the `passcode-input` class alone stays as the styling hook).
- Add `input.setAttribute('spellcheck', 'false')`, `input.setAttribute('autocapitalize', 'off')`, and `input.setAttribute('autocorrect', 'off')`.

Keep everything else exactly as-is: the level-lettered label (`level + ':'`), passcode.png image, `.passcode-input` CSS (18px font), Enter/Escape/backdrop handling, aria-label, and focus call. The `.passcode-input:focus` styling already exists; text inputs inherit it. Do not touch `#api-key` in providers.html (that one IS a real secret and should stay a password field).
  </action>
  <verify>
    <automated>grep -c "input.type = 'password'" src/eth_pipeline/static/index.html src/eth_pipeline/static/providers.html; grep -c "input.type = 'text'" src/eth_pipeline/static/index.html src/eth_pipeline/static/providers.html; grep -c "autocomplete', 'off'" src/eth_pipeline/static/index.html src/eth_pipeline/static/providers.html; node -e "const fs=require('fs');for(const f of ['src/eth_pipeline/static/index.html','src/eth_pipeline/static/providers.html']){const m=fs.readFileSync(f,'utf8').match(/<script>([\s\S]*)<\/script>/);new Function(m[1]);console.log(f+': syntax OK')}"</automated>
  </verify>
  <done>
Both `promptPasscode` functions create a text input with autocomplete/spellcheck/autocapitalize/autocorrect disabled and a non-password-suggestive name; the password-typed `#api-key` field in providers.html is unchanged; both inline scripts parse without syntax errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Logout button to index.html and providers.html headers</name>
  <files>src/eth_pipeline/static/index.html, src/eth_pipeline/static/providers.html</files>
  <action>
**index.html** — in the header `<div class="header-actions">` (lines 1295-1299), insert a logout button BEFORE (left of) `#providers-gear-btn`:

- `<button class="btn-icon" id="logout-btn" aria-label="Cerrar sesión" title="Cerrar sesión">` containing an inline SVG logout/exit icon (stroke-based, 20x20 viewBox 24, matching the gear icon's stroke-width 2 style — e.g. the standard feather `log-out` path: door arrow `M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4` + `M16 17l5-5-5-5` + `M21 12H9`).
- It inherits the existing `.header-actions .btn-icon` CSS — no new CSS needed.

In the passcode section of the script (near `applyPasscodeVisibility`, ~line 4164), add a click handler for `#logout-btn`:
- On click: `clearStoredPasscode('A'); clearStoredPasscode('B'); clearStoredPasscode('C');` then `window.location.reload()`.
- Rationale: reload re-runs the existing unauthenticated path — top-level `applyPasscodeVisibility()` hides the Cargar tab/panel and delete buttons (no A/B stored), and the gated startup IIFE re-prompts the C modal before any fetch. This reuses the already-verified startup flow instead of duplicating visibility-reset and refetch logic inline (which risks double-appending provider dropdown options).

**providers.html** — in the header (lines 440-443), add a logout control before the back-link:
- `<button class="btn-outline" id="logout-btn" type="button">Salir</button>` placed before the `<a class="back-link">`. Reuse the existing `.btn-outline` CSS; if spacing looks off, a one-line margin rule for `header .btn-outline` is acceptable — no other new CSS.
- Handler in the script near the other passcode helpers: on click, `clearStoredPasscode('A'); clearStoredPasscode('B'); clearStoredPasscode('C'); window.location.href = '/ui';` — clearing then redirecting to /ui is the simplest consistent behavior: /ui reloads with empty storage, shows its C modal, and applyPasscodeVisibility hides gated controls (matches the page's existing redirect-on-cancel pattern).

Guard the handler registration on the element existing (`if (logoutBtn)`) so the duplicated helper blocks stay copy-safe.
  </action>
  <verify>
    <automated>grep -c 'id="logout-btn"' src/eth_pipeline/static/index.html src/eth_pipeline/static/providers.html; grep -c "clearStoredPasscode('A')" src/eth_pipeline/static/index.html src/eth_pipeline/static/providers.html; node -e "const fs=require('fs');for(const f of ['src/eth_pipeline/static/index.html','src/eth_pipeline/static/providers.html']){const m=fs.readFileSync(f,'utf8').match(/<script>([\s\S]*)<\/script>/);new Function(m[1]);console.log(f+': syntax OK')}"; ./test.sh --unit</automated>
  </verify>
  <done>
index.html header shows a logout icon-button left of the gear whose handler clears all three localStorage passcode keys and reloads; providers.html shows a Salir button that clears all three keys and redirects to /ui; `./test.sh --unit` passes at the 94-passed baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→UI localStorage | Passcodes stored client-side; logout must fully clear them on shared machines |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-L1O-01 | Information Disclosure | localStorage passcode keys | medium | mitigate | Logout clears eth_passcode_A/B/C in both pages; verified by grep for all three clearStoredPasscode calls in each file |
| T-L1O-02 | Information Disclosure | passcode modal input | low | mitigate | text type + autocomplete=off avoids browser save-prompt storing the code in the OS password vault; passcodes remain short shared codes, not high-value secrets |
</threat_model>

<verification>
1. `grep -c "input.type = 'password'"` returns 0 in index.html and 1 in providers.html (only the real `#api-key` secret field remains).
2. Both pages: `promptPasscode` input has type text, autocomplete off, non-suggestive name, spellcheck/autocapitalize/autocorrect off.
3. `grep 'id="logout-btn"'` finds exactly one match per file.
4. index.html logout handler clears A, B, and C then reloads; providers.html handler clears A, B, and C then redirects to /ui.
5. Both inline scripts parse cleanly via `new Function(...)`.
6. `./test.sh --unit` green (94 passed, 4 skipped baseline — no server-side changes expected).
7. Live spot check (if dev stack is running, after `docker compose build api && docker compose up -d api`): open /ui/, confirm no password-manager prompt on the C modal, click logout, confirm C modal re-appears and Cargar/deletes are hidden; on /ui/providers click Salir, confirm redirect to /ui with C prompt.
</verification>

<success_criteria>
Password managers no longer trigger on either page's passcode modal; a working logout exists on both pages that fully clears saved passcodes and restores the unauthenticated state; existing modal styling and all passcode enforcement behavior (A/B/C levels, fetchWithC, startup gates) are unchanged; unit tests green.
</success_criteria>

<output>
Create .planning/quick/260906-l1o-passcode-ux-fixes-1-passcode-modal-input/260906-l1o-SUMMARY.md when done
</output>
