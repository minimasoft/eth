# Quick Task 260604-n9q: Fix document delete 405 Method Not Allowed error

## Description
The UI sends `DELETE /documents/{document_id}` for document deletion, but the route handler was accidentally deleted in commit `7913676` (feat(19): token recording). Only `DELETE /documents/{document_id}/events` remained, which caused FastAPI to return HTTP 405 Method Not Allowed.

Additionally, `clear_document_events` was repurposed in the same commit to perform full deletion but had a bug (undefined variable `affected_ce_rids`) and used the wrong route path.

## Tasks

### Task 1: Restore delete_document endpoint
- **Action:** Add back `DELETE /documents/{document_id}` route with full cascade delete logic
- **Files:** `src/eth_pipeline/api/routes/documents.py`
- **Details:** Restore `delete_document` function from commit `f88e54d`, which properly cascades deletes across event_entity_link, document_chunk, reference, event, document_event_log, document, and orphaned canonical_entities

### Task 2: Fix clear_document_events endpoint
- **Action:** Revert `DELETE /documents/{document_id}/events` to original behavior (clear events + reset document status, not delete the document)
- **Files:** `src/eth_pipeline/api/routes/documents.py`
- **Details:** Remove the full deletion logic, restore original clear-events behavior that deletes chunks, references, events, event_log, llm_usage, and resets document to pending status
