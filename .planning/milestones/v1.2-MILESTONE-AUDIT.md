# Milestone Audit: v1.2 M002 Integration Test Fixes

**Audited:** 2026-05-31
**Status:** passed

## Summary

Milestone v1.2 "M002 Integration Test Fixes" is complete. All 4 failing M002 tests (Tests 2-5) now pass, and all M001 tests (Tests 1-8) remain green with no regression.

## Root Causes Identified and Fixed

| Test | Symptom | Root Cause | Fix |
|------|---------|------------|-----|
| 2 | Canonical entity query returned 0 rows | Test SQL set `properties: { test: true, ... }` — SCHEMAFULL rejected undefined sub-fields | Changed to `properties: {}` |
| 3 | Reference-to-canonical links not found | Reference SQL used `text` (wrong field), missing `reference_type`, `span_start`, `span_end`, wrong types for `event`/`document` | Fixed field names, added required fields, used proper record IDs |
| 4 | Merge endpoint returned 502 | API writes `updated_at` on reference table — field didn't exist in schema | Added `created_at`, `updated_at` to reference table |
| 5 | Split endpoint returned 502 | API writes `properties.split_from` — sub-field not defined, `properties` not FLEXIBLE | Made `properties` FLEXIBLE on canonical_entity |

## Schema Changes

- `reference` table: added `created_at`, `updated_at` fields
- `canonical_entity` table: changed `properties` from strict `object` to `object | null FLEXIBLE`

## Test Changes

- `pipeline_m002.test.ts`: All reference CREATE statements updated to match schema (field names, required fields, record ID types)
- Added `before` hook to create shared `event:test_event` and `document:test_doc` records
- GraphQL query field changed from `text` to `verbatim_text`

## Verification

- All 17/17 integration tests pass (M001: 11/11, M002: 6/6)
- `docker compose run --rm integration-tests` exits with code 0
- No regression in M001 tests

## Gaps

No gaps identified.
