# Quick Task 260605-l4s: Fix canonical_entity_id column bug, add entity delete endpoint, UI button, cleanup orphans

## Tasks

1. **Fix column name bug** — Replace all `canonical_entity_id` with `canonical_entity` in `entities.py` SQL queries. Fix wrong join columns and `event_participant` column names.
2. **Add entity delete endpoint** — Add `EntityDeleted` model and `DELETE /entities/{entity_id}` route that deletes a canonical entity.
3. **Add delete button in UI** — Add "Acciones" column with delete button in entity list. Event delegation for click handling.
4. **Cleanup orphans** — Find and delete canonical_entities with zero references/edges.
