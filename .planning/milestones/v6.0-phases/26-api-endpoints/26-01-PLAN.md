---
phase: 26-api-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/api/routes/entities.py
autonomous: true
requirements:
  - API-03
user_setup: []
must_haves:
  truths:
    - "Merging a place entity rewires location_place_id — events pointing to the source now point to the target"
    - "Merging a person entity rewires event_participant edges — participant links pointing to the source now point to the target"
    - "Splitting an entity does NOT transfer location_place_id or event_participant links to new entities (they are separate entities — appropriate partition)"
    - "Failed location_place_id or event_participant rewiring during merge raises a visible error, not silent success"
  artifacts:
    - path: "src/eth_pipeline/api/routes/entities.py"
      provides: "Merge/split endpoint logic"
      min_lines: 737
      exports:
        - merge_entities
        - split_entity
  key_links:
    - from: "merge_entities"
      to: "event.location_place_id"
      via: "UPDATE event SET location_place_id = $1 WHERE location_place_id = $2"
      pattern: "location_place_id.*rewire"
    - from: "merge_entities"
      to: "event_participant.out_entity"
      via: "UPDATE event_participant SET out_entity = $1 WHERE out_entity = $2"
      pattern: "event_participant.*rewire"
---

<objective>
Harden merge/split endpoint handling for v6.0 structured fields (location_place_id, event_participant). Replace silent error swallowing with proper propagation. Add explicit split behavior documentation for the "appropriate partition" of links — new split entities receive zero location/participant links from the original entity (they are separate entities).
</objective>

<execution_context>
@/home/u/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/u/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@src/eth_pipeline/api/routes/entities.py
@.planning/phases/25-llm-extraction-pipeline/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden merge endpoint — remove silent try/except on location_place_id and event_participant rewiring</name>
  <files>src/eth_pipeline/api/routes/entities.py</files>
  <action>
    In `merge_entities()`, replace the two `try/except logger.warning(...)` blocks (lines ~287-306) that handle:
    1. `UPDATE event SET location_place_id = $1 WHERE location_place_id = $2`
    2. `UPDATE event_participant SET out_entity = $1 WHERE out_entity = $2`

    These wrappers were added defensively during Phase 24/25 deployment transition when the `event.location_place_id` column and `event_participant` table might not have existed yet. Phase 25 has shipped — these tables are guaranteed.

    Replace with direct `await db.execute(...)` calls inside the existing `async with get_db() as db:` block (same `try` that handles the `UPDATE reference` query). If either query fails, the outer `try/except` already catches the exception and raises HTTP 502 with "Failed to execute merge operation." — correct behavior.

    The per-table error messages in the removal should update the outer HTTPException detail to include specific context. Change the outer exception message from `"Failed to execute merge operation."` to `"Merge failed during reference/location/participant rewiring: {exc}"` to aid debugging.

    After the changes, the two rewire steps should be logged at `logger.info` level (not `logger.warning`) and report the number of affected rows for each.

    Do NOT change function signature, request/response models, or the overall try/except structure.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('src/eth_pipeline/api/routes/entities.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>
    - No bare `try/except` around `location_place_id` rewire (the two `try: ... except Exception as exc: logger.warning(...)` blocks are removed)
    - Outer exception handler reports specific failure context
    - Every asyncpg operation inside the merge transaction is covered by the same outer try block
    - `logger.info` calls report row counts for location_place_id and event_participant rewiring
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explicit split behavior for location_place_id and event_participant — with logging</name>
  <files>src/eth_pipeline/api/routes/entities.py</files>
  <action>
    In `split_entity()`, after the loop that creates new entities and moves references (around the `logger.info` at line 554), add:

    1. A log statement documenting that location_place_id and event_participant links are NOT transferred to new split entities — per the "appropriate partition" principle (new entities are separate from the original and should not inherit its event connections).
    2. Query the count of events where `location_place_id` points to the original entity, and the count of `event_participant` edges where `out_entity` points to the original entity. Log both counts as "retained by original" — confirming the split did not affect them.
    3. Use `try/except` with proper HTTPException(502) failure propagation (not silent swallowing) since the query is diagnostic only — if the count query fails, log a warning but do not abort the split.

    The log message should say something like:
    "Split entity {entity_id} ({entity_type}): {N} events with location_place_id retained by original, {M} event_participant edges retained by original (no links transferred to new entities — appropriate partition)"
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('src/eth_pipeline/api/routes/entities.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>
    - split_entity() logs location_place_id and event_participant retention counts for the original entity
    - The log message explicitly documents the "appropriate partition" behavior
    - Any query failure is handled gracefully (warning-level, not abort)
    - Return format (SplitResponse) unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Entity merge/split requests cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | merge_entities | mitigate | Input validation via MergeRequest model (source_id/target_id as strings); both entities verified to exist BEFORE mutation; same-type check prevents cross-type corruption; already-merged guard prevents double-merge |
| T-26-02 | Info Disclosure | split_entity | mitigate | Accepts only entity IDs and reference IDs owned by that entity; validates each reference belongs to the source entity before mutation |
| T-26-03 | Repudiation | merge/split rewiring | mitigate | All operations logged with entity IDs, row counts, and outcome at info level |
</threat_model>

<verification>
1. Python syntax check passes for entities.py
2. Code review confirms no silent error swallowing remains for the two rewire operations
3. Merge endpoint still returns MergeResponse with correct field shapes
4. Split endpoint still returns SplitResponse with correct field shapes
</verification>

<success_criteria>
- Merge endpoint propagates all DB errors during location_place_id and event_participant rewiring (no silent success)
- Split endpoint logs location_place_id and event_participant retention counts, documenting the "appropriate partition" design decision
- All response model shapes unchanged — backward compatible
</success_criteria>

<output>
Create `.planning/phases/26-api-endpoints/26-01-SUMMARY.md` when done
</output>
