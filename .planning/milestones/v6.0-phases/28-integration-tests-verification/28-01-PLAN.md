---
phase: 28-integration-tests-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/helpers.ts
  - tests/integration/golden_fixture.ts
  - tests/integration/pipeline_v6.test.ts
  - tests/integration/package.json
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]
user_setup: []

must_haves:
  truths:
    - "Golden fixture document produces events with non-null time_window.start and time_window.end after full pipeline run"
    - "Events have location_place_name populated when the fixture contains a named location"
    - "Events have participant_count > 0 indicating event_participant edges exist"
    - "DELETE /documents/{id} cascade removes event_participant edges — zero orphan participant edges"
    - "Reprocessing the same document via a new submission produces no duplicate event_participant edges"
    - "TypeScript types in helpers.ts expose time_window, location_place_name, participant_count, element_field, and reference_index"
    - "All tests pass with exit code 0 when running `docker compose run --rm integration-tests`"

  artifacts:
    - path: "tests/integration/helpers.ts"
      provides: "Updated TypeScript interface types for v6.0 API fields"
      min_lines: 480
    - path: "tests/integration/golden_fixture.ts"
      provides: "Statically importable GOLDEN_FIXTURE constant with known expected output"
      min_lines: 50
    - path: "tests/integration/pipeline_v6.test.ts"
      provides: "4 test groups covering TEST-01 through TEST-04"
      min_lines: 300

  key_links:
    - from: "tests/integration/helpers.ts"
      to: "src/eth_pipeline/api/models.py"
      via: "TypeScript interface mirrors Python EventListItem/ReferenceListItem fields"
      pattern: "time_window|location_place_name|participant_count"
    - from: "tests/integration/pipeline_v6.test.ts"
      to: "tests/integration/golden_fixture.ts"
      via: "imports GOLDEN_FIXTURE from golden_fixture.ts"
      pattern: "import.*GOLDEN_FIXTURE"
    - from: "tests/integration/pipeline_v6.test.ts"
      to: "tests/integration/helpers.ts"
      via: "imports createDocument, waitForProcessing, listEvents, listReferences, httpDelete, clearEvents"
      pattern: "from.*helpers\\.js"
    - from: "tests/integration/package.json"
      to: "tests/integration/pipeline_v6.test.ts"
      via: "test script compiles and runs pipeline_v6.test.js alongside e2e_pipeline.test.js"
      pattern: "pipeline_v6"

---

<objective>
Write a v6.0 integration test suite that validates:
- Golden test fixture processing produces structured event fields (time_window, location_place_name, participant_count)
- Cascade delete removes event_participant edges when a document is deleted
- Temporal replay safety produces no duplicate edges on reprocess
- TypeScript types in helpers.ts match the v6.0 Python API response models
- All tests pass with zero regressions

**Purpose:** Deliver the final verification layer for v6.0. Every new data structure, pipeline behavior, and API contract introduced in Phases 24-27 has an automated integration test proving it works end-to-end.

**Output:**
- Updated `tests/integration/helpers.ts` — EventListItem gains time_window, location_point, location_place_name, participant_count, document_filename; ReferenceListItem gains element_field, reference_index, canonical_entity_name, canonical_entity_id, canonical_entity_type
- NEW `tests/integration/golden_fixture.ts` — exported GOLDEN_FIXTURE constant
- NEW `tests/integration/pipeline_v6.test.ts` — 4 test groups
- Updated `tests/integration/package.json` — test script includes pipeline_v6.test.js
- Verified: `docker compose run --rm integration-tests` exits 0
</objective>

<execution_context>
@/home/u/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/u/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-integration-tests-verification/28-RESEARCH.md
@.planning/phases/28-integration-tests-verification/28-VALIDATION.md

# Source of truth — Python API models that TS types must match
@src/eth_pipeline/api/models.py

# Existing test patterns to replicate
@tests/integration/helpers.ts
@tests/integration/e2e_pipeline.test.ts
@tests/integration/package.json
@tests/integration/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TypeScript types in helpers.ts + create golden_fixture.ts</name>

  <read_first>
  - @tests/integration/helpers.ts (existing EventListItem, ReferenceListItem interfaces at lines 136-165)
  - @src/eth_pipeline/api/models.py (EventListItem at line 338, ReferenceListItem at line 267 — the source-of-truth Python models)
  </read_first>

  <action>
  Make TWO modifications and ONE creation:

  === A. Update EventListItem interface in helpers.ts ===

  The Python `EventListItem` (models.py line 338) contains these v6.0 fields that are MISSING from the TypeScript interface:

  ```
  time_window: dict | None = None        # Python → TypeScript: time_window: Record<string, string> | null
  location_point: dict | None = None     # → location_point: Record<string, unknown> | null
  location_place_name: str | None = None # → location_place_name: string | null
  participant_count: int = 0             # → participant_count: number (already present in EventListItem? verify)
  document_filename: str | None = None   # → document_filename: string | null
  ```

  Add these fields to the existing TypeScript `EventListItem` interface (line 136). Keep all existing fields (event_id, que_paso, espacio, tiempo, humanos, objetos, document_id, reference_count, participant_count). Do NOT remove any existing fields.

  === B. Update ReferenceListItem interface in helpers.ts ===

  The Python `ReferenceListItem` (models.py line 267) contains these v6.0 fields MISSING from the TypeScript interface:

  ```
  element_field: str | None = None            # → element_field: string | null
  reference_index: int | None = None          # → reference_index: number | null
  canonical_entity_name: str | None = None    # → canonical_entity_name: string | null
  canonical_entity_id: str | None = None      # → canonical_entity_id: string | null
  canonical_entity_type: str | None = None    # → canonical_entity_type: string | null
  ```

  Add these fields to the existing TypeScript `ReferenceListItem` interface (line 156). Keep all existing fields (reference_id, reference_type, verbatim_text, event_id, canonical_entity, event_que_paso, document_filename, document_id). Do NOT remove any existing fields.

  === C. Create tests/integration/golden_fixture.ts ===

  Create a new file exporting a single constant `GOLDEN_FIXTURE`. This is a crafted Spanish legal text (5-10 paragraphs) designed to reliably produce:
  - **2 events** with clear date boundaries: one in "enero de 2024" (filing a demanda), one in "febrero de 2024" (vista oral)
  - **3 person entities**: Laura Fernández, Miguel Ángel Torres, Antonio Ruiz
  - **1 place entity**: Barcelona (or a specific address within)
  - **element_field-tagged references** for tiempo, humanos, espacio, objetos

  The fixture text must use explicit, unambiguous patterns:
  - Full dates: "15 de enero de 2024" (not relative)
  - Named locations: "Juzgado de Primera Instancia Número 3 de Barcelona", "Avenida Diagonal, 200"
  - Named persons with full names: "Laura Fernández", "Miguel Ángel Torres"
  - Clear event boundaries (not embedded in a single sentence)

  Use the template from RESEARCH.md (line 257) as the base, which already has the correct structure for a reclamación de cantidad with two clear events. The text should be 250-500 words and produce at least 2 events with structured fields.

  Export: `export const GOLDEN_FIXTURE = [...]\n".join("\\n");`
  </action>

  <verify>
  <automated>npx tsc --noEmit --project tests/integration/tsconfig.json 2>&1 | head -20</automated>
  TypeScript compilation must succeed with zero errors. If errors occur, fix the type mismatches and re-run.
  </verify>

  <acceptance_criteria>
  1. TypeScript `npx tsc --noEmit` exits 0 with no errors
  2. `EventListItem` interface has all Python-model fields: event_id, que_paso, espacio, tiempo, humanos, objetos, document_id, reference_count, participant_count, time_window, location_point, location_place_name, document_filename
  3. `participant_count` type is `number` not `number | null`
  4. `ReferenceListItem` interface has all Python-model fields: reference_id, reference_type, verbatim_text, span_start, span_end, page_number, element_field, reference_index, resolution_confidence, event_que_paso, event_id, document_filename, document_id, canonical_entity_name, canonical_entity_id, canonical_entity_type
  5. `reference_count` remains `number` on EventListItem (not changed to optional)
  6. `tests/integration/golden_fixture.ts` exports `GOLDEN_FIXTURE` as a single string
  7. `GOLDEN_FIXTURE` is 250-500 words of Spanish legal text mentioning Laura Fernández, Miguel Ángel Torres, Barcelona, and dates "15 de enero de 2024" and "28 de febrero de 2024"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create pipeline_v6.test.ts with 4 v6.0 test groups</name>

  <read_first>
  - @tests/integration/e2e_pipeline.test.ts (existing test patterns: describe/it, skipIfDegraded, createDocument, waitForProcessing, listEvents, httpDelete, clearEvents, testDocIds array, after cleanup)
  - @tests/integration/golden_fixture.ts (just created — the GOLDEN_FIXTURE constant)
  - @tests/integration/helpers.ts (updated EventListItem with time_window, location_place_name, participant_count)
  - @.planning/phases/28-integration-tests-verification/28-RESEARCH.md (Pattern sections at lines 252-413 for reference code)
  </read_first>

  <action>
  Create `tests/integration/pipeline_v6.test.ts`. Follow the exact pattern from `e2e_pipeline.test.ts`: same imports from `./helpers.js`, same `describe`/`it`/`after` structure, same `skipIfDegraded` wrapping, same `testDocIds` array + `documentWasProcessed` flag + `cleanupTestDocuments` helper.

  The file MUST contain 4 test groups. Group them under a single describe block: `describe("v6.0 — structured event fields, cascade, replay safety", () => {`.

  === Test Group 1: "1. Golden fixture — process and verify basic output" (TEST-01) ===

  Pattern: mirror existing test 1 from e2e_pipeline.test.ts. Submit GOLDEN_FIXTURE with filename "golden_fixture.txt", poll for processing, assert:
  - `events.total >= 2` (at least 2 events for the 2-event fixture)
  - `refs.total > 0` (references exist)
  - `logs.total > 0` (processing logs exist)
  - Log event count and reference count

  === Test Group 2: "2. Structured event fields — time_window, location_place_name, participant_count" (TEST-02) ===

  Pattern: mirror existing test 2 from e2e_pipeline.test.ts (check entities first), then add v6.0 field assertions:
  - Call `listEvents(docId)` to get typed EventListResponse
  - For each event in `events.items`:
    - If `evt.time_window` exists: assert `typeof evt.time_window.start === "string"` and `typeof evt.time_window.end === "string"`, and `evt.time_window.start` matches `/^\d{4}-\d{2}-\d{2}/` (ISO 8601 date start)
    - Assert `evt.participant_count >= 0` (type: number, always present)
  - Count events where `evt.time_window?.start` is truthy → assert `≥1`
  - If `evt.location_place_name` is non-null on any event, log it. If no events have location_place_name, log "No events had location_place_name populated (LLM may not have extracted it)" — do NOT assert failure for this (LLM non-determinism per Pitfall 1)
  - Log total events and events with time_window

  === Test Group 3: "3. Temporal replay safety — no duplicate edges on reprocess" (TEST-04) ===

  RUNS BEFORE cascade delete to avoid shared-state contamination (testDocIds mutation).
  Pattern: mirror existing test 4 from e2e_pipeline.test.ts:
  - Count events + sum participant_count before clear
  - Call `clearEvents(docId)` (DELETE /documents/${docId}/events)
  - Assert `clearResult.events_cleared` is true
  - Verify events are gone (`listEvents(docId)` returns `total === 0`)
  - Create a NEW document with the same GOLDEN_FIXTURE text and filename "golden_reprocess.txt"
  - Wait for processing
  - Count events + sum participant_count after reprocess
  - Log before/after counts. Assert that participant_count is not wildly inflated (after >= before * 0.5 as a sanity check — LLM nondeterminism may produce slightly different results)
  - Do NOT assert exact equality — structural check only (per Pitfall 3)

  === Test Group 4: "4. Cascade delete — zero orphan event_participant edges" (TEST-03) ===

  RUNS AFTER replay safety to ensure testDocIds[0] still exists.
  Pattern: mirror existing test 5 from e2e_pipeline.test.ts but add participant edge verification:
  - Sum `participant_count` across all events before delete (use `.reduce((sum, e) => sum + (e.participant_count || 0), 0)`)
  - Log total participant edges before delete
  - Execute `DELETE /documents/${docId}`, assert HTTP 200
  - Verify document is gone (`GET /documents/${docId}` returns non-200)
  - Remove docId from testDocIds so cleanup hook doesn't try again
  - Log confirmation: "✓ Cascade delete: document + event_participant edges cleaned"
  - Do NOT include the entity orphan leak verification from existing test 5 (that is already tested in e2e_pipeline.test.ts and including it twice would create duplicate assertions on the same document)

  === Cleanup ===
  Include `after` hook identical to e2e_pipeline.test.ts (delete all documents in `testDocIds`).
  </action>

  <verify>
  <automated>npx tsc --project tests/integration/tsconfig.json 2>&1 | head -20</automated>
  TypeScript compilation of the integration test suite must succeed with zero errors.
  </verify>

  <acceptance_criteria>
  1. `npx tsc --project tests/integration/tsconfig.json` exits 0 with no errors
  2. `tests/integration/pipeline_v6.test.ts` exists and is not empty
  3. File contains exactly 4 `it(` calls (one per test group)
  4. File imports GOLDEN_FIXTURE from `./golden_fixture.js` (compiled JS extension)
  5. File imports helpers from `./helpers.js` using same import paths as e2e_pipeline.test.ts
  6. File has `after` hook calling `cleanupTestDocuments()`
  7. Test Group 1 submits with filename "golden_fixture.txt" and asserts ≥2 events
  8. Test Group 2 asserts `time_window.start` is ISO date string on at least 1 event
  9. Test Group 3 sums participant_count before and after reprocess with non-inflation check (replay safety)
  10. Test Group 4 sums participant_count before delete and removes docId from testDocIds

Note: Test Group 3 (replay safety) runs BEFORE Test Group 4 (cascade delete) to prevent shared-state contamination of testDocIds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update package.json test script and run full regression suite</name>

  <read_first>
  - @tests/integration/package.json (existing test script at line 8)
  </read_first>

  <action>
  === A. Update test script in package.json ===

  Change the `test` script from:
  `"npx tsc && node --test dist/e2e_pipeline.test.js"`
  To:
  `"npx tsc && node --test dist/e2e_pipeline.test.js dist/pipeline_v6.test.js"`

  This ensures the v6.0 test suite runs alongside the existing e2e test suite when executing `npm test` or when Docker runs `docker compose run --rm integration-tests`.

  === B. Run the full test suite ===

  1. Verify Docker services are running: `docker compose ps`
  2. If services are not running, start them: `docker compose up -d api worker`
  3. Wait for API healthcheck: poll `curl -s http://localhost:8001/health` until 200
  4. Run full test suite: `docker compose run --rm integration-tests`
  5. Examine the output:
     - All 5 e2e tests (submit, entities, tokens, reprocess, cascade) must pass
     - All 4 v6.0 tests (golden fixture, structured fields, cascade, replay) must pass
     - Test output must show ≥9 total tests passing (5 existing + 4 new)
     - Exit code must be 0
  6. If any test fails:
     - Read the failure output and error trace
     - Fix the issue (likely type mismatch in the test file or incorrect assertion)
     - Re-run: `docker compose run --rm integration-tests`
     - Repeat until exit code 0

  === C. Confirm TEST-05 ===
  After all tests pass, verify that 5 existing e2e tests + 4 new v6.0 tests = at least 9 tests passed. Log the exact test counts.
  </action>

  <verify>
  <automated>docker compose run --rm integration-tests 2>&1</automated>
  Must exit with code 0 and show ≥9 passing tests (5 existing e2e + 4 new v6.0). If run against a degraded environment (Temporal/LLM unavailable), tests are expected to skip gracefully per the established pattern — verify the count of tests that ACTUALLY RAN vs. skipped. The minimum passing test count in a fully operational environment is 9.
  </verify>

  <acceptance_criteria>
  1. `tests/integration/package.json` test script includes `dist/pipeline_v6.test.js` alongside `dist/e2e_pipeline.test.js`
  2. `docker compose run --rm integration-tests` exits with code 0
  3. Test output shows ≥9 total tests (5 existing e2e + 4 new v6.0)
  4. All 4 new v6.0 test groups pass in the output (no FAIL or ✗ for test groups 1-4)
  5. All 5 existing e2e tests pass (no FAIL or ✗ for existing test groups 1-5)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test client → API | Integration tests send HTTP requests to the REST API. Tests operate within the same Docker network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | EventListItem/ReferenceListItem type mismatch | mitigate | TypeScript compilation gate catches field name/type divergence between TS interfaces and Python models |
| T-28-02 | Tampering | Golden fixture LLM nondeterminism causes assertion fragility | accept | Structural assertions (non-null fields, ISO date format) instead of exact value matching. LLM variability expected per Pitfall 1. |
| T-28-03 | Information Disclosure | Test output leaks PII from golden fixture | accept | Golden fixture uses fictional names/places. No real personal data. |
| T-28-SC | Tampering | npm install in Docker (node:22-slim) | mitigate | No new packages added — only existing TypeScript and @types/node. Slopcheck reference: RESEARCH.md confirms 0 new packages. |
</threat_model>

<verification>

### Per-Task Verification

| Task | Requirement | Automated Command |
|------|-------------|-------------------|
| 1. Types + fixture | TEST-01 | `npx tsc --noEmit --project tests/integration/tsconfig.json` |
| 2. Test suite | TEST-01, TEST-02, TEST-03, TEST-04 | `npx tsc --project tests/integration/tsconfig.json` |
| 3. Run + regressions | TEST-05 | `docker compose run --rm integration-tests` |

### must_haves checks

- [ ] `helpers.ts` EventListItem has time_window, location_place_name, participant_count, document_filename
- [ ] `helpers.ts` ReferenceListItem has element_field, reference_index, canonical_entity_name, canonical_entity_id, canonical_entity_type
- [ ] `golden_fixture.ts` exports GOLDEN_FIXTURE with 2-event Spanish legal text
- [ ] `pipeline_v6.test.ts` exists with 4 test groups
- [ ] `package.json` test script includes pipeline_v6.test.js
- [ ] `docker compose run --rm integration-tests` exits 0
- [ ] All 5 e2e tests pass (existing regression)
- [ ] All 4 v6.0 tests pass (new verification)
</verification>

<success_criteria>

1. **Type fidelity:** TypeScript EventListItem and ReferenceListItem interfaces in helpers.ts include all v6.0 fields matching the Python models. Compilation succeeds.

2. **Golden fixture pipeline:** Submitting GOLDEN_FIXTURE through the full pipeline (POST → Temporal → processed) produces ≥2 events accessible via GET /events with non-null time_window on at least 1 event.

3. **Structured field verification:** Events returned by GET /events include participant_count (number, always present), time_window (Record<string,string> | null, with ISO 8601 start when present), and location_place_name (string | null).

4. **Cascade delete cleanup:** DELETE /documents/{id} removes all associated records; verifying GET /events returns 0 total for the deleted document confirms event_participant edges were cleaned.

5. **Replay safety:** After DELETE events + re-submit of same text, the reprocessed document has participant_count that is not inflated (non-inflation check passes).

6. **Zero regressions:** All 5 existing e2e tests continue to pass alongside the 4 new v6.0 tests — `docker compose run --rm integration-tests` exits with code 0.

## Artifacts this phase produces

- **Updated helpers.ts types:**
  - `EventListItem.time_window: Record<string, string> | null` — structured time range
  - `EventListItem.location_point: Record<string, unknown> | null` — geolocation object
  - `EventListItem.location_place_name: string | null` — resolved place entity name
  - `EventListItem.participant_count: number` — count of event_participant edges
  - `EventListItem.document_filename: string | null` — source document filename
  - `ReferenceListItem.element_field: string | null` — event element this reference substantiates
  - `ReferenceListItem.reference_index: number | null` — ordering within element_field
  - `ReferenceListItem.canonical_entity_name: string | null` — resolved entity name
  - `ReferenceListItem.canonical_entity_id: string | null` — resolved entity ID
  - `ReferenceListItem.canonical_entity_type: string | null` — resolved entity type

- **New file:** `tests/integration/golden_fixture.ts` — GOLDEN_FIXTURE constant (250-500 words Spanish legal text with 2 events, 3 persons, 1 place)

- **New file:** `tests/integration/pipeline_v6.test.ts` — 4 test groups under `describe("v6.0 — structured event fields, cascade, replay safety")`:
  - Test 1: Golden fixture process + basic output (≥2 events)
  - Test 2: Structured field verification (time_window ISO dates, participant_count)
  - Test 3: Cascade delete (participant_count summed before delete)
  - Test 4: Replay safety (participant_count before/after, non-inflation check)

- **Updated file:** `tests/integration/package.json` — test script includes `dist/pipeline_v6.test.js`
</success_criteria>

<output>
Create `.planning/phases/28-integration-tests-verification/28-01-SUMMARY.md` when done
</output>
