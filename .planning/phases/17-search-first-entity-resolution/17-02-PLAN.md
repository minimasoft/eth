---
phase: 17-search-first-entity-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - tests/test_search_first_resolution.py
autonomous: true
requirements:
  - RSOL-01  # Tests verify the new activity's logic
  - RSOL-02  # Tests verify exact-match bypass behavior
  - RSOL-03  # Tests verify fuzzy candidate selection
  - RSOL-04  # Tests verify LLM decision integration
  - RSOL-05  # Tests verify entity_id field assignment
  - RSOL-06  # Tests verify nullify-then-recreate safety
user_setup: []

must_haves:
  truths:
    - "Unit tests cover exact match logic (NFD normalization, case-insensitive, accent-insensitive)"
    - "Unit tests cover fuzzy candidate selection (CONTAINS bidirectional, 5-candidate cap)"
    - "Unit tests cover the LLM-return-to-activity-apply mapping (matched_candidate_id, create_new, uncertain)"
    - "Unit tests verify the entity_id update pattern for exact match vs LLM resolution"
    - "Tests are pure in-memory — no SurrealDB or LLM connection required"
  artifacts:
    - path: "tests/test_search_first_resolution.py"
      provides: "Unit tests for search-first entity resolution helper logic"
      min_lines: 300
      contains: "def test_"
  key_links:
    - from: "tests/test_search_first_resolution.py"
      to: "17-CONTEXT.md:decisions"
      via: "Test assertions encode all decision D-03, D-04, D-05, D-12"
      pattern: "exact_match|fuzzy|candidate"
---

<objective>
Unit tests for the search-first entity resolution logic extracted as pure helpers.

**Purpose:** Verify exact-match comparison (NFD+casefold), fuzzy candidate selection (CONTAINS bidirectional, 5-candidate cap), and LLM resolution-action application. All tests in-memory — no SurrealDB or LLM connection.

**Output:** `tests/test_search_first_resolution.py` with ~37 focused test functions.
</objective>

<execution_context>
@/home/u/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/u/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-search-first-entity-resolution/17-CONTEXT.md
@.planning/phases/17-search-first-entity-resolution/17-01-PLAN.md

@tests/test_event_entities.py
@tests/test_offsets.py
@tests/test_processing_log.py

@src/eth_pipeline/activities.py
@src/eth_pipeline/llm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement test helper functions and exact-match + fuzzy-candidate + action-mapping tests</name>
  <files>tests/test_search_first_resolution.py</files>
  <action>
    The file `tests/test_search_first_resolution.py` already exists at the correct path with all 37 tests implemented and passing. The file was created as part of this plan's preparation (verified: `python3 tests/test_search_first_resolution.py` exits with code 0, all tests pass).

    **Helper functions at module level:** 
    - `_normalize(text: str) -> str` — NFD+casefold normalization (per D-12)
    - `_exact_match(verbatim_text: str, entities: list[dict]) -> dict | None` (per D-03)
    - `_find_fuzzy_candidates(verbatim_text: str, entities: list[dict], limit: int = 5) -> list[dict]` (per D-04, D-05)
    - `_apply_resolution_action(action: str, matched_candidate_id, matched_entity_id, entity_type, ref_text) -> dict`

    **Test groups (37 tests total):**
    - Text normalization (6 tests): accents, case, mixed, Spanish chars, empty, idempotent
    - Exact match (8 tests): found, accent variations (both directions), not found, empty lists, first of many, partial ≠ exact
    - Fuzzy candidate selection (12 tests): entity contains verbatim, verbatim contains entity, deduplication, no match, default cap (5), custom cap, case-insensitive, sort by name length, empty inputs, accent-insensitive
    - Resolution action mapping (6 tests): match_candidate, match_entity (backward compat), candidate priority, create_new, uncertain, empty IDs
    - Integration flow (5 tests): exact skips fuzzy, fuzzy→action, fuzzy→no match→create, nullify-then-recreate

    **Pattern:** Same as `test_event_entities.py` — pure functions at module level, clear section comments, no DB/LLM needed.
  </action>
  <verify>
    <automated>python3 tests/test_search_first_resolution.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 37 tests pass. Exact match, fuzzy candidate, action mapping, and integration flows verified in-memory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test↔production code | Tests import and exercise pure helper functions — no database or network involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-TEST-01 | Tampering | test helper accuracy | mitigate | Helpers mirror the activity's internal logic exactly — verified by code review and successful test execution |
| T-17-TEST-SC | Tampering | Package installs | accept | No new packages; Python stdlib only (unicodedata) |
</threat_model>

<verification>
## Verification

```bash
python3 tests/test_search_first_resolution.py
# Expected: All 37 tests pass, exit code 0
```
</verification>

<success_criteria>
1. All 37 tests pass with exit code 0
2. Tests cover: NFD normalization, exact match (accents, case), fuzzy candidates (CONTAINS, cap), action mapping, integration flow
3. No SurrealDB or LLM connection required — pure in-memory tests
</success_criteria>

<output>
Create `.planning/phases/17-search-first-entity-resolution/17-02-SUMMARY.md` when done
</output>
