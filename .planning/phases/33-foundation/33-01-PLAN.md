---
phase: 33-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/eth_pipeline/alembic/env.py
  - src/eth_pipeline/alembic/script.py.mako
  - alembic.ini
autonomous: true
requirements: [FND-02]
user_setup: []

must_haves:
  truths:
    - "Alembic CLI is installed and available via `uv run alembic`"
    - "Alembic async migration environment is initialized with correct asyncpg DSN"
    - "env.py can create an AsyncEngine and connect to PostgreSQL"
    - "The models package (`src/eth_pipeline/models/`) is ready for SQLAlchemy model definitions"
  artifacts:
    - path: "pyproject.toml"
      provides: "alembic>=1.18.4 and sqlalchemy[asyncio]>=2.0.50 in dependencies"
      contains: "sqlalchemy\\[asyncio\\]"
    - path: "alembic.ini"
      provides: "Alembic configuration with sqlalchemy.url from env vars"
    - path: "src/eth_pipeline/alembic/env.py"
      provides: "Async-capable migration runner with target_metadata"
      min_lines: 30
    - path: "src/eth_pipeline/models/__init__.py"
      provides: "Empty models package (with docstring + __future__ annotations)"
  key_links:
    - from: "pyproject.toml"
      to: ".venv"
      via: "uv sync --frozen updates uv.lock"
    - from: "src/eth_pipeline/alembic/env.py"
      to: "src/eth_pipeline/models/v7_event.py"
      via: "target_metadata = Base.metadata"
---

<objective>
Install Alembic + SQLAlchemy dependencies and initialize the Alembic async migration environment.

Purpose: FND-02 requires a proper Alembic migration system for schema versioning. This plan sets up the tooling so future plans can create migration scripts and run them.
Output: Working Alembic CLI with async env.py configured for this project's PostgreSQL connection.
</objective>

<execution_context>
@/home/u/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/u/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-foundation/33-RESEARCH.md
@.planning/phases/33-foundation/33-PATTERNS.md

@src/eth_pipeline/db.py
@src/eth_pipeline/api/lifespan.py
@pyproject.toml
@src/eth_pipeline/schema.sql
@scripts/init_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alembic and sqlalchemy dependencies to pyproject.toml and install</name>

  <read_first>
    /home/u/src/eth/pyproject.toml — current dependencies list, exact formatting
  </read_first>

  <files>
    pyproject.toml
  </files>

  <action>
    Edit pyproject.toml to add two new entries to the `dependencies` list in alphabetical order:
    - `"alembic>=1.18.4"`
    - `"sqlalchemy[asyncio]>=2.0.50"`
    
    Insert them between the existing `asyncpg>=0.30.0` and `fastapi[standard]>=0.115.0` lines (alphabetical order: `alembic` comes before `asyncpg` in ASCII). Keep the existing formatting (double quotes, trailing comma on every line except the last).
    
    After editing, run `uv sync` (not `uv sync --frozen` since pyproject.toml changed) to install the new packages and update uv.lock. Confirm both are importable: `uv run python -c "import alembic; import sqlalchemy; print(alembic.__version__, sqlalchemy.__version__)"`.
    
    Do NOT add geoalchemy2 — per RESEARCH.md Section "Standard Stack — Supporting", the project uses raw SQL via asyncpg, not the SQLAlchemy ORM, so geoalchemy2 is unnecessary.
  </action>

  <acceptance_criteria>
    - pyproject.toml contains `"alembic>=1.18.4"` in dependencies
    - pyproject.toml contains `"sqlalchemy[asyncio]>=2.0.50"` in dependencies
    - `uv run python -c "import alembic; import sqlalchemy; print(alembic.__version__, sqlalchemy.__version__)"` exits 0 and prints version strings
    - `uv lock` file is up to date (confirmed by `uv sync` completing without error)
  </acceptance_criteria>

  <verify>
    <automated>uv run python -c "import alembic; import sqlalchemy; print(alembic.__version__, sqlalchemy.__version__)"</automated>
  </verify>

  <done>
    alembic 1.18.4+ and sqlalchemy 2.0.50+ are installed; pyproject.toml has both deps; uv.lock updated
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize Alembic with async template and configure env.py for the project</name>

  <read_first>
    /home/u/src/eth/src/eth_pipeline/db.py — DSN construction pattern (env var names, defaults)
    /home/u/src/eth/src/eth_pipeline/api/lifespan.py — async engine lifecycle, import conventions, `from __future__ import annotations` pattern
    /home/u/src/eth/.planning/phases/33-foundation/33-RESEARCH.md — Section "Pattern 2: Manual Initial Migration" for env.py structure, Section "Code Examples: Alembic Async env.py" for the full async env.py template
    /home/u/src/eth/.planning/phases/33-foundation/33-PATTERNS.md — "src/eth_pipeline/alembic/env.py" section for project-specific import conventions and patterns
  </read_first>

  <files>
    src/eth_pipeline/alembic/env.py
    src/eth_pipeline/alembic/script.py.mako
    alembic.ini
    src/eth_pipeline/models/__init__.py
  </files>

  <action>
    Step 1: Create the `src/eth_pipeline/alembic/` directory structure by running:
    ```
    uv run alembic init --template async src/eth_pipeline/alembic
    ```
    This will create `src/eth_pipeline/alembic/env.py`, `src/eth_pipeline/alembic/script.py.mako`, and `alembic.ini` in the project root.
    If `alembic.ini` is created at the wrong path relative to the project, move it to `/home/u/src/eth/alembic.ini`.

    Step 2: Edit `alembic.ini`:
    - Set `sqlalchemy.url` to: `postgresql+asyncpg://%(PGUSER)s:%(PGPASSWORD)s@%(PGHOST)s:%(PGPORT)s/%(PGDATABASE)s`
    - Set `script_location` to: `src/eth_pipeline/alembic`
    - Do NOT hardcode credentials. Use Alembic's `%(VAR)s` substitution syntax.
    
    Step 3: Edit `src/eth_pipeline/alembic/env.py`:
    - Replace the entire file content with the async env.py pattern from RESEARCH.md Section "Code Examples" (lines 325-373), with these project-specific changes:
      - Imports follow project convention: standard library first, then third-party, then project-local
      - `from __future__ import annotations` at the top (project convention from all existing files)
      - `import logging` and `logger = logging.getLogger(__name__)` after imports
      - The `target_metadata` line should import from `eth_pipeline.models.v7_event`: `from eth_pipeline.models.v7_event import Base`
      - Keep the `run_async_migrations()` function using `create_async_engine` from `sqlalchemy.ext.asyncio`
      - DSN resolution: read the alembic.ini `sqlalchemy.url` via `config.get_main_option("sqlalchemy.url")` and SUBSTITUTE `%(PGUSER)s` etc. placeholders with env var values using `os.environ.get(...)` with defaults matching `db.py` (PGUSER=eth, PGPASSWORD=eth, PGHOST=localhost, PGPORT=5432, PGDATABASE=eth).
      
    Step 4: Create `src/eth_pipeline/models/__init__.py`:
    - Docstring: `"""SQLAlchemy ORM models for eth-pipeline."""`
    - Follow project boilerplate from PATTERNS.md "Module Boilerplate": `from __future__ import annotations`, `import logging`, `logger = logging.getLogger(__name__)`
    - Import and re-export `Base` from `.v7_event` so consumers can do `from eth_pipeline.models import Base`
    
    Important: The `env.py` must work with async (async engine) and the `alembic upgrade head` command must run without errors. The `target_metadata` import will fail until Plan 02 creates `v7_event.py` — that is intentional. The env.py is set up to import it; Plan 02 creates the file.
    
    Step 5: Verify the Alembic environment is minimally functional:
    - `uv run alembic --version` shows version
    - `uv run alembic check` or `uv run alembic branches` should not crash with Python import errors (even if migration files are missing)
  </action>

  <acceptance_criteria>
    - Alembic directory exists at `src/eth_pipeline/alembic/` with `env.py` and `script.py.mako`
    - `alembic.ini` exists at project root with `script_location = src/eth_pipeline/alembic`
    - `alembic.ini` uses `%(PGUSER)s` etc. substitution — no hardcoded credentials
    - `env.py` uses `from __future__ import annotations` and `from sqlalchemy.ext.asyncio import create_async_engine`
    - `env.py` references `Base` from `eth_pipeline.models.v7_event` as `target_metadata`
    - `src/eth_pipeline/models/__init__.py` exists and exports `Base`
    - `uv run alembic --version` exits 0
  </acceptance_criteria>

  <verify>
    <automated>uv run alembic --version</automated>
  </verify>

  <done>
    Alembic async environment initialized; alembic.ini configured with DSN substitution; env.py targets v7_event.Base.metadata; models/__init__.py package ready for Plan 02
  </done>
</task>

</tasks>

<!-- Artifacts this phase produces -->
<!--
New file paths:
- src/eth_pipeline/alembic/env.py
- src/eth_pipeline/alembic/script.py.mako
- alembic.ini
- src/eth_pipeline/models/__init__.py

New CLI commands:
- `uv run alembic --version`
- `uv run alembic upgrade head`

Modified files:
- pyproject.toml (added alembic>=1.18.4, sqlalchemy[asyncio]>=2.0.50)

New packages in .venv:
- alembic >=1.18.4
- sqlalchemy >=2.0.50
-->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml + uv sync | The package install boundary — new dependencies (alembic, sqlalchemy) cross from PyPI into the project |
| alembic.ini DSN | The DSN string embeds credentials; env.py resolves them from env vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | PyPI package install (alembic, sqlalchemy) | mitigate | Per RESEARCH.md Package Legitimacy Audit: all packages verified against official docs. [ASSUMED] verdict allows proceeding without human checkpoint. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | All packages [ASSUMED] per RESEARCH.md audit — well-established mature libraries. No blocking checkpoint needed. |
| T-33-02 | Info Disclosure | alembic.ini DSN with credentials | accept | Local development only; credentials are non-production defaults. Production deployment sets via env vars. |
</threat_model>

<verification>
- `uv run python -c "import alembic; import sqlalchemy; print(alembic.__version__, sqlalchemy.__version__)"` succeeds
- `uv run alembic --version` exits 0
- `src/eth_pipeline/alembic/env.py` exists and contains async engine setup
- `alembic.ini` has `script_location = src/eth_pipeline/alembic`
</verification>

<success_criteria>
- All 4 new files created: env.py, script.py.mako, alembic.ini, models/__init__.py
- pyproject.toml updated with alembic and sqlalchemy deps
- uv sync completed, lock file updated
- Alembic CLI responds correctly
</success_criteria>

<output>
Create `.planning/phases/33-foundation/33-01-SUMMARY.md` when done
</output>
