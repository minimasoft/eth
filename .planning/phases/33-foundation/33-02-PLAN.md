---
phase: 33-foundation
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/eth_pipeline/models/v7_event.py
  - src/eth_pipeline/alembic/versions/0001_v7_foundation.py
autonomous: true
requirements: [FND-01, FND-02, FND-03, FND-04]
user_setup:
  - service: PostgreSQL (Docker)
    why: "Must be running to execute alembic upgrade head (schema push)"
    env_vars:
      - name: PGUSER
        source: "docker-compose.yml (eth)"
      - name: PGPASSWORD
        source: "docker-compose.yml (eth)"
      - name: PGHOST
        source: "docker-compose.yml (localhost for local dev)"
      - name: PGPORT
        source: "docker-compose.yml (5432)"
      - name: PGDATABASE
        source: "docker-compose.yml (eth)"

must_haves:
  truths:
    - "SQLAlchemy ORM models exist for all 5 v7.0 tables: event_v2, event_location, event_participant_v2, event_document, event_ref"
    - "Base.metadata targets the correct tables for future --autogenerate use"
    - "Migration script 0001 is a manual migration with down_revision=None, enabling PostGIS, adding schema_version, creating all 5 tables"
    - "All FK constraints use ON DELETE CASCADE"
    - "Migration can be upgraded and downgraded cleanly"
    - "PostGIS extension is enabled (SELECT PostGIS_Version() returns a version string)"
  artifacts:
    - path: "src/eth_pipeline/models/v7_event.py"
      provides: "SQLAlchemy Base + 5 model classes for v7.0 schema"
      contains: "class Base"
    - path: "src/eth_pipeline/alembic/versions/0001_v7_foundation.py"
      provides: "Initial migration: PostGIS + schema_version + 5 new tables with CASCADE FKs"
      min_lines: 80
  key_links:
    - from: "0001_v7_foundation.py"
      to: "v7_event.py"
      via: "both define the same 5 tables with matching column definitions"
    - from: "v7_event.py"
      to: "alembic/env.py"
      via: "target_metadata = Base.metadata (env.py imports Base from v7_event)"
---

<objective>
Create the SQLAlchemy ORM models for the v7.0 event-centric schema and the initial Alembic migration script, then apply the migration to the database.

Purpose: FND-01 (additive schema tables), FND-02 (Alembic migration), FND-03 (PostGIS), FND-04 (cascade FKs) all converge here. This plan produces the actual DDL.
Output: 5 new PostgreSQL tables created, PostGIS enabled, schema_version column added to document table.
</objective>

<execution_context>
@/home/u/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/u/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-foundation/33-RESEARCH.md
@.planning/phases/33-foundation/33-PATTERNS.md

@src/eth_pipeline/schema.sql
@src/eth_pipeline/db.py
@src/eth_pipeline/models/__init__.py
@.planning/phases/33-foundation/33-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQLAlchemy ORM models for the v7.0 schema</name>

  <read_first>
    /home/u/src/eth/.planning/phases/33-foundation/33-RESEARCH.md — "Pattern 2: Manual Initial Migration" for the exact column definitions, table names, FK constraints, index names, and column types
    /home/u/src/eth/.planning/phases/33-foundation/33-PATTERNS.md — "src/eth_pipeline/models/v7_event.py" section for the SQLAlchemy DeclarativeBase pattern, relationship definitions, module boilerplate conventions (from__future__import annotations, module docstring, logging logger)
    /home/u/src/eth/src/eth_pipeline/schema.sql — existing document table primary key type is TEXT; match that type in FKs
    /home/u/src/eth/src/eth_pipeline/models/__init__.py — to understand the package structure this model file belongs to
  </read_first>

  <files>
    src/eth_pipeline/models/v7_event.py
  </files>

  <action>
    Create `/home/u/src/eth/src/eth_pipeline/models/v7_event.py` with these definitions. Follow the project's module boilerplate from PATTERNS.md: module docstring, `from __future__ import annotations`, imports in order (stdlib, third-party, local), `logger = logging.getLogger(__name__)`.

    Use `sqlalchemy.orm.DeclarativeBase` for the Base class (not `declarative_base()` factory — the PATTERNS.md template uses `class Base(DeclarativeBase): pass`).

    Define these 5 model classes matching the DDL in RESEARCH.md lines 194-265:

    1. **EventV2** — `__tablename__ = "event_v2"`
       - id: Column(String, primary_key=True)
       - document_id: Column(String, ForeignKey("document.id", ondelete="CASCADE"), nullable=False)
       - title: Column(String, nullable=False)
       - description: Column(Text, default="")
       - time_start: Column(DateTime(timezone=True), nullable=True)
       - time_end: Column(DateTime(timezone=True), nullable=True)
       - time_precision: Column(String, nullable=True)
       - extraction_confidence: Column(Float, default=1.0)
       - created_at: Column(DateTime(timezone=True), server_default=func.now()) — NOTE use `server_default=func.now()`
       - updated_at: Column(DateTime(timezone=True), server_default=func.now()) — NOTE use `server_default=func.now()`
       - Relationships: locations, participants, documents, refs — each with `back_populates="event"` and `cascade="all, delete-orphan"`

    2. **EventLocation** — `__tablename__ = "event_location"`
       - id: Column(String, primary_key=True)
       - event_id: Column(String, ForeignKey("event_v2.id", ondelete="CASCADE"), nullable=False)
       - name: Column(String, nullable=False)
       - location_type: Column(String, nullable=True)
       - geom: Column(String, nullable=True) — stored as EWKT text (raw SQL via asyncpg, no geoalchemy2)
       - created_at: Column(DateTime(timezone=True), server_default=func.now())
       - relationship: event = relationship("EventV2", back_populates="locations")

    3. **EventParticipantV2** — `__tablename__ = "event_participant_v2"`
       - id: Column(String, primary_key=True)
       - event_id: Column(String, ForeignKey("event_v2.id", ondelete="CASCADE"), nullable=False)
       - name: Column(String, nullable=False)
       - role: Column(String, default="")
       - confidence: Column(Float, nullable=True)
       - created_at: Column(DateTime(timezone=True), server_default=func.now())
       - relationship: event = relationship("EventV2", back_populates="participants")

    4. **EventDocument** — `__tablename__ = "event_document"`
       - id: Column(String, primary_key=True)
       - event_id: Column(String, ForeignKey("event_v2.id", ondelete="CASCADE"), nullable=False)
       - document_id: Column(String, ForeignKey("document.id", ondelete="CASCADE"), nullable=False)
       - chunk_index: Column(Integer, nullable=True)
       - created_at: Column(DateTime(timezone=True), server_default=func.now())
       - relationship: event = relationship("EventV2", back_populates="documents")

    5. **EventRef** — `__tablename__ = "event_ref"`
       - id: Column(String, primary_key=True)
       - event_id: Column(String, ForeignKey("event_v2.id", ondelete="CASCADE"), nullable=False)
       - reference_type: Column(String, nullable=False)
       - verbatim_text: Column(Text, nullable=False)
       - span_start: Column(Integer, nullable=False)
       - span_end: Column(Integer, nullable=False)
       - chunk_index: Column(Integer, nullable=True)
       - created_at: Column(DateTime(timezone=True), server_default=func.now())
       - relationship: event = relationship("EventV2", back_populates="refs")

    Key constraints from RESEARCH.md:
    - Foreign key to `document` table uses `"document.id"` (the existing v6 table uses TEXT PK)
    - All FK constraints use `ondelete="CASCADE"` (FND-04)
    - All primary keys are `Column(String, primary_key=True)` (matching existing uuid.uuid4().hex pattern)
    - No geoalchemy2 — geom is stored as EWKT text in a String column (per RESEARCH.md "Don't Hand-Roll" and "Standard Stack")
    - Import `func` from `sqlalchemy` for `server_default=func.now()` — NOT from `sqlalchemy.sql`

    Do NOT add any Alembic `__tablename__` metadata or `__table_args__` beyond what's specified.
  </action>

  <acceptance_criteria>
    - `src/eth_pipeline/models/v7_event.py` exists with all 5 model classes
    - Each model class has `__tablename__` matching the DDL in RESEARCH.md
    - EventV2 has all relationships with `cascade="all, delete-orphan"` (locations, participants, documents, refs)
    - Every FK uses `ondelete="CASCADE"`
    - Import verification: `uv run python -c "from eth_pipeline.models.v7_event import Base, EventV2, EventLocation, EventParticipantV2, EventDocument, EventRef; print('OK')"` exits 0
    - `Base.metadata.tables` contains 5 table names: event_v2, event_location, event_participant_v2, event_document, event_ref
  </acceptance_criteria>

  <verify>
    <automated>uv run python -c "from eth_pipeline.models.v7_event import Base, EventV2, EventLocation, EventParticipantV2, EventDocument, EventRef; print('OK:', list(Base.metadata.tables.keys()))"</automated>
  </verify>

  <done>
    5 SQLAlchemy model classes created with matching column definitions, relationship mappings, and ON DELETE CASCADE on all FKs
  </done>
</task>

<task type="auto">
  <name>Task 2: Create manual initial Alembic migration (0001_v7_foundation)</name>

  <read_first>
    /home/u/src/eth/.planning/phases/33-foundation/33-RESEARCH.md — lines 170-275 for the complete migration script code, lines 295-305 for Pitfalls (autogenerate trap, no PostGIS downgrade), lines 397-405 for PostGIS enablement pattern
    /home/u/src/eth/.planning/phases/33-foundation/33-PATTERNS.md — "Migration File Convention (Alembic)" section for the migration header/docstring format
    /home/u/src/eth/src/eth_pipeline/alembic/versions/ — to see if any migration files already exist (create if empty)
    /home/u/src/eth/src/eth_pipeline/schema.sql — lines 1-17 for the `document` table structure (TEXT primary key, column names)
  </read_first>

  <files>
    src/eth_pipeline/alembic/versions/0001_v7_foundation.py
  </files>

  <action>
    Create `/home/u/src/eth/src/eth_pipeline/alembic/versions/0001_v7_foundation.py` following the exact DDL pattern from RESEARCH.md Section "Pattern 2: Manual Initial Migration" (lines 170-275).

    The migration MUST include these steps in order within `upgrade()`:

    Step 1 — Enable PostGIS:
    ```python
    op.execute("CREATE EXTENSION IF NOT EXISTS postgis")
    ```

    Step 2 — Add schema_version to document:
    - Column name: `schema_version`
    - Type: `sa.String(10)`
    - `server_default='v6'` (safety — existing docs stay v6; new pipeline explicitly sets v7)
    - `nullable=False`

    Steps 3-7 — Create the 5 tables with EXACT column definitions from RESEARCH.md lines 194-265:
    - `event_v2` with document_id FK → document(id) ON DELETE CASCADE, indexes on document_id and time_start
    - `event_location` with event_id FK → event_v2(id) ON DELETE CASCADE, index on event_id
    - `event_participant_v2` with event_id FK → event_v2(id) ON DELETE CASCADE, index on event_id
    - `event_document` with event_id FK → event_v2(id) ON DELETE CASCADE AND document_id FK → document(id) ON DELETE CASCADE, indexes on both event_id and document_id
    - `event_ref` with event_id FK → event_v2(id) ON DELETE CASCADE, index on event_id

    CRITICAL details from RESEARCH.md:
    - Index names MUST match: idx_event_v2_document, idx_event_v2_time_start, idx_event_location_event, idx_event_participant_v2_event, idx_event_document_event, idx_event_document_doc, idx_event_ref_event
    - `time_start` and `time_end` use `sa.DateTime(timezone=True)` — NOT sa.TIMESTAMP
    - `extraction_confidence` uses `sa.Float()` — NOT sa.REAL or sa.Numeric
    - All PK columns use `sa.String()` without length limit (matching existing TEXT PK pattern)
    - `ondelete='CASCADE'` on ALL ForeignKeyConstraints (FND-04)
    - `description` uses `sa.Text()` with `default=''` — NOT nullable
    - `server_default=sa.func.now()` for `created_at` columns — NOT `sa.text('now()')`

    In `downgrade()`:
    1. Drop event_ref
    2. Drop event_document
    3. Drop event_participant_v2
    4. Drop event_location
    5. Drop event_v2
    6. Drop schema_version column from document
    7. Do NOT drop PostGIS extension (per RESEARCH.md anti-pattern: "never drop PostGIS in downgrade")

    Migration metadata:
    - `revision = '0001'`
    - `down_revision = None`
    - `branch_labels = None`
    - `depends_on = None`
    - Docstring: `"""v7.0 foundation: PostGIS, schema_version, new event tables"""`
    - Create Date: `2026-06-09`
  </action>

  <acceptance_criteria>
    - File exists at the correct path with revision='0001' and down_revision=None
    - `upgrade()` contains: op.execute with "CREATE EXTENSION IF NOT EXISTS postgis", op.add_column for schema_version, op.create_table for all 5 tables (event_v2, event_location, event_participant_v2, event_document, event_ref), op.create_index for all 7 indexes
    - `downgrade()` contains: op.drop_table for all 5 tables (reverse order), op.drop_column for schema_version
    - downgrade() does NOT drop PostGIS extension
    - Every FK constraint uses ondelete='CASCADE'
    - `uv run alembic heads` shows revision 0001
  </acceptance_criteria>

  <verify>
    <automated>uv run alembic heads 2>&1 | grep -q "0001"</automated>
  </verify>

  <done>
    Migration 0001 exists with complete upgrade/downgrade; Alembic recognizes it as the head revision
  </done>
</task>

<task type="auto" gate="sequential">
  <name>[BLOCKING] Task 3: Apply the migration to the database with alembic upgrade head</name>

  <read_first>
    /home/u/src/eth/src/eth_pipeline/db.py — DSN env var names (PGUSER, PGPASSWORD, PGHOST, PGPORT, PGDATABASE) for local testing
    /home/u/src/eth/docker-compose.yml — service names and credentials for Docker-based PostgreSQL
  </read_first>

  <files>
    (no files created — this is a database operation)
  </files>

  <action>
    This task MUST run AFTER Tasks 1 and 2 of Plan 02 are complete. It applies the migration to a running PostgreSQL database.

    Step 1 — Ensure PostgreSQL is running:
    Check if Docker PostgreSQL is running: `docker compose ps postgres 2>/dev/null | grep -q "Up"`. If not running locally, start it: `docker compose up -d postgres`.
    If Docker is not available, check for a local PostgreSQL instance via `pg_isready -h localhost -p 5432`.
    If neither is available, print instructions to start PostgreSQL and flag the plan as requiring a running database.

    Step 2 — Run the migration:
    ```
    PGUSER=eth PGPASSWORD=eth PGHOST=localhost PGPORT=5432 PGDATABASE=eth PYTHONPATH=. alembic upgrade head
    ```
    Use `PYTHONPATH=.` because the project imports use `eth_pipeline.*` and `src/` is not in the default path.

    If `alembic upgrade head` fails, diagnose the error:
    - "relation 'alembic_version' already exists" → run `alembic stamp head` first, then `alembic upgrade head`
    - ImportError for alembic/sqlalchemy → verify Task 1 of Plan 01 completed
    - Connection refused → PostgreSQL not running
    
    Step 3 — Verify the migration succeeded by checking:
    ```
    PGUSER=eth PGPASSWORD=eth PGHOST=localhost PGPORT=5432 PGDATABASE=eth uv run python -c "
    import asyncio, asyncpg
    async def check():
        conn = await asyncpg.connect('postgresql://eth:eth@localhost:5432/eth')
        # Check PostGIS
        pg_version = await conn.fetchval(\"SELECT PostGIS_Version()\")
        print(f'PostGIS version: {pg_version}')
        # Check tables exist
        tables = ['event_v2', 'event_location', 'event_participant_v2', 'event_document', 'event_ref']
        for t in tables:
            exists = await conn.fetchval(\"SELECT EXISTS(SELECT 1 FROM information_schema.tables WHERE table_name=$1)\", t)
            print(f'Table {t}: {\"✓\" if exists else \"✗ MISSING\"}')
        # Check schema_version column
        col_exists = await conn.fetchval(\"\"\"
            SELECT EXISTS(SELECT 1 FROM information_schema.columns 
            WHERE table_name='document' AND column_name='schema_version')
        \"\"\")
        print(f'schema_version column: {\"✓\" if col_exists else \"✗ MISSING\"}')
        await conn.close()
    asyncio.run(check())
    "
    ```
    
    Step 4 — Test the downgrade works (round-trip):
    ```
    PGUSER=eth PGPASSWORD=eth PGHOST=localhost PGPORT=5432 PGDATABASE=eth PYTHONPATH=. alembic downgrade -1
    ```
    Verify tables are gone, then re-upgrade:
    ```
    PGUSER=eth PGPASSWORD=eth PGHOST=localhost PGPORT=5432 PGDATABASE=eth PYTHONPATH=. alembic upgrade head
    ```
    
    NOTE: The downgrade drops all 5 tables and the schema_version column but leaves PostGIS. This is intentional per RESEARCH.md downgrade pattern.
  </action>

  <acceptance_criteria>
    - `SELECT PostGIS_Version()` returns a non-null version string
    - All 5 tables (event_v2, event_location, event_participant_v2, event_document, event_ref) exist in information_schema.tables
    - `document` table has `schema_version` column
    - `alembic upgrade head` and `alembic downgrade -1` both complete without errors (round-trip verified)
    - After final re-upgrade: `alembic current` shows revision 0001 as current
    - All FK constraints on new tables use ON DELETE CASCADE (verify with: `SELECT tc.constraint_name, rc.delete_rule FROM information_schema.table_constraints tc JOIN information_schema.referential_constraints rc ON tc.constraint_name = rc.constraint_name WHERE tc.table_name LIKE 'event_%' AND tc.constraint_type = 'FOREIGN KEY'`)
  </acceptance_criteria>

  <verify>
    <automated>PGUSER=eth PGPASSWORD=eth PGHOST=localhost PGPORT=5432 PGDATABASE=eth PYTHONPATH=. alembic current 2>&1 | grep -q "0001"</automated>
  </verify>

  <done>
    Alembic migration 0001 applied to database; all 5 tables + PostGIS + schema_version column present; downgrade tested; CASCADE FKs verified
  </done>
</task>

</tasks>

<!-- Artifacts this phase produces -->
<!--
New classes:
- `eth_pipeline.models.v7_event.Base` — SQLAlchemy DeclarativeBase subclass
- `eth_pipeline.models.v7_event.EventV2` — event_v2 table ORM model
- `eth_pipeline.models.v7_event.EventLocation` — event_location table ORM model
- `eth_pipeline.models.v7_event.EventParticipantV2` — event_participant_v2 table ORM model
- `eth_pipeline.models.v7_event.EventDocument` — event_document table ORM model
- `eth_pipeline.models.v7_event.EventRef` — event_ref table ORM model

New file paths:
- src/eth_pipeline/models/v7_event.py
- src/eth_pipeline/alembic/versions/0001_v7_foundation.py

Database objects created:
- Table event_v2 (columns: id, document_id, title, description, time_start, time_end, time_precision, extraction_confidence, created_at, updated_at)
- Table event_location (columns: id, event_id, name, location_type, geom, created_at)
- Table event_participant_v2 (columns: id, event_id, name, role, confidence, created_at)
- Table event_document (columns: id, event_id, document_id, chunk_index, created_at)
- Table event_ref (columns: id, event_id, reference_type, verbatim_text, span_start, span_end, chunk_index, created_at)
- PostGIS extension enabled
- document.schema_version column added (String(10), server_default='v6')
- 7 indexes: idx_event_v2_document, idx_event_v2_time_start, idx_event_location_event, idx_event_participant_v2_event, idx_event_document_event, idx_event_document_doc, idx_event_ref_event
-->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration script → PostgreSQL | Static DDL executed from version-controlled code — no user input |
| Model file → SQLAlchemy | ORM definitions used only for autogenerate and future queries |
| schema_version column | Feature flag that controls pipeline routing (v6 vs v7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Denial of Service | ON DELETE CASCADE cascading deletes | accept | Intentional design per FND-04. Ensures referential integrity when parent document is deleted. Risk: a document delete cascades to all v7.0 rows. Accepting — this is correct behavior for clean state management. |
| T-33-04 | Tampering | Migration DDL static code | mitigate | Migration scripts are version-controlled, peer-reviewed code. No user input can modify them. Alembic tracks which migrations have been applied, preventing re-execution. |
| T-33-05 | Information Disclosure | schema_version column on document | accept | Contains only 'v6' or 'v7' string — no sensitive data. Feature flag only. |
</threat_model>

<verification>
- All 5 v7.0 tables exist in the database (verified via information_schema)
- PostGIS extension enabled (verified via SELECT PostGIS_Version())
- document.schema_version column exists with server_default='v6'
- Migration round-trip (upgrade → downgrade → re-upgrade) succeeds
- alembic current shows 0001 as current head
</verification>

<success_criteria>
- Alembic migration 0001 is applied to the database and all 5 new tables exist
- PostGIS extension is enabled and version-checkable
- All FK relations have ON DELETE CASCADE
- document.schema_version column exists with server_default='v6'
- Migration downgrade + re-upgrade round-trip tested successfully
</success_criteria>

<output>
Create `.planning/phases/33-foundation/33-02-SUMMARY.md` when done
</output>
