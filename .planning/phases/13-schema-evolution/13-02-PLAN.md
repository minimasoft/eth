---
phase: 13-schema-evolution
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tests/integration/13-schema-evolution.test.ts
autonomous: false
requirements:
  - OFFS-01
  - OFFS-02
  - OFFS-04
  - LOGS-01
  - EVNT-01
  - EVNT-05
user_setup: []

must_haves:
  truths:
    - "Integration test file exists at tests/integration/13-schema-evolution.test.ts"
    - "All v4.0 schema artifacts are verified via GraphQL introspection (type presence) and SQL DESCRIBE TABLE (field confirmation)"
    - "Test file can execute via `docker compose run --rm integration-tests tests/integration/13-schema-evolution.test.ts`
    - "Existing reference fields (span_start, span_end, reference_type) remain queryable — no regression"
    - "document_event_log INSERT via SQL succeeds and fields can be SELECTed back"
    - "event_entity_link INSERT via SQL succeeds with valid event and canonical_entity record links"
  artifacts:
    - path: "tests/integration/13-schema-evolution.test.ts"
      provides: "Integration tests verifying all Phase 13 schema changes"
      min_lines: 200
      contains: "describe('13. Schema Evolution'"
    - path: "tests/integration/13-schema-evolution.test.ts"
      contains: "skipIfDegraded"
    - path: "tests/integration/13-schema-evolution.test.ts"
      contains: "graphqlQuery"
    - path: "tests/integration/13-schema-evolution.test.ts"
      contains: "sqlExecute"
  key_links:
    - from: "13-schema-evolution.test.ts →"
      to: "GraphQL proxy"
      via: "Schema introspection query verifying new types appear"
      pattern: "__schema"
    - from: "13-schema-evolution.test.ts →"
      to: "SurrealDB /sql endpoint"
      via: "Direct SQL queries for DESCRIBE TABLE and INSERT verification"
      pattern: "sqlExecute"

---

<objective>
Create integration tests in `tests/integration/13-schema-evolution.test.ts` that verify all Phase 13 schema changes are deployed correctly via GraphQL introspection and direct SurrealDB SQL queries.

Purpose: Prove that the v4.0 DDL was applied successfully and that the new schema artifacts are visible through both GraphQL and direct SQL access. These tests serve as the validation gate for Plan 13-01 and as a regression baseline for downstream v4.0 phases.

Output: `tests/integration/13-schema-evolution.test.ts` with comprehensive schema-verification tests.
</objective>

<execution_context>
@/home/u/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/u/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-schema-evolution/13-CONTEXT.md
@.planning/phases/13-schema-evolution/13-RESEARCH.md
@.planning/phases/13-schema-evolution/13-PATTERNS.md
@src/eth_pipeline/schema.surql
@tests/integration/helpers.ts
@tests/integration/pipeline_m002.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test file 13-schema-evolution.test.ts</name>

  <read_first>
    tests/integration/helpers.ts — Read the full file to understand available helpers (graphqlQuery, graphqlOk, assertGraphqlOk, skipIfDegraded, getSchemaTypeNames, assertNonNull, SQL helpers, httpPost, etc.).
    tests/integration/pipeline_m002.test.ts — Read the following sections:
      - Lines 19-34: Import pattern for node:test, node:assert/strict, and helpers
      - Lines 106-137: sqlExecute helper (direct SurrealDB SQL queries)
      - Lines 143-154: extractSqlRows helper
      - Lines 303-361: Schema introspection test pattern (describe + skipIfDegraded + GraphQL introspection)
      - Lines 504-606: Test with SQL data setup, GraphQL query, and SQL confirmation
    .planning/phases/13-schema-evolution/13-PATTERNS.md — Review the Patterns section for test file patterns especially the degraded-awareness wrapper, getSchemaTypeNames pattern, and sqlExecute pattern.
  </read_first>

  <files>
    tests/integration/13-schema-evolution.test.ts
  </files>

  <action>
    Create `tests/integration/13-schema-evolution.test.ts` with the following structure, following the exact patterns from `pipeline_m002.test.ts`:

    **Imports** (follow pipeline_m002.test.ts lines 19-34 pattern exactly):
      - `describe`, `it`, `before`, `after` from `node:test`
      - `assert` from `node:assert/strict`
      - Helpers from `./helpers.js`: `API_BASE`, `SURREAL_HTTP`, `graphqlQuery`, `graphqlOk`, `assertGraphqlOk`, `skipIfDegraded`, `getSchemaTypeNames`, `assertNonNull`, `GraphQLResponse`
      - Additional helpers defined inline (see below)

    **Inline sqlExecute helper** (copy exact pattern from pipeline_m002.test.ts lines 106-154):
      - `sqlExecute(sql, timeout?)` — POSTs raw SQL to `${SURREAL_HTTP}/sql` with Basic auth and NS/DB headers. Returns `[status, parsedJsonArray | null, error | null]`.
      - `extractSqlRows(sqlResult)` — extracts `.result` arrays from SurrealDB's JSON response envelope.
      - Add these at module scope after imports.

    **Test suite structure** — use `describe("13. Schema Evolution", () => { ... })` as the top-level name. Within it, use the `skipIfDegraded` pattern (wrapping every `it()` test body) matching pipeline_m002.test.ts lines 327-329 exactly.

    Use the following test groups (each as a separate `describe` block):

    **Test Group 1: "1. Schema introspection — reference offset fields"**
      - Test: reference table exposes pageNumber, pageOffsetStart, pageOffsetEnd via GraphQL introspection
      - Use `getSchemaTypeNames()` to verify `reference` type exists
      - Use GraphQL introspection with sub-selection (matching the PATTERNS.md lines 268-294 pattern) to fetch fields on the `reference` type and assert that `pageNumber`, `pageOffsetStart`, and `pageOffsetEnd` are in the field list
      - Note: GraphQL auto camelCases these — page_number → pageNumber, page_offset_start → pageOffsetStart, page_offset_end → pageOffsetEnd

    **Test Group 2: "2. Schema introspection — document_event_log table"**
      - Test: documentEventLog type exists in GraphQL schema type list
      - Use `getSchemaTypeNames()` to assert `documentEventLog` type exists
      - Use field introspection on the `documentEventLog` type to verify fields: `id`, `document`, `stepName`, `severity`, `message`, `details`, `createdAt`
        - step_name → stepName, created_at → createdAt

    **Test Group 3: "3. Schema introspection — canonical_entity entity_type includes 'event'"**
      - Test: `entityType` field on `canonical_entity` type still exists and the GraphQL endpoint accepts `entityType` filter with `"event"` value
      - Note: The GraphQL enum for entity_type is derived from the SurrealDB ASSERT. Verify the field exists on the canonical_entity type (same introspection approach as Group 1).
      - Use `sqlExecute` to run a raw DESCRIBE TABLE canonical_entity and confirm the entity_type definition contains 'event' in the assert clause — this is the most reliable way to verify the enum value was added.

    **Test Group 4: "4. Schema introspection — event_entity_link table"**
      - Test: eventEntityLink type exists in GraphQL schema type list
      - Use `getSchemaTypeNames()` to assert `eventEntityLink` type exists
      - Use field introspection to verify fields: `id`, `event`, `entity`, `relationshipType`, `role`, `confidence`, `notes`, `createdAt`
        - relationship_type → relationshipType, created_at → createdAt
      - Additionally use `sqlExecute` with `DESCRIBE TABLE event_entity_link` to verify all 7 fields exist at the DB level and confirm confidence has the 0-1 ASSERT

    **Test Group 5: "5. No regression — existing reference fields still queryable"**
      - Test: Existing reference fields (span_start, span_end, reference_type, event, canonical_entity, created_at, updated_at) are still exposed via GraphQL introspection
      - Use the same field introspection on the `reference` type — assert all original fields exist
      - This verifies the additive DDL did not break existing definitions

    **Test Group 6: "6. Direct SQL — INSERT and SELECT on document_event_log"**
      - Skip if service degraded (uses `skipIfDegraded`)
      - First, use `sqlExecute` to create a minimal test document entry (or use existing document if one is available via GET /documents)
      - Use `sqlExecute` to INSERT a row into document_event_log with:
        - id set to a deterministic string (like `"test-verify-log-entry-001"`)
        - document set to a valid `document:{id}` record link
        - step_name: `"test_verify"`
        - severity: `"info"`
        - message: `"Schema evolution test log entry"`
        - details: a minimal JSON object like `{ "test": true }`
      - Use `sqlExecute` to SELECT the row back and assert `severity === "info"` and `details.test === true`
      - Clean up: DELETE the test row

    **Test Group 7: "7. Direct SQL — INSERT and SELECT on event_entity_link"**
      - Skip if service degraded
      - First create a canonical_entity of type "place" and a canonical_entity of type "event" (to have valid record links)
      - Use `sqlExecute` to INSERT a row into event_entity_link with:
        - event set to the event entity record link
        - entity set to the place entity record link
        - relationship_type: `"located_at"`
        - role: `"location"`
        - confidence: 0.95
        - notes: `"Test link"`
      - Use `sqlExecute` to SELECT the row back and assert `relationship_type === "located_at"` and `confidence === 0.95`
      - Clean up: DELETE the test rows (both link and entities)

    **Per-test patterns to follow:**
    - Each `it()` block wraps its body in `await skipIfDegraded(`${API_BASE}/health`, async () => { ... });` matching pipeline_m002.test.ts pattern exactly
    - Use `const typeNames = await getSchemaTypeNames(); assertNonNull(typeNames, "..."); assert.ok(typeNames.has("TypeName"), "...");` for type presence checks
    - Use GraphQL introspection query pattern:
      ```
      query IntrospectionWithFields {
        __schema { types { name fields { name } } }
      }
      ```
    - For SQL-based verifications, use `sqlExecute` + `extractSqlRows` then assert on extracted field values
    - Follow the same formatting, indentation, and assertion style as pipeline_m002.test.ts

    **TypeScript conventions:**
    - Use `node:test` (not jest) — describe/it/before/after
    - Use `node:assert/strict` for assertions
    - Import helpers from `./helpers.js` (relative import, .js extension for ESM)
    - Use `async function` for inline helper definitions
    - Function return type annotations: `Promise<[number, unknown[] | null, string | null]>` for sqlExecute
  </action>

  <acceptance_criteria>
    tests/integration/13-schema-evolution.test.ts exists and is at least 200 lines
    File has correct imports: `describe`, `it`, `before`, `after` from `node:test`; `assert` from `node:assert/strict`; helpers from `./helpers.js`
    File has inline `sqlExecute` function using Basic auth to ${SURREAL_HTTP}/sql with NS/DB headers
    File has inline `extractSqlRows` function that extracts `.result` arrays
    Top-level describe name is `"13. Schema Evolution"`
    Contains describe "1. Schema introspection — reference offset fields" with GraphQL field check for pageNumber, pageOffsetStart, pageOffsetEnd
    Contains describe "2. Schema introspection — document_event_log table" with GraphQL type+field check
    Contains describe "3. Schema introspection — canonical_entity entity_type includes 'event'" with SQL DESCRIBE TABLE verification
    Contains describe "4. Schema introspection — event_entity_link table" with GraphQL type+field check and SQL DESCRIBE TABLE verification
    Contains describe "5. No regression — existing reference fields still queryable"
    Contains describe "6. Direct SQL — INSERT and SELECT on document_event_log" with INSERT, SELECT, and cleanup
    Contains describe "7. Direct SQL — INSERT and SELECT on event_entity_link" with canonical_entity creation, INSERT, SELECT, and cleanup
    Every `it()` block wraps body in `skipIfDegraded(...)` 
    Every `it()` block returns `Promise<void>` (async)
    All 7 describe groups present with at least one it() block each
  </acceptance_criteria>

  <verify>
    <automated>
      # File exists and is non-empty
      wc -l tests/integration/13-schema-evolution.test.ts
      # Top-level describe
      grep -c "describe.*13. Schema Evolution" tests/integration/13-schema-evolution.test.ts
      # All 7 test groups present
      grep -c "describe.*reference offset fields" tests/integration/13-schema-evolution.test.ts
      grep -c "describe.*document_event_log table" tests/integration/13-schema-evolution.test.ts
      grep -c "describe.*includes.*event" tests/integration/13-schema-evolution.test.ts
      grep -c "describe.*event_entity_link table" tests/integration/13-schema-evolution.test.ts
      grep -c "describe.*regression" tests/integration/13-schema-evolution.test.ts
      grep -c "describe.*INSERT and SELECT.*document_event_log" tests/integration/13-schema-evolution.test.ts
      grep -c "describe.*INSERT and SELECT.*event_entity_link" tests/integration/13-schema-evolution.test.ts
      # All use skipIfDegraded
      grep -c "skipIfDegraded" tests/integration/13-schema-evolution.test.ts
      # Has sqlExecute helper
      grep -c "sqlExecute" tests/integration/13-schema-evolution.test.ts
      # Has GraphQL field assertions for camelCased field names
      grep -c "pageNumber" tests/integration/13-schema-evolution.test.ts
      grep -c "pageOffsetStart" tests/integration/13-schema-evolution.test.ts
      grep -c "pageOffsetEnd" tests/integration/13-schema-evolution.test.ts
      grep -c "stepName" tests/integration/13-schema-evolution.test.ts
      grep -c "relationshipType" tests/integration/13-schema-evolution.test.ts
    </automated>
  </verify>

  <done>
    13-schema-evolution.test.ts exists with 7 test groups (14+ individual tests) covering all schema changes. Tests verify GraphQL type+field exposure, SQL-level field definitions, no regression on existing fields, and round-trip INSERT+SELECT for both new tables.
  </done>
</task>

</tasks>

## Artifacts This Phase Produces

| Artifact | Type | Location |
|----------|------|----------|
| `13-schema-evolution.test.ts` | New integration test file | `tests/integration/13-schema-evolution.test.ts` |

**What it tests:**
| Test Group | Requirement | Verification Method | What It Proves |
|-----------|-------------|-------------------|----------------|
| 1. Reference offset fields | OFFS-01 | GraphQL introspection | pageNumber, pageOffsetStart, pageOffsetEnd are queryable fields |
| 2. document_event_log table | LOGS-01 | GraphQL introspection + field list | All 7 fields exposed via GraphQL |
| 3. entity_type includes 'event' | EVNT-01 | SQL DESCRIBE TABLE | The ASSERT clause at DB level includes 'event' |
| 4. event_entity_link table | EVNT-05 | GraphQL introspection + SQL DESCRIBE | All 7 fields exist, confidence has 0-1 ASSERT |
| 5. No regression | — | GraphQL field introspection | Existing reference fields unchanged |
| 6. Log INSERT/SELECT | LOGS-01 | Direct SQL round-trip | document_event_log accepts INSERT, data retrievable |
| 7. Link INSERT/SELECT | EVNT-05 | Direct SQL round-trip | event_entity_link accepts INSERT with valid record links |

## Artifacts NOT Created

| Artifact | Rationale |
|----------|-----------|
| `test-corpus/` documents/PDFs | Phase 18 concern — synthetic fixtures actively harmful (RESEARCH.md) |
| Python test files | All existing integration tests use TypeScript/node:test |

<threat_model>
## Security Context

Phase 13 integration tests are **read-only schema verification** with isolated test data. No security boundaries are crossed.

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → SurrealDB /sql endpoint | Authenticated via Basic auth (root/root credentials from env vars). Test data is isolated and cleaned up after each test group. |
| Test runner → GraphQL proxy | Passes through the same FastAPI service with no authentication. Tests use the existing degraded-mode check (skipIfDegraded). |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information Disclosure | GraphQL schema introspection | accept | Introspection reveals only schema structure (table names, field names, types) — no data. This is the same surface as the existing test suite. |
| T-13-02-02 | Tampering | Test data INSERT/DELETE into document_event_log and event_entity_link | accept | Test data uses deterministic IDs prefixed with `test-` for easy identification. Each test group performs its own cleanup. In CI, the schema-init service re-applies DDL on each run, leaving a clean slate. |
| T-13-02-03 | Repudiation | No audit trail for test operations | accept | Test runs in CI are ephemeral. No production data is touched. |
| T-13-02-SC | Tampering | N/A — no packages installed | accept | Phase 13 creates no new dependencies. Node.js stdlib only. |
</threat_model>

<verification>

### Test Execution

```bash
# Run just this test file
docker compose run --rm integration-tests tests/integration/13-schema-evolution.test.ts
```

Expected: All test groups pass. The `skipIfDegraded` wrapper handles CI environments where Docker isn't running (graceful skip).

### Smoke Test Commands

```bash
# Confirm the test file has correct syntax
node --check tests/integration/13-schema-evolution.test.ts

# Count test groups
grep -c "^describe(" tests/integration/13-schema-evolution.test.ts
grep -c "^[ ]*it(" tests/integration/13-schema-evolution.test.ts

# Verify imports are correct (no unknown symbols)
grep "from" tests/integration/13-schema-evolution.test.ts | head -10
```

### File Structure Verification

```bash
# All 7 describe groups present
grep -c "Schema introspection" tests/integration/13-schema-evolution.test.ts
grep -c "No regression" tests/integration/13-schema-evolution.test.ts
grep -c "Direct SQL" tests/integration/13-schema-evolution.test.ts

# Every test wrapped in skipIfDegraded
awk '/^[ ]*it\(/{flag=1} flag && /skipIfDegraded/{count++; flag=0} /^[ ]*\}\); flag{flag=0}' tests/integration/13-schema-evolution.test.ts
</verification>

<success_criteria>

Phase 13 integration tests are complete when:
1. `tests/integration/13-schema-evolution.test.ts` exists with all 7 test groups
2. Each test group contains at least one meaningful assertion about schema state
3. All tests use `skipIfDegraded` for graceful CI degradation
4. The test file uses `sqlExecute` for direct SQL queries and `graphqlQuery` for GraphQL introspection
5. Test data is properly cleaned up after the SQL round-trip tests (groups 6 and 7)
6. `node --check tests/integration/13-schema-evolution.test.ts` succeeds (no syntax errors)

</success_criteria>

<output>
Create `.planning/phases/13-schema-evolution/13-02-SUMMARY.md` when done.
</output>
