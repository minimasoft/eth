---
phase: 36-event-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/api/models.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03

must_haves:
  truths:
    - "Pydantic models exist for all v7 event API response shapes"
    - "All 7 new models are importable from eth_pipeline.api.models"
    - "Existing models and imports are NOT broken"
  artifacts:
    - path: "src/eth_pipeline/api/models.py"
      provides: "All v7 event API response models"
      contains: "EventV2ListItem"
      contains: "EventListV2Response"
      contains: "EventLocationDetail"
      contains: "EventParticipantDetail"
      contains: "EventRefDetail"
      contains: "EventV2DetailResponse"
      contains: "ChunkTextResponse"
  key_links:
    - from: "models.py __all__"
      to: "api/__init__.py re-exports"
      via: "import list at lines 12-36"
      pattern: "EventV2ListItem|EventListV2Response"
</---

### Plan 01 — Models

<objective>
Add 7 Pydantic v2 response models for the Phase 36 Event API to `src/eth_pipeline/api/models.py` and update the `__all__` export list.

Purpose: Every API endpoint in Phase 36 needs typed response models for FastAPI `response_model` validation and serialization. These models define the contract that routes, tests, and the Phase 37 UI will consume.

Output: `models.py` gains 7 new model classes and an updated `__all__` tuple.

**Artifacts this phase produces:**
- **New Pydantic classes:** `EventV2ListItem`, `EventListV2Response`, `EventLocationDetail`, `EventParticipantDetail`, `EventRefDetail`, `EventV2DetailResponse`, `ChunkTextResponse` — all in `src/eth_pipeline/api/models.py`
- **Modified exports:** `__all__` tuple in `src/eth_pipeline/api/models.py` — 7 new string entries added
</objective>

<execution_context>
@/home/u/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/u/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-event-api/36-RESEARCH.md (Pattern 4 — Pydantic Models section)
@.planning/phases/36-event-api/36-PATTERNS.md (models.py analog sections)
@src/eth_pipeline/api/models.py (existing models, conventions, __all__ at line 616)
@src/eth_pipeline/models/v7_event.py (v7 schema: EventV2, EventLocation, EventParticipantV2, EventDocument, EventRef column definitions)
@.planning/codebase/CONVENTIONS.md (Pydantic model conventions: PascalCase, snake_case fields, PEP 604 `| None` syntax, per-field """docstrings""")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v7 event API Pydantic models to models.py</name>
  <files>src/eth_pipeline/api/models.py</files>
  <read_first>
src/eth_pipeline/api/models.py (lines 340-418 — existing EventListItem/EventListResponse pattern to follow; lines 240-280 — EntityDetailResponse with nested list models pattern)
src/eth_pipeline/models/v7_event.py (column names and types for all v7 tables — these are the source-of-truth for field names and nullability)
  </read_first>
  <action>
Append 7 new Pydantic v2 BaseModel classes at line 418 of models.py (immediately after `EventListResponse`).

Follow the exact conventions of the existing models:
- `from __future__ import annotations` is already present at line 1
- Use PEP 604 union syntax: `str | None = None`, `list[EventV2ListItem]`
- Every class and field gets a `"""docstring"""`
- Fields reference v7 schema column names from `src/eth_pipeline/models/v7_event.py`:
  - `event_v2`: id, document_id, title, description, time_start (TIMESTAMPTZ), time_end (TIMESTAMPTZ), time_precision, extraction_confidence (Float), created_at (TIMESTAMPTZ), updated_at (TIMESTAMPTZ)
  - `event_location`: id, event_id, name, location_type, geom (EWKT string)
  - `event_participant_v2`: id, event_id, name, role (default=''), confidence (Float, nullable)
  - `event_ref`: id, event_id, reference_type, verbatim_text, span_start (Integer, NOT NULL), span_end (Integer, NOT NULL), chunk_index (Integer, nullable)

Models to create (exact field signatures):

1. **EventV2ListItem** — paginated list item:
   `event_id: str`, `title: str`, `description: str`, `time_start: str | None = None`, `time_end: str | None = None`, `time_precision: str | None = None`, `location_name: str | None = None`, `participant_count: int = 0`, `reference_count: int = 0`, `document_id: str | None = None`, `document_filename: str | None = None`, `extraction_confidence: float = 1.0`, `created_at: str | None = None`

2. **EventListV2Response** — pagination envelope:
   `items: list[EventV2ListItem]`, `total: int`, `page: int`, `per_page: int`, `pages: int`

3. **EventLocationDetail** — location entry in detail:
   `location_id: str`, `name: str`, `location_type: str | None = None`, `geom: str | None = None`

4. **EventParticipantDetail** — participant entry in detail:
   `participant_id: str`, `name: str`, `role: str = ""`, `confidence: float | None = None`

5. **EventRefDetail** — reference entry in detail:
   `reference_id: str`, `reference_type: str`, `verbatim_text: str`, `span_start: int | None = None`, `span_end: int | None = None`, `chunk_index: int | None = None`

6. **EventV2DetailResponse** — full event detail:
   `event_id: str`, `title: str`, `description: str`, `time_start: str | None = None`, `time_end: str | None = None`, `time_precision: str | None = None`, `extraction_confidence: float = 1.0`, `document_id: str | None = None`, `document_filename: str | None = None`, `locations: list[EventLocationDetail]`, `participants: list[EventParticipantDetail]`, `references: list[EventRefDetail]`, `created_at: str | None = None`, `updated_at: str | None = None`

7. **ChunkTextResponse** — chunk text with offsets:
   `document_id: str`, `part_index: int`, `text: str`, `offset_start: int`, `offset_end: int`, `chunk_offset_start: int`, `chunk_offset_end: int`

Do NOT: remove, reorder, or modify any existing models (EventListItem, EventListResponse, ProcessingLogListItem, etc.). Keep everything above line 418 intact. Do NOT include fenced code blocks in action directives.
  </action>
  <verify>
    <automated>uv run python -c "from eth_pipeline.api.models import EventV2ListItem, EventListV2Response, EventLocationDetail, EventParticipantDetail, EventRefDetail, EventV2DetailResponse, ChunkTextResponse; print('All 7 models importable')"</automated>
  </verify>
  <acceptance_criteria>
- `models.py` contains class `EventV2ListItem` with fields including `event_id: str`, `title: str`, `time_start: str | None = None`, `participant_count: int = 0`
- `models.py` contains class `EventListV2Response` with fields `items: list[EventV2ListItem]`, `total`, `page`, `per_page`, `pages`
- `models.py` contains class `EventLocationDetail` with `location_id`, `name`, `location_type`, `geom`
- `models.py` contains class `EventParticipantDetail` with `participant_id`, `name`, `role`, `confidence`
- `models.py` contains class `EventRefDetail` with `reference_id`, `reference_type`, `verbatim_text`, `span_start`, `span_end`, `chunk_index`
- `models.py` contains class `EventV2DetailResponse` with nested `locations: list[EventLocationDetail]`, `participants: list[EventParticipantDetail]`, `references: list[EventRefDetail]`
- `models.py` contains class `ChunkTextResponse` with `document_id`, `part_index`, `text`, `offset_start`, `offset_end`, `chunk_offset_start`, `chunk_offset_end`
- All 7 classes importable without errors
- No existing models (EventListItem, EventListResponse, etc.) are removed or broken
- Pydantic field validation: `EventV2ListItem(event_id="x", title="T", description="D")` succeeds without error
  </acceptance_criteria>
  <done>All 7 new Pydantic models are importable from eth_pipeline.api.models; existing models unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update __all__ export list in models.py</name>
  <files>src/eth_pipeline/api/models.py</files>
  <read_first>
src/eth_pipeline/api/models.py (lines 616-646 — existing __all__ tuple format)
  </read_first>
  <action>
Add the 7 new model class names to the `__all__` tuple starting at line 616.

Insert after the existing `"EventListResponse",` entry (line 632), add these 7 names in alphabetical order mixed with the existing list:
- `"EventLocationDetail"`
- `"EventListV2Response"`
- `"EventParticipantDetail"`
- `"EventRefDetail"`
- `"EventV2DetailResponse"`
- `"EventV2ListItem"`
- `"ChunkTextResponse"`

The existing entries like `"EventsCleared"`, `"LlmCallLogListItem"`, etc. must remain in place. Only add — do NOT remove or reorder existing entries.

Do NOT include fenced code blocks in action directives.
  </action>
  <verify>
    <automated>uv run python -c "from eth_pipeline.api.models import __all__; expected = {'EventV2ListItem','EventListV2Response','EventLocationDetail','EventParticipantDetail','EventRefDetail','EventV2DetailResponse','ChunkTextResponse'}; assert expected.issubset(set(__all__)), f'Missing: {expected - set(__all__)}'; print('__all__ contains all 7 new models')"</automated>
  </verify>
  <acceptance_criteria>
- `models.py` `__all__` tuple contains `"EventV2ListItem"`, `"EventListV2Response"`, `"EventLocationDetail"`, `"EventParticipantDetail"`, `"EventRefDetail"`, `"EventV2DetailResponse"`, `"ChunkTextResponse"`
- All existing `__all__` entries remain present (EventListItem, EventListResponse, EventsCleared, LlmCallLogListItem, etc.)
- `uv run python -c "from eth_pipeline.api.models import *"` succeeds and imports all 7 new models
  </acceptance_criteria>
  <done>__all__ contains all 7 new models + all existing entries; `from eth_pipeline.api.models import *` works</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Unauthenticated HTTP requests reach FastAPI route handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-SC | Tampering | pip installs | accept | No new packages installed in this plan. All deps (fastapi, pydantic, asyncpg) already verified in prior phases. |

No runtime threats in this plan — models are pure data definitions with no DB access, no user input handling, no I/O. STRIDE threats begin in Plan 02 when routes handle query parameters.
</threat_model>

<verification>
```bash
uv run python -c "
from eth_pipeline.api.models import (
    EventV2ListItem, EventListV2Response,
    EventLocationDetail, EventParticipantDetail,
    EventRefDetail, EventV2DetailResponse, ChunkTextResponse
)
print('All 7 v7 event API models import successfully')
"
```
</verification>

<success_criteria>
- 7 new Pydantic v2 BaseModel classes exist in models.py
- All follow existing conventions: `from __future__ import annotations`, PEP 604 union syntax, per-field docstrings
- All are listed in `__all__`
- All are importable without errors
- No existing models modified or removed
</success_criteria>

<output>
Create `.planning/phases/36-event-api/36-01-SUMMARY.md` when done
</output>
