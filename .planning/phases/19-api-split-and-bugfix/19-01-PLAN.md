---
phase: 19-api-split-and-bugfix
plan: 01
type: fix
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/eth_pipeline/api.py
requirements: []
user_setup: []

must_haves:
  truths:
    - "api.py no longer references undefined variable ref_canonical_str"
    - "Split endpoint returns 400 (not 500) when ref canonical_entity doesn't match source"
  artifacts:
    - path: src/eth_pipeline/api.py
      not_contains: "ref_canonical_str"
      not_contains_reason: "Undefined variable causes NameError"

must_not_haves:
  regressions:
    - "Split endpoint behavior changes for valid requests"
    - "Any other variable renamed unintentionally"

key_links:
  - from: CONCERNS.md
    to: src/eth_pipeline/api.py:2183
    via: "ref_canonical_str → str(source_rid) fix"
---

# Plan 01: Fix NameError in Split Endpoint

## Problem

At `api.py:2183`, the log message references `ref_canonical_str` — a variable that is never defined. When the split endpoint's reference validation fails, this raises a `NameError` (500) instead of the intended `HTTPException(400)`.

## Fix

Change `ref_canonical_str` to `str(source_rid)` on line 2183.

`source_rid` is the `RecordID` of the source entity (computed at line 2151), and it's the value the variable was clearly meant to reference (the correct entity the ref actually points to).

## Verification

1. Confirm the line reads `str(source_rid)` (or `ref_canonical` or `str(ref_canonical)`)
2. Run: `python -c "import ast; ast.parse(open('src/eth_pipeline/api.py').read())"` — no syntax errors
3. Optional: start dev server, call split with mismatched ref, expect 400 not 500

## Risk

None. Single-character-ish change, fully backward compatible.
