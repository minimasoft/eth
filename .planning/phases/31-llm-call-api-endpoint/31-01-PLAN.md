---
phase: 31-llm-call-api-endpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/api/models.py
  - src/eth_pipeline/api/routes/documents.py
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "GET /documents/{id}/llm-calls returns paginated results as { items, total, page, per_page, pages }"
    - "Each item contains prompt_text, response_text, all token/cost/duration fields, model, activity_type, and timestamp"
    - "Results are sorted by timestamp ascending (oldest first)"
    - "A document with no LLM call log entries returns empty items — not a 404"
    - "Pagination parameters (page, per_page) are respected with per_page capped at 100"
    - "A non-existent document ID returns 404"
    - "A database error returns 502"
  artifacts:
    - path: "src/eth_pipeline/api/models.py"
      provides: "LlmCallLogListItem and LlmCallLogListResponse Pydantic models"
      contains: "class LlmCallLogListItem"
    - path: "src/eth_pipeline/api/routes/documents.py"
      provides: "GET /documents/{document_id}/llm-calls endpoint"
      contains: "async def get_document_llm_calls"
  key_links:
    - from: "src/eth_pipeline/api/routes/documents.py"
      to: "src/eth_pipeline/api/models.py"
      via: "import LlmCallLogListItem, LlmCallLogListResponse"
      pattern: "from eth_pipeline\\.api\\.models import.*LlmCallLogList"
    - from: "src/eth_pipeline/api/routes/documents.py"
      to: "llm_call_log table (PostgreSQL)"
      via: "SQL queries against llm_call_log"
      pattern: "FROM llm_call_log"
    - from: "LlmCallLogListItem fields"
      to: "llm_call_log table columns (schema.sql:155-169)"
      via: "1:1 field mapping from DB row to Pydantic model"
      pattern: "prompt_text|response_text|prompt_tokens|completion_tokens|total_tokens|cached_tokens|cost|duration_ms|model|activity_type|document|timestamp"

---

<objective>
Add a paginated GET /documents/{document_id}/llm-calls REST API endpoint that returns LLM call log entries for a specific document, including full prompt/response text and all metrics.

**Purpose:** Users can query LLM call logs per document through the REST API, enabling the future Phase 32 UI viewer and external tooling.

**Output:**
- `src/eth_pipeline/api/models.py` — `LlmCallLogListItem` and `LlmCallLogListResponse` Pydantic response models
- `src/eth_pipeline/api/routes/documents.py` — `GET /documents/{document_id}/llm-calls` endpoint
</objective>

<execution_context>
@/home/u/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/u/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/u/src/eth/.planning/ROADMAP.md
@/home/u/src/eth/.planning/STATE.md
@/home/u/src/eth/.planning/phases/31-llm-call-api-endpoint/31-CONTEXT.md
@/home/u/src/eth/src/eth_pipeline/api/models.py
@/home/u/src/eth/src/eth_pipeline/api/routes/documents.py
@/home/u/src/eth/src/eth_pipeline/schema.sql

The `llm_call_log` table schema (schema.sql:155-172) defines these columns:
- `id TEXT PRIMARY KEY`
- `prompt_text TEXT`, `response_text TEXT`
- `prompt_tokens INTEGER`, `completion_tokens INTEGER`, `total_tokens INTEGER`, `cached_tokens INTEGER`
- `cost REAL`, `duration_ms INTEGER`
- `model TEXT`, `activity_type TEXT`
- `document TEXT NOT NULL` (FK to document.id, ON DELETE CASCADE)
- `timestamp TIMESTAMPTZ`

Indexes exist on `document` and `timestamp` columns (Phase 29).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LlmCallLogListItem and LlmCallLogListResponse models</name>
  <files>src/eth_pipeline/api/models.py</files>
  <action>
    Add two new Pydantic models to `src/eth_pipeline/api/models.py` before the `__all__` export list (before line 548), following the `ProcessingLogListItem` / `ProcessingLogListResponse` pattern.

    **LlmCallLogListItem(BaseModel):**
    - `id: str` — llm_call_log record ID
    - `document_id: str` — FK to document
    - `prompt_text: str | None = None` — full LLM prompt text (per D-01)
    - `response_text: str | None = None` — full LLM response text (per D-01)
    - `prompt_tokens: int | None = None`
    - `completion_tokens: int | None = None`
    - `total_tokens: int | None = None`
    - `cached_tokens: int | None = None`
    - `cost: float | None = None` — nullable, matches DocumentTokenUsage.total_cost pattern
    - `duration_ms: int | None = None`
    - `model: str | None = None`
    - `activity_type: str | None = None`
    - `timestamp: str | None = None` — ISO-8601 string
    Include a docstring on the class and each field (same style as ProcessingLogListItem).

    **LlmCallLogListResponse(BaseModel):**
    - `items: list[LlmCallLogListItem]`
    - `total: int`
    - `page: int`
    - `per_page: int`
    - `pages: int`
    Include a docstring on the class and each field (same style as ProcessingLogListResponse).

    Add both names to the `__all__` export list (alphabetically: LlmCallLogListItem before MergeRequest, LlmCallLogListResponse before MergeResponse). Use `int | None` not `Optional[int]`.
  </action>
  <verify>
    <automated>python3 -c "from eth_pipeline.api.models import LlmCallLogListItem, LlmCallLogListResponse; print('Models import OK')"</automated>
  </verify>
  <done>
    - LlmCallLogListItem and LlmCallLogListResponse classes defined in models.py
    - Both names present in __all__
    - Import round-trips without error
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement GET /documents/{document_id}/llm-calls endpoint</name>
  <files>src/eth_pipeline/api/routes/documents.py</files>
  <action>
    Add a new `get_document_llm_calls` endpoint to `documents.py`, placed after `get_document_logs` (after line 549) and before the "List documents" section comment (line 552-554). Follow the exact structure of `get_document_logs` (lines 431-549).

    **Imports:** Add `LlmCallLogListItem, LlmCallLogListResponse` to the existing `from eth_pipeline.api.models import ...` block (lines 15-28).

    **Route:**
    ```python
    @router.get(
        "/documents/{document_id}/llm-calls",
        response_model=LlmCallLogListResponse,
    )
    ```

    **Signature:**
    ```python
    async def get_document_llm_calls(
        document_id: str,
        page: int = Query(1, ge=1),
        per_page: int = Query(20, ge=1, le=100),
    ) -> LlmCallLogListResponse:
    ```

    **Decisions per CONTEXT.md (all must trace back):**
    - D-01: Include full `prompt_text` and `response_text` in response items
    - D-02: Sort by `timestamp ASC` (first call first)
    - D-03: Default `per_page=20` (not 50 like logs)
    - D-04: Max `per_page=100` enforced via `Query(..., le=100)`
    - D-05: No additional filter params beyond `document_id`
    - D-06: Missing document → `HTTPException(404)` (verify doc exists first)
    - D-07: DB error → `HTTPException(502)` with `"Failed to query database."`
    - D-08: Empty (no llm_call_log entries) → `LlmCallLogListResponse(items=[], total=0, page=page, per_page=per_page, pages=1)` — NOT 404, NOT pages=0
    - D-09: per_page capped at 100

    **Implementation flow (same structure as get_document_logs template):**
    1. Verify document exists: `SELECT id FROM document WHERE id = $1` → 404 if None
    2. Count: `SELECT COUNT(*) AS total FROM llm_call_log WHERE document = $1`
    3. Compute pages: if total == 0 → pages = 1 (per D-08); else pages = max(1, (total + per_page - 1) // per_page)
    4. Fetch: `SELECT * FROM llm_call_log WHERE document = $1 ORDER BY timestamp ASC LIMIT $2 OFFSET $3`
    5. Convert timestamp via isoformat() pattern (same as created_at_str in lines 516-523)
    6. Build LlmCallLogListItem with all fields mapped from DB row: id, document_id, prompt_text, response_text, prompt_tokens, completion_tokens, total_tokens, cached_tokens, cost, duration_ms, model, activity_type, timestamp
    7. Log at INFO level on success (same style as line 535-540)
  </action>
  <verify>
    <automated>python3 -c "
from eth_pipeline.api.models import LlmCallLogListResponse, LlmCallLogListItem

# Verify empty response shape
r = LlmCallLogListResponse(items=[], total=0, page=1, per_page=20, pages=1)
assert r.items == []
assert r.total == 0
assert r.page == 1
assert r.per_page == 20
assert r.pages == 1

# Verify item shape with all fields
i = LlmCallLogListItem(
    id='rec1', document_id='doc123',
    prompt_text='hello', response_text='world',
    prompt_tokens=10, completion_tokens=20, total_tokens=30,
    cached_tokens=5, cost=0.0015, duration_ms=500,
    model='gpt-4', activity_type='extract_events',
    timestamp='2026-06-07T12:00:00'
)
assert i.prompt_text == 'hello'
assert i.response_text == 'world'
assert i.total_tokens == 30
assert i.cost == 0.0015

print('Shape verification OK')
"</automated>
  </verify>
  <done>
    - GET /documents/{document_id}/llm-calls endpoint implemented in documents.py
    - Endpoint follows get_document_logs pattern with all 9 D-NN decisions implemented
    - Import of new models added to the import block
    - Empty document returns pages=1, not 404
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → FastAPI | Untrusted input crosses here via URL path and query params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Spoofing | endpoint URL | accept | Public read-only endpoint; no auth layer in scope |
| T-31-02 | Tampering | per_page query param | mitigate | FastAPI Query(ge=1, le=100) caps per_page to prevent abuse |
| T-31-03 | Tampering | page query param | mitigate | FastAPI Query(ge=1) prevents negative page numbers |
| T-31-04 | Information Disclosure | full prompt/response text | accept | API is internal pipeline; prompt/response are already accessible via DB |
| T-31-05 | Denial of Service | unbounded queries | mitigate | LIMIT/OFFSET pagination via per_page cap at 100 prevents single-query overload |
| T-31-06 | Elevation of Privilege | document_id injection | mitigate | SQL uses parameterized queries ($1, $2, $3) — no string interpolation |
</threat_model>

<verification>

### Model Verification
```bash
python3 -c "from eth_pipeline.api.models import LlmCallLogListItem, LlmCallLogListResponse; print('Models OK')"
```

### Endpoint Verification (requires running server with test data)
Run the pipeline API container and verify with curl:
```bash
# Empty document
curl -s 'http://localhost:8001/documents/{test-doc-id}/llm-calls' | python3 -m json.tool
# Expect: {"items":[],"total":0,"page":1,"per_page":20,"pages":1}

# Missing document
curl -s -o /dev/null -w '%{http_code}' 'http://localhost:8001/documents/nonexistent/llm-calls'
# Expect: 404

# Pagination
curl -s 'http://localhost:8001/documents/{doc-with-data}/llm-calls?page=1&per_page=5' | python3 -m json.tool
# Expect: 5 items per page, pages computed correctly

# Max per_page boundary
curl -s 'http://localhost:8001/documents/{doc-id}/llm-calls?per_page=200' | python3 -c "import sys,json; print(json.load(sys.stdin)['per_page'])"
# Expect: 100 (capped)
```
</verification>

<success_criteria>

1. [ ] `LlmCallLogListItem` and `LlmCallLogListResponse` models defined in models.py with all 13 fields + pagination envelope
2. [ ] `GET /documents/{document_id}/llm-calls` endpoint defined in documents.py
3. [ ] Models import successfully: `python3 -c "from eth_pipeline.api.models import LlmCallLogListItem, LlmCallLogListResponse"`
4. [ ] All 9 CONTEXT.md decisions (D-01 through D-09) implemented in endpoint logic
5. [ ] Missing document returns HTTP 404
6. [ ] Document with no llm_call_log entries returns `{ items: [], total: 0, page: N, per_page: N, pages: 1 }` — not 404
7. [ ] Results ordered by timestamp ASC
8. [ ] per_page capped at 100 via Query validator
9. [ ] DB errors return HTTP 502 with `"Failed to query database."`
</success_criteria>

<output>
Create `.planning/phases/31-llm-call-api-endpoint/31-01-SUMMARY.md` when done
</output>
