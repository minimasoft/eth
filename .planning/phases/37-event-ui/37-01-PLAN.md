---
phase: 37-event-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/static/index.html
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Eventos nav tab appears after Registros in the navigation bar"
    - "Clicking the Eventos tab switches to the Eventos tab content area"
    - "The Eventos tab shows a toolbar with search input, document filter dropdown, and refresh button"
    - "The Eventos tab shows a table with 6 column headers: ID, Tiempo, Título, Lugar, Particip., Refs"
    - "The Eventos tab shows a pagination bar with prev/next buttons and page info"
    - "The Eventos tab shows a loading state with spinner and empty state with placeholder message"
  artifacts:
    - path: "src/eth_pipeline/static/index.html"
      provides: "Eventos tab scaffold — nav button, section HTML, CSS, tab registration"
      contains: "tab-btn-eventos"
      contains: "tab-eventos"
      contains: ".event-time"
      contains: "eventos: document.getElementById('tab-eventos')"
  key_links:
    - from: "tab-btn-eventos nav button"
      to: "#tab-eventos section"
      via: "data-tab=\"eventos\" + aria-controls=\"tab-eventos\""
      pattern: "data-tab=\"eventos\""
    - from: "onTabClick('eventos')"
      to: "#tab-eventos"
      via: "switchTab('eventos')"
      pattern: "if \\(tabName === 'eventos'\\)"
---

<objective>
Insert the Eventos tab scaffold into index.html — nav button, full tab section HTML (toolbar, table, pagination, loading/empty states), CSS rules for event list styling, and JS tab registration.

Purpose: Establish the structural foundation for the Eventos tab so Plan 02 can add fetch/render/search/filter logic and Plan 03 can add the detail panel and document viewer.

Output: The Eventos tab is visible in the navigation, clicks switch to a content area with correct layout, but the tab shows the "no data" empty state since no JS fetch logic exists yet.
</objective>

<execution_context>
@/home/u/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/u/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-event-ui/37-RESEARCH.md
@.planning/phases/37-event-ui/37-PATTERNS.md
@.planning/phases/37-event-ui/37-UI-SPEC.md
@src/eth_pipeline/static/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Eventos nav button and tab section HTML</name>
  <files>src/eth_pipeline/static/index.html</files>
  <read_first>
    src/eth_pipeline/static/index.html (lines 972-978 for nav buttons, lines 1148-1200 for tab sections insertion point)
    .planning/phases/37-event-ui/37-PATTERNS.md (lines 1-97 for exact HTML structure, lines 398-397 for nav button position)
    .planning/phases/37-event-ui/37-UI-SPEC.md (lines 132-160 for table column specs, lines 254-295 for copywriting contract)
  </read_first>
  <action>
    1. Insert nav button after the "Registros" button (after line 978, per PATTERNS.md line 396):
        `<button role="tab" aria-selected="false" aria-controls="tab-eventos" id="tab-btn-eventos" data-tab="eventos">Eventos</button>`

    2. Insert the full `<section id="tab-eventos">` HTML between the closing `</section>` of #tab-references (line 1198) and `<section id="tab-logs">` (line 1200). The section must contain:

       **Container:** `<div id="eventos-container">`

       **Header:** `<div class="documents-header"><h2>Eventos</h2></div>`

       **Toolbar** (`.documents-toolbar`):
       - Search wrapper with `<input type="text" id="eventos-search" class="search-input" placeholder="Buscar por título..." aria-label="Buscar eventos por título" />`
       - Search clear button `<button id="eventos-search-clear" class="search-clear" aria-label="Limpiar búsqueda" style="display:none">&times;</button>`
       - Document filter `<select id="eventos-doc-filter" class="status-filter" aria-label="Filtrar por documento">` with default option `<option value="">Todos los documentos</option>`
       - Refresh button with the same SVG icon as `#doc-refresh-btn` (lines 1004 copy the SVG), id="eventos-refresh-btn", aria-label="Actualizar eventos"

       **Loading state:** `<div id="eventos-loading" class="documents-loading">Cargando eventos...</div>`

       **Table container** (style="display:none"): `#eventos-table-container` with `.documents-table` containing:
       - `<thead>` with columns: ID (`.col-id`, 100px width), Tiempo (`.col-time`, 180px width), Título (auto width), Lugar (`.col-location`, 160px), Particip. (`.col-participants`, 100px, right-aligned), Refs (`.col-refs`, 100px, right-aligned)
       - `<tbody id="eventos-tbody"></tbody>`

       **Empty state:** `<div id="eventos-empty" class="placeholder-card" style="display:none">` with heading "No se encontraron eventos" and body text "Los eventos aparecerán aquí después de procesar los documentos"

       **Pagination:** `#eventos-pagination` with `#eventos-prev-btn` (class="btn-outline", disabled, text="← Anterior"), `#eventos-page-info` (class="page-info", text="Página 1 de 1"), `#eventos-next-btn` (class="btn-outline", disabled, text="Siguiente →")

    3. Use the exact CSS class names (`documents-header`, `documents-toolbar`, `search-wrapper`, `search-input`, `search-clear`, `status-filter`, `btn-icon`, `btn-refresh`, `table-container`, `documents-table`, `documents-loading`, `placeholder-card`, `documents-pagination`, `btn-outline`, `page-info`) from the existing codebase — do NOT invent new class names for these reused elements.

    4. Make the `<section>` use `role="tabpanel"`, `aria-labelledby="tab-btn-eventos"`, class="tab-content".
  </action>
  <verify>
    <automated>grep -c 'tab-btn-eventos' src/eth_pipeline/static/index.html | xargs test 1 -eq</automated>
    <automated>grep -c 'id="tab-eventos"' src/eth_pipeline/static/index.html | xargs test 1 -eq</automated>
    <automated>grep -c 'id="eventos-container"' src/eth_pipeline/static/index.html | xargs test 1 -eq</automated>
    <automated>grep -c 'id="eventos-tbody"' src/eth_pipeline/static/index.html | xargs test 1 -eq</automated>
    <automated>grep -c 'id="eventos-pagination"' src/eth_pipeline/static/index.html | xargs test 1 -eq</automated>
  </verify>
  <acceptance_criteria>
     - Nav bar contains "Eventos" button after "Registros" with correct data-tab and aria attributes
    - #tab-eventos section exists with all child elements: toolbar, table container, loading, empty, pagination
    - Table has 6 column headers matching UI-01 spec: ID, Tiempo, Título, Lugar, Particip., Refs
  </acceptance_criteria>
  <done>The Eventos tab section HTML and nav button exist in index.html. Clicking the nav button will switch to the tab area (once JS registration is added).</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for event list and register tab in JS</name>
  <files>src/eth_pipeline/static/index.html</files>
  <read_first>
    src/eth_pipeline/static/index.html (lines 713-745 for loading/pagination CSS, lines 356-530 for table CSS, lines 1309-1361 for sections object and onTabClick)
    .planning/phases/37-event-ui/37-UI-SPEC.md (lines 95-109 for exact CSS class specs, lines 136-139 for nav button markup)
    .planning/phases/37-event-ui/37-PATTERNS.md (lines 395-397 for exact insertion code)
  </read_first>
  <action>
   1. Add CSS for `.event-time` inside the `<style>` block (append before `</style>` near line 965). The rule is: `.event-time { color: #64748b; font-variant-numeric: tabular-nums; white-space: nowrap; }`. No other new CSS is needed — the event list HTML reuses existing `.documents-table`, `.documents-toolbar`, `.documents-pagination`, `.placeholder-card`, `.documents-loading`, `.col-id`, `.search-input`, `.search-wrapper`, `.search-clear`, `.status-filter` classes that already have CSS definitions.

    2. Register "eventos" in the `sections` object (line 1311-1316): add `eventos: document.getElementById('tab-eventos'),` after the `logs` entry.

    3. Add `if (tabName === 'eventos') fetchEventos();` inside `onTabClick()` (after line 1356, before the closing `}`) — this registers the deferred fetch call (fetchEventos() will be defined in Plan 02; until then, clicking the tab will throw a ReferenceError which is acceptable in this scaffold stage). Per PATTERNS.md line 397, the exact insertion is: `if (tabName === 'eventos') fetchEventos();` on a new line before `}` that closes the function.
  </action>
  <verify>
    <automated>grep -c '\.event-time' src/eth_pipeline/static/index.html | xargs test 1 -eq</automated>
    <automated>grep -c "eventos: document.getElementById('tab-eventos')" src/eth_pipeline/static/index.html | xargs test 1 -eq</automated>
    <automated>grep -c "if (tabName === 'eventos')" src/eth_pipeline/static/index.html | xargs test 1 -eq</automated>
  </verify>
  <acceptance_criteria>
    - .event-time CSS class exists with color #64748b, tabular-nums, nowrap
    - sections.eventos maps to document.getElementById('tab-eventos')
    - onTabClick contains branch for 'eventos' calling fetchEventos()
    - Tab registration follows exact pattern of entities/references tabs
  </acceptance_criteria>
  <done>The Eventos tab is fully registered — nav button clicks will call onTabClick('eventos'), which switches to the tab and invokes fetchEventos() (to be defined in Plan 02). The tab shows the empty state by default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → User | Untrusted data rendered via innerHTML using escapeHtml() — existing boundary respected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | None (HTML scaffold only) | accept | No data rendering in this plan — only static HTML structure and CSS. No user input handling, no API data display. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — pure vanilla JS inline code |

**Note:** Threat T-37-02 (XSS via chunk text / verbatim reference rendering) is addressed in Plan 03 where the document viewer with text highlighting is implemented. Plan 01 has no data rendering surface.
</threat_model>

<verification>
## Wave 0 verification

```bash
# Verify backend endpoints are available (prerequisite from Phase 36)
curl -s http://localhost:8000/events?page=1&per_page=20 | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'items' in d, 'Missing items'; print('OK')"
```

## Plan verification

```bash
# All required HTML elements exist
grep -c 'tab-btn-eventos' src/eth_pipeline/static/index.html | xargs test 1 -eq
grep -c 'id="tab-eventos"' src/eth_pipeline/static/index.html | xargs test 1 -eq
grep -c 'id="eventos-search"' src/eth_pipeline/static/index.html | xargs test 1 -eq
grep -c 'id="eventos-doc-filter"' src/eth_pipeline/static/index.html | xargs test 1 -eq
grep -c 'id="eventos-tbody"' src/eth_pipeline/static/index.html | xargs test 1 -eq
grep -c 'id="eventos-pagination"' src/eth_pipeline/static/index.html | xargs test 1 -eq
grep -c 'id="eventos-loading"' src/eth_pipeline/static/index.html | xargs test 1 -eq
grep -c 'id="eventos-empty"' src/eth_pipeline/static/index.html | xargs test 1 -eq
grep -c '\.event-time' src/eth_pipeline/static/index.html | xargs test 1 -eq
grep -c "eventos: document.getElementById('tab-eventos')" src/eth_pipeline/static/index.html | xargs test 1 -eq
```
</verification>

<success_criteria>
- [ ] "Eventos" nav button visible after "Registros"
- [ ] Clicking "Eventos" tab switches to the tab area (empty state shown)
- [ ] Toolbar contains search input, document filter dropdown (empty), refresh button
- [ ] Table has 6 column headers: ID, Tiempo, Título, Lugar, Particip., Refs
- [ ] Pagination bar exists with prev/next buttons and page info
- [ ] Loading state and empty state elements exist in the DOM
- [ ] `.event-time` CSS class is defined
- [ ] `sections.eventos` is registered
- [ ] `onTabClick('eventos')` branch exists
</success_criteria>

## Artifacts This Phase Produces

### DOM Element IDs
- `tab-btn-eventos` — Nav button for Eventos tab
- `tab-eventos` — Tab content section
- `eventos-container` — Wrapper div for list view
- `eventos-search` — Title search input
- `eventos-search-clear` — Search clear button (×)
- `eventos-doc-filter` — Document filter `<select>` dropdown
- `eventos-refresh-btn` — Refresh button
- `eventos-loading` — Loading state container (spinner + "Cargando eventos...")
- `eventos-table-container` — Table wrapper div
- `eventos-tbody` — Table body for event rows
- `eventos-empty` — Empty state placeholder card
- `eventos-pagination` — Pagination bar
- `eventos-prev-btn` — Previous page button
- `eventos-next-btn` — Next page button
- `eventos-page-info` — Page info span ("Página X de Y")

### CSS Classes
- `.event-time` — Time column styling (color #64748b, tabular-nums, nowrap)

### JS Symbols
- `sections.eventos` — Tab section reference in sections map
- `onTabClick` `'eventos'` branch — Deferred fetch call

</output>

<output>
Create `.planning/phases/37-event-ui/37-01-SUMMARY.md` when done
</output>
