# Plan: Phase 23 — Entity Resolution Prompt & Batching Fix — Verification

**Goal:** Verify all 5 requirements (RES-01 through RES-05) are satisfied by the existing codebase, run existing tests, and confirm no regressions.

## Success Criteria

1. ✓ **RES-01**: LLM resolution prompts do NOT include full document text — confirmed: `_build_resolution_payload()` sends only `verbatim_text` + `reference_type`
2. ✓ **RES-02**: Only reference `verbatim_text` snippets are included — confirmed: no `document_context` or `existing_entities` in payload
3. ✓ **RES-03**: Batching at ~240K tokens — confirmed: `_BATCH_MAX_TOKENS = 240_000`, `batch_references()` exists and is used
4. ✓ **RES-04**: LLM groups, code dedup — confirmed: `ENTITY_RESOLUTION_SCHEMA` returns groups; activities do NFD+casefold exact match → fuzzy CONTAINS → create new
5. ✓ **RES-05**: Both activities updated — confirmed: both `resolve_entities_activity` and `resolve_entities_with_search_activity` follow the same pattern

## Verification Steps

1. Run existing integration tests to confirm no regressions
2. Run unit tests for llm.py (if any exist)
3. Verify `build_resolution_payload` never includes `text_content` or `document_context` via grep
4. Verify `resolve_entities_activity` and `resolve_entities_with_search_activity` never query or send `text_content`
5. Write verification summary

## No Code Changes

Requirements are already implemented. No new code needed.
