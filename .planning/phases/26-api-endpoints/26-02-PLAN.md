---
phase: 26-api-endpoints
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/pipeline_v6.test.ts
  - tests/integration/helpers.ts
autonomous: true
requirements:
  - API-01
  - API-02
user_setup: []
must_haves:
  truths:
    - "GET /references?document=X returns only references belonging to document X"
    - "GET /references?entity_type=person returns only references linked to person-type entities"
    - "GET /references?entity_id=X returns only references linked to entity X"
    - "GET /references?event_element=tiempo returns only references with element_field=tiempo"
    - "GET /events?entity_type=person returns only events with person-type participants"
    - "GET /events?entity_id=X returns only events with participant entity X"
  artifacts:
    - path: "tests/integration/pipeline_v6.test.ts"
      provides: "Integration test suite — new test groups for enhanced API filters"
      min_lines: 450
      test_groups:
        - "Enhanced reference filters (document, event_element, entity_type, entity_id)"
        - "Event entity filters (entity_type, entity_id)"
    - path: "tests/integration/helpers.ts"
      provides: "Updated helper functions for filter-based queries"
  key_links:
    - from: "pipeline_v6.test.ts"
      to: "helpers.ts"
      via: "listReferences, listEvents"
      pattern: "import.*from.*helpers\\.(js|ts)"
    - from: "pipeline_v6.test.ts"
      to: "GET /references"
      via: "HTTP GET request with query params"
      pattern: "/references"
    - from: "pipeline_v6.test.ts"
      to: "GET /events"
      via: "HTTP GET request with query params"
      pattern: "/events"
---

<objective>
Add integration test coverage for enhanced API filter parameters on GET /references and GET /events. Verify that the new filter params (event_element, entity_type, entity_id for references; entity_type, entity_id for events) return correct, filtered results from the REST API.
</objective>

<execution_context>
@/home/u/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/u/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@tests/integration/pipeline_v6.test.ts
@tests/integration/helpers.ts
@src/eth_pipeline/api/routes/references.py
@src/eth_pipeline/api/routes/events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add helper functions for enhanced filter tests</name>
  <files>tests/integration/helpers.ts</files>
  <action>
    Add new helper functions below the existing `listEvents`, `listReferences`, `listEntities`, `mergeEntities` block (around line 478):

    1. `filterReferences(params: Record<string, string>): Promise<ReferenceListResponse | null>` — wraps `httpGet` to `/references?${params}` with per_page=100, returns parsed body or null on error. Same pattern as `listReferences` but without default params (overrides fully).

    2. `filterEvents(params: Record<string, string>): Promise<EventListResponse | null>` — wraps `httpGet` to `/events?${params}` with per_page=100, returns parsed body or null on error.

    Do NOT modify existing helper functions. Add new exports and update the `__all__` (or equivalent) if an export list exists at the bottom of the file. Use the same error handling pattern as existing helpers (null on non-200 or fetch error).

    Both helpers should use the existing `httpGet` and `API_BASE` constants imported at the top.
  </action>
  <verify>
    <automated>node --check tests/integration/helpers.ts 2>&1 || true; node -e "import('./tests/integration/helpers.js').then(m => { console.log('Has filterReferences:', typeof m.filterReferences); console.log('Has filterEvents:', typeof m.filterEvents); }).catch(e => console.log('Module check:', e.message))"</automated>
  </verify>
  <done>
    - `filterReferences` helper exported from helpers.ts
    - `filterEvents` helper exported from helpers.ts
    - Both follow the existing error-returns-null pattern
    - Existing test file (pipeline_v6.test.ts) can import them
  </done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for enhanced reference filters</name>
  <files>tests/integration/pipeline_v6.test.ts</files>
  <action>
    Append a new `describe("Enhanced GET /references filters", ...)` block at the end of the file (before any top-level `after` hooks — verify the existing `after` is at line 59 not at the file end).

    The new test group must:

    1. **Document filter** — Create a second document (short text), then call `filterReferences({ document: docId })` and verify `total > 0` and every returned reference's `document_id` matches the requested document.

    2. **entity_type filter** — Call `filterReferences({ entity_type: "person", document: docId })` and verify: 
       - `total >= 0` (non-negative)
       - If `total > 0`, every returned reference has `canonical_entity_type === "person"` or `canonical_entity_id !== null`

    3. **entity_id filter** — If references exist from step 1, pick a reference with a non-null `canonical_entity_id`, then call `filterReferences({ entity_id: thatId })` and verify all returned references have matching `canonical_entity_id`.

    4. **Event element filter** — Call `filterReferences({ event_element: "tiempo", document: docId })` and verify `total >= 0`.

    All tests must use `skipIfDegraded` (same as existing tests) to handle non-LLM environments gracefully.

    Use the existing `documentWasProcessed` flag to skip if the document didn't process. Create a second document if `testDocIds.length >= 1` (reuse the first doc).
  </action>
  <verify>
    <automated>MISSING — Run manually: `npm test -- --test-name-pattern="Enhanced" 2>&1 | tail -30`</automated>
  </verify>
  <done>
    - New test group "Enhanced GET /references filters" exists in pipeline_v6.test.ts
    - Document filter test verifies filtered results belong to the requested document
    - entity_type filter test verifies type-consistent results
    - entity_id filter test verifies entity-specific filtering (when entity data exists)
    - event_element filter test verifies element_field filtering
    - All tests use skipIfDegraded for graceful non-LLM fallback
  </done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests for event entity filters</name>
  <files>tests/integration/pipeline_v6.test.ts</files>
  <action>
    Append a new `describe("Enhanced GET /events filters", ...)` block after the references filter test group.

    The new test group must:

    1. **Entity ID filter** — Call `filterEvents({ entity_id: someEntityId })`:
       - If the document was processed and events exist, pick an entity_id from the first event's references or skip if no entity-linked events exist
       - This is best-effort — if no entity-linked events, log "No entity-linked events to test entity_id filter" and pass
       - If events are returned, verify they all have participant_count >= 0

    2. **Entity type filter** — Call `filterEvents({ entity_type: "person" })`:
       - Verify `total >= 0`
       - Log the count

    3. **Date range filter** — Call `filterEvents({ date_from: "2000-01-01", date_to: "2100-01-01", document: docId })`:
       - Verify `total` equals the document's total event count (or fewer, depending on LLM date extraction)
       - Log the count for comparison

    Use `skipIfDegraded`, `documentWasProcessed`, and the same pattern as existing test groups. Add both `filterEvents` and `filterReferences` to the import from helpers.js.
  </action>
  <verify>
    <automated>MISSING — Run manually: `npm test -- --test-name-pattern="Enhanced" 2>&1 | tail -30`</automated>
  </verify>
  <done>
    - New test group "Enhanced GET /events filters" exists in pipeline_v6.test.ts
    - entity_id filter test exercises entity-specific event filtering
    - entity_type filter exercises type-based event filtering
    - date range filter exercises time-bounded event filtering
    - All tests use skipIfDegraded for graceful fallback
    - `filterEvents` imported from helpers.js
    - `filterReferences` imported from helpers.js
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner→API | Integration tests send HTTP GET requests to the running API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | test helpers | mitigate | Tests use read-only GET endpoints only — no mutations from filter test helpers |
| T-26-05 | Denial of Service | test runner | accept | Tests run against local dev server, small payloads, per_page capped at 100 |
</threat_model>

<verification>
1. TypeScript syntax check: `node --check tests/integration/helpers.ts`
2. Test runner can import new helpers: `node -e "import('./tests/integration/helpers.js').then(m => console.log('OK'))"`
3. Syntax check that no existing test groups were modified (only appended to)
</verification>

<success_criteria>
- Two new test groups appended to pipeline_v6.test.ts (reference filters + event filters)
- Two new helper functions in helpers.ts (filterReferences, filterEvents)
- All new tests compiled and runnable: `npm test -- --test-name-pattern="Enhanced"` discovers 7+ tests
- Existing 4 v6.0 test groups remain unmodified (no regressions)
- Each filter test validates correct result filtering, not just existence (e.g., checks every item's field matches the filter param)
</success_criteria>

<output>
Create `.planning/phases/26-api-endpoints/26-02-SUMMARY.md` when done
</output>
