---
phase: 38-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0002_cleanup_old_tables.py
  - tests/test_schema.py
autonomous: true
requirements:
  - CLN-01

must_haves:
  truths:
    - "Alembic migration 0002 drops event_participant, event_entity_link, reference, event, canonical_entity in FK-safe order"
    - "document_event_log table survives the migration (retained for v7 ProcessingLogger)"
    - "test_schema.py no longer expects old tables to exist"
  artifacts:
    - path: "alembic/versions/0002_cleanup_old_tables.py"
      provides: "Alembic migration that drops 5 old v6 tables"
      contains: "op.drop_table('canonical_entity')"
    - path: "tests/test_schema.py"
      provides: "Schema test updated for post-cleanup state"
      contains: "test_old_tables_dropped"
  key_links:
    - from: "alembic/versions/0002_cleanup_old_tables.py"
      to: "PostgreSQL"
      via: "op.drop_table() calls in FK-safe order"
      pattern: "op\\.drop_table\\('event_participant'\\)"
---

<objective>
Drop the 5 old v6 event/reference/entity tables from PostgreSQL via an Alembic migration, then update schema tests to reflect the post-cleanup state.

**CRITICAL OVERRIDE (from RESEARCH.md):** The `document_event_log` table is RETAINED — it is actively used by the `ProcessingLogger` class in the v7 system. Success Criterion #1 is overridden on this point. Tables to drop: `event_participant`, `event_entity_link`, `reference`, `event`, `canonical_entity` only.

Purpose: Fulfill CLN-01 — old tables must be removed. This migration plus test updates is the database half of the cleanup.
Output: New Alembic revision 0002, updated test_schema.py
</objective>

<execution_context>
@/home/u/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/u/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-cleanup/38-RESEARCH.md
@src/eth_pipeline/alembic/versions/0001_v7_foundation.py
@src/eth_pipeline/alembic/env.py
@tests/test_schema.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Alembic migration 0002_cleanup_old_tables.py</name>
  <read_first>
    - src/eth_pipeline/alembic/versions/0001_v7_foundation.py — study the upgrade()/downgrade() pattern, revision assignment, and import style
    - src/eth_pipeline/alembic/env.py — verify the async engine config and migration discovery path
    - .planning/phases/38-cleanup/38-RESEARCH.md — review lines 109-211 (Pattern 2: Alembic Migration for Table Drops) and lines 316-319 (Pitfall 2: FK Violation)
  </read_first>
  <behavior>
    - Test 1: Import succeeds — the module can be imported without ImportError
    - Test 2: revision='0002' and down_revision='0001' are set correctly
    - Test 3: upgrade() calls op.drop_table() in FK-safe order: event_participant, event_entity_link, reference, event, canonical_entity
    - Test 4: downgrade() is a no-op (pass) per Phase 38 design — recreating empty deprecated tables serves no purpose
    - Test 5: module-level docstring explains this is Phase 38 cleanup
  </behavior>
  <files>alembic/versions/0002_cleanup_old_tables.py</files>
  <action>
    Create a new file at src/eth_pipeline/alembic/versions/0002_cleanup_old_tables.py following the exact pattern of 0001_v7_foundation.py. The file must match these concrete identifiers:

    - revision = '0002'
    - down_revision = '0001'
    - upgrade() drops tables in THIS EXACT ORDER: event_participant → event_entity_link → reference → event → canonical_entity. Each is a simple op.drop_table('tablename') call — no conditionals.
    - downgrade() contains only `pass` (no downgrade path — these old tables contained deprecated v6 data and recreating them empty serves no purpose)
    - Use the same import style as 0001: `from alembic import op` (NO sqlalchemy import needed since we're only dropping, not creating)
    - docstring: "Drop old v6 event/reference/entity tables (Phase 38 cleanup)"
    - Do NOT drop: document_event_log, document_chunk, document, llm_usage, llm_call_log, or any v7 table (event_v2, event_location, event_participant_v2, event_document, event_ref)
    - Do NOT add branch_labels or depends_on — match the minimal pattern from 0001

    The migration file lives at: src/eth_pipeline/alembic/versions/0002_cleanup_old_tables.py (under the existing alembic package directory). Verify this path exists by checking that the existing 0001_v7_foundation.py is in the same directory.
  </action>
  <verify>
    <automated>uv run alembic -c src/eth_pipeline/alembic.ini upgrade 0002</automated>
  </verify>
  <done>Migration file exists, revision chain is '0001' → '0002', upgrade() drops 5 tables in FK-safe order, downgrade() is a no-op</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run migration + update test_schema.py for post-cleanup state</name>
  <read_first>
    - tests/test_schema.py — read the current V6_TABLES and V7_TABLES lists, and the test_old_tables_survive test
    - .planning/phases/38-cleanup/38-RESEARCH.md — review lines 269-273 (Phase Requirements → Test Map for CLN-01)
  </read_first>
  <behavior>
    - Test 1: V6_TABLES list no longer contains any of the 5 dropped tables (event, reference, canonical_entity, event_participant, event_entity_link)
    - Test 2: test_old_tables_survive is renamed to test_old_tables_dropped and asserts tables do NOT exist (NOT EXISTS → True)
    - Test 3: V6_TABLES list still contains 'document' and 'document_chunk' (these are retained)
    - Test 4: V7_TABLES list is unchanged
    - Test 5: Running `uv run pytest tests/test_schema.py -x -v` passes
  </behavior>
  <files>tests/test_schema.py</files>
  <action>
    First, run the migration: `uv run alembic -c src/eth_pipeline/alembic.ini upgrade head` (this applies 0002, dropping the 5 old tables). Verify with: `uv run python -c "import asyncpg; import asyncio; asyncio.run(asyncpg.connect(user='eth', password='eth', host='localhost', port=5432, database='eth'))"` — not needed but confirm the migration succeeded by checking no errors.

    Then update tests/test_schema.py:
    - Change V6_TABLES from [document, event, reference, canonical_entity, document_chunk] to [document, document_chunk] — these two shared tables are retained
    - Rename test_old_tables_survive to test_old_tables_dropped
    - Change its assertions: instead of `assert exists is True` (tables must exist), assert `assert exists is False` for each of the 5 dropped tables (event, reference, canonical_entity, event_participant, event_entity_link). Add these 5 table names as a separate list DROPPED_TABLES within the test.
    - Keep document and document_chunk existence checks as-is (they must still exist)
    - Keep test_v7_tables_exist, test_postgis_version, test_schema_version_column_exists unchanged
  </action>
  <verify>
    <automated>uv run pytest tests/test_schema.py -x -v --timeout=30</automated>
  </verify>
  <done>Alembic revision 0002 applied — 5 old tables dropped, test_schema.py verifies old tables are gone while v7 and shared tables remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Alembic → PostgreSQL | Migration DDL crosses from application server to database server |

## Assets
| Asset | Sensitivity |
|-------|-------------|
| event_v2, event_location, event_participant_v2, event_document, event_ref | Production v7 data — must survive migration intact |
| document_chunk | Shared table used by v7 pipeline — must survive |
| document_event_log | Processing log used by v7 ProcessingLogger — must survive |
| llm_usage, llm_call_log | LLM cost tracking — must survive |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Tampering | 0002_cleanup migration — `op.drop_table()` | mitigate | Only 5 tables in the drop list. FK-safe order prevents PostgreSQL errors. Documented override: document_event_log is NOT dropped despite appearing in success criteria. |
| T-38-01-02 | Denial of Service | Migration failure leaving DB in unknown state | mitigate | Alembic wraps upgrade in a transaction; failure rolls back. If downgrade is needed, the empty downgrade() means tables are gone permanently — but this is intentional (deprecated data). |
| T-38-01-03 | Information Disclosure | Orphan data surviving after migration | accept | Data in old tables was superseded by v7 pipeline output. No PII sensitivity. |
</threat_model>

<verification>
- `uv run alembic -c src/eth_pipeline/alembic.ini upgrade head` succeeds without errors
- `uv run pytest tests/test_schema.py -x -v --timeout=30` passes
- Manual DB check: `SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('event','reference','canonical_entity','event_participant','event_entity_link')` returns 0 rows
</verification>

<success_criteria>
- [ ] Alembic migration 0002 created and applied — 5 old tables dropped
- [ ] document_event_log, document_chunk, all v7 tables survive
- [ ] test_schema.py updated and passing
</success_criteria>

<output>
Create `.planning/phases/38-cleanup/38-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

### Schema Changes (database)
| Artifact | Type | Description |
|----------|------|-------------|
| (DROPPED) `event_participant` table | Table removal | Old v6 participant table — FK constraints removed, data deleted |
| (DROPPED) `event_entity_link` table | Table removal | Old v6 entity-event linking table |
| (DROPPED) `reference` table | Table removal | Old v6 verbatim reference table |
| (DROPPED) `event` table | Table removal | Old v6 event flat-field table |
| (DROPPED) `canonical_entity` table | Table removal | Old v6 entity resolution table |

### New Files
| Artifact | Path | Description |
|----------|------|-------------|
| 0002_cleanup migration | `src/eth_pipeline/alembic/versions/0002_cleanup_old_tables.py` | Alembic migration dropping 5 old v6 tables |

### Modified Files
| Artifact | Path | Changes |
|----------|------|---------|
| test_schema.py | `tests/test_schema.py` | V6_TABLES reduced to [document, document_chunk]; test_old_tables_survive renamed to test_old_tables_dropped with inverted assertions |
