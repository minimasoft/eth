---
phase: 38-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 38-02
files_modified:
  - src/eth_pipeline/activities/extract_events.py
  - src/eth_pipeline/activities/resolve_entities.py
  - src/eth_pipeline/activities/resolve_entities_with_search.py
  - src/eth_pipeline/activities/store_extraction_results.py
  - src/eth_pipeline/activities/create_event_canonical_entities.py
  - scripts/cleanup_orphan_entities.py
  - scripts/cleanup_orphan_references.py
  - src/eth_pipeline/activities/__init__.py
  - src/eth_pipeline/activities/_common.py
  - src/eth_pipeline/activities/chunk_document.py
  - src/eth_pipeline/workflows.py
  - src/eth_pipeline/chunker.py
  - src/eth_pipeline/api/routes/documents.py
autonomous: true
requirements:
  - CLN-02

must_haves:
  truths:
    - "extract_events_activity, resolve_entities_activity, store_extraction_results_activity, resolve_entities_with_search_activity, create_event_canonical_entities_activity are not importable"
    - "DocumentProcessingWorkflow class does not exist in workflows.py"
    - "DocumentChunker class and chunk_document() convenience function removed from chunker.py; SmartChunker and DocumentChunk retained"
    - "documents.py no longer references old tables (canonical_entity, event_participant, event_entity_link, reference) or old models (EventsCleared)"
    - "_create_canonical_entity removed from _common.py (inserted into dropped canonical_entity table)"
  artifacts:
    - path: "src/eth_pipeline/activities/__init__.py"
      provides: "Activity re-exports — only v7 activities remain"
      excludes: "extract_events_activity"
    - path: "src/eth_pipeline/activities/_common.py"
      provides: "Shared helpers — _create_canonical_entity removed"
      excludes: "_create_canonical_entity"
    - path: "src/eth_pipeline/activities/chunk_document.py"
      provides: "Chunking activity — imports only SmartChunker, no DocumentChunker"
      excludes: "from eth_pipeline.chunker import DocumentChunker"
    - path: "src/eth_pipeline/workflows.py"
      provides: "Workflow definitions — only DocumentProcessingV7Workflow remains"
      excludes: "class DocumentProcessingWorkflow"
    - path: "src/eth_pipeline/chunker.py"
      provides: "Chunker module — DocumentChunker, ChunkResult, chunk_document() removed; SmartChunker, SmartChunk, DocumentChunk, distribute_balanced retained"
      excludes: "class DocumentChunker"
    - path: "src/eth_pipeline/api/routes/documents.py"
      provides: "Document routes — old-table cleanup block deleted; clear_document_events endpoint removed"
      excludes: "EventsCleared"
  key_links:
    - from: "activities/__init__.py"
      to: "chunk_document.py"
      via: "from eth_pipeline.activities.chunk_document import chunk_document_activity"
      pattern: "chunk_document_activity"
    - from: "workflows.py"
      to: "activities/__init__.py"
      via: "with workflow.unsafe.imports_passed_through()"
      pattern: "from eth_pipeline\\.activities import"
    - from: "documents.py"
      to: "api/models.py"
      via: "DocumentDeleted import (without orphaned_entities_cleaned)"
      pattern: "DocumentDeleted"
---

<objective>
Delete 5 old activity Python files and 2 orphan-cleanup scripts. Clean up imports in `activities/__init__.py`, `_common.py`, and `chunk_document.py`. Remove `DocumentProcessingWorkflow` class and old activity imports from `workflows.py`. Remove `DocumentChunker` class (keeping `SmartChunker` and `DocumentChunk` dataclass) from `chunker.py`. Finally, surgically remove all old-table and old-model references from `documents.py` — the `delete_document` orphan-cleanup block (~260 lines referencing `canonical_entity`, `event_participant`, `event_entity_link`, `reference`) and the `clear_document_events` endpoint (~110 lines returning `EventsCleared`).

Purpose: Fulfill the activity/script/chunker half of CLN-02 — no deprecated activity or workflow code survives. Resolves RESEARCH open questions 2, 3, and 4.

CRITICAL: `chunk_document_activity` is RETAINED — it is the chunking entry point used by the v7 pipeline (routes to `SmartChunker` via `schema_version` check). Only the old `DocumentChunker` import and old-path branch are removed.

Output: 7 files deleted, 6 files edited — all old activity/script/workflow code removed
</objective>

<execution_context>
@/home/u/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/u/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-cleanup/38-RESEARCH.md
@.planning/phases/38-cleanup/38-02-PLAN.md
@src/eth_pipeline/activities/__init__.py
@src/eth_pipeline/activities/_common.py
@src/eth_pipeline/activities/chunk_document.py
@src/eth_pipeline/workflows.py
@src/eth_pipeline/chunker.py
@src/eth_pipeline/api/routes/documents.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 5 old activity files + 2 orphan scripts; clean __init__.py, _common.py, chunk_document.py</name>
  <read_first>
    - src/eth_pipeline/activities/__init__.py — study the full import block (lines 1-58) and __all__ list
    - src/eth_pipeline/activities/_common.py — study _create_canonical_entity function (lines 54-77) and _normalize (line 22-25)
    - src/eth_pipeline/activities/chunk_document.py — study the import block (lines 1-13) and the schema_version branch (lines 40-47)
    - .planning/phases/38-cleanup/38-RESEARCH.md — review lines 448-470 (Open Questions 2-4, now RESOLVED) and lines 127-155 (Recommended Project Structure)
    - Verify _normalize usage: `grep -rn "_normalize" src/eth_pipeline/` — if only used in _common.py itself or old activities, remove it. If used by v7 activities (extract_events_v7.py, store_events_v7.py, resolve_references_v7.py), KEEP it.
  </read_first>
  <files>
    src/eth_pipeline/activities/__init__.py
    src/eth_pipeline/activities/_common.py
    src/eth_pipeline/activities/chunk_document.py
    src/eth_pipeline/activities/extract_events.py
    src/eth_pipeline/activities/resolve_entities.py
    src/eth_pipeline/activities/resolve_entities_with_search.py
    src/eth_pipeline/activities/store_extraction_results.py
    src/eth_pipeline/activities/create_event_canonical_entities.py
    scripts/cleanup_orphan_entities.py
    scripts/cleanup_orphan_references.py
  </files>
  <action>
    Seven deletions + three edits, in this exact order:

    **Step A — DELETE 5 old activity files:**
    - src/eth_pipeline/activities/extract_events.py
    - src/eth_pipeline/activities/resolve_entities.py
    - src/eth_pipeline/activities/resolve_entities_with_search.py
    - src/eth_pipeline/activities/store_extraction_results.py
    - src/eth_pipeline/activities/create_event_canonical_entities.py

    **Step B — DELETE 2 orphan-cleanup scripts:**
    - scripts/cleanup_orphan_entities.py
    - scripts/cleanup_orphan_references.py
    (NOTE: these are at project-root `scripts/`, NOT under `src/eth_pipeline/scripts/`)

    **Step C — EDIT activities/__init__.py:**
    Remove these import lines:
    - `from eth_pipeline.activities._common import (...)` — remove `_create_canonical_entity` from the import. If `_normalize` is confirmed unused by v7 activities (per grep check), remove it too. Keep `_db_params`, `_extract_query_results`, `_get_blob_from_minio` (used by v7 workflows).
    - `from eth_pipeline.activities.create_event_canonical_entities import create_event_canonical_entities_activity`
    - `from eth_pipeline.activities.extract_events import extract_events_activity`
    - `from eth_pipeline.activities.resolve_entities import resolve_entities_activity`
    - `from eth_pipeline.activities.resolve_entities_with_search import resolve_entities_with_search_activity`
    - `from eth_pipeline.activities.store_extraction_results import store_extraction_results_activity`

    Remove these symbols from `__all__`:
    - `create_event_canonical_entities_activity`, `extract_events_activity`, `resolve_entities_activity`, `resolve_entities_with_search_activity`, `store_extraction_results_activity`
    - `_create_canonical_entity` (and `_normalize` if removed)

    KEEP these imports (v7 + shared): `chunk_document_activity`, `extract_events_v7_activity`, `extract_text_activity`, `get_document_metadata_activity`, `get_document_text_activity`, `resolve_references_v7_activity`, `store_events_v7_activity`, `update_document_status_activity`, plus retained `_common` helpers.

    **Step D — EDIT activities/_common.py:**
    Remove `_create_canonical_entity` function (lines 54-77): the entire async function including its try/except block. It inserts into the `canonical_entity` table which was dropped by Plan 38-01. If `_normalize` confirmed v7-unused by grep, remove it too (lines 22-25). Keep `_db_params`, `_extract_query_results`, and `_get_blob_from_minio`.
    
    Also remove the now-unused `import uuid` at the top if `_create_canonical_entity` was the only consumer — check if any other function uses `uuid`.

    **Step E — EDIT activities/chunk_document.py:**
    Remove `from eth_pipeline.chunker import DocumentChunker, SmartChunker` → change to `from eth_pipeline.chunker import SmartChunker` (only SmartChunker remains).
    Remove the old-path branch in the schema_version check (lines 43-46):
    ```python
    # REMOVE:
    else:
        chunker = DocumentChunker()
        chunk_result = chunker.chunk(text, page_offsets)
        chunks = chunk_result.chunks
    ```
    After removal, the if/else becomes a simple if-block: only the `if schema_version == 'v7':` branch remains (lines 40-42). Keep `chunk_document_activity` — it is the chunking entry point for the system.

    **Verification before commit:** Run `uv run python -c "from eth_pipeline.activities import chunk_document_activity, extract_events_v7_activity, store_events_v7_activity, resolve_references_v7_activity; print('All v7 activities importable')"`
  </action>
  <verify>
    <automated>uv run python -c "from eth_pipeline.activities import chunk_document_activity, extract_events_v7_activity, store_events_v7_activity, resolve_references_v7_activity, extract_text_activity, update_document_status_activity, get_document_metadata_activity, get_document_text_activity; print('All v7+shared activities import OK')" && uv run python -c "from eth_pipeline.activities import extract_events_activity" 2>&1 | grep -q ImportError && echo "extract_events_activity correctly not importable"</automated>
  </verify>
  <done>5 old activity files and 2 orphan scripts deleted; __init__.py imports only v7+shared activities; _common.py has no _create_canonical_entity; chunk_document.py imports only SmartChunker; all v7 activities importable; old activity names raise ImportError</done>
</task>

<task type="auto">
  <name>Task 2: Remove DocumentProcessingWorkflow (old) from workflows.py + remove DocumentChunker from chunker.py</name>
  <read_first>
    - src/eth_pipeline/workflows.py — study: lines 21-42 (imports), lines 47-50 (__all__), lines 53-256 (DocumentProcessingWorkflow class — entire class to remove), lines 327-427 (DocumentProcessingV7Workflow — KEEP)
    - src/eth_pipeline/chunker.py — study: lines 25-72 (DocumentChunk, ChunkResult dataclasses), lines 79-212 (DocumentChunker class), lines 250-272 (chunk_document convenience function)
    - .planning/phases/38-cleanup/38-RESEARCH.md — review lines 333-337 (Pitfall 5: Old Activities Still Imported in workflows.py)
  </read_first>
  <files>
    src/eth_pipeline/workflows.py
    src/eth_pipeline/chunker.py
  </files>
  <action>
    Two file edits — atomic, same commit.

    **File 1: src/eth_pipeline/workflows.py**
    In the `with workflow.unsafe.imports_passed_through():` block (lines 28-42), remove these 5 import lines:
    - `create_event_canonical_entities_activity,`
    - `extract_events_activity,`
    - `resolve_entities_activity,`
    - `resolve_entities_with_search_activity,`
    - `store_extraction_results_activity,`

    Clean up the remaining multi-line import — ensure the closing parenthesis and trailing comma are valid Python. The import should now have: `chunk_document_activity`, `extract_events_v7_activity`, `extract_text_activity`, `get_document_metadata_activity`, `resolve_references_v7_activity`, `store_events_v7_activity`, `update_document_status_activity`.

    Remove the old `from eth_pipeline.activities._common import _db_params, _extract_query_results` if both are still only used by old workflow code — check: `get_document_chunks_activity` (lines 264-285) and `get_prior_events_activity` (lines 288-319) use `_db_params()` and `_extract_query_results()`. Since these two helper activities are in workflows.py AND used by DocumentProcessingV7Workflow, KEEP the _common import.

    Remove `class DocumentProcessingWorkflow` entirely — lines 53-256 (the old workflow class with its `@workflow.run` async method and `run()` method). This is the class decorated with `@workflow.defn` that orchestrates the old v6 pipeline (extract_text → chunk_document → extract_events → store → resolve_entities → create_event_canonical_entities → update_status).

    Update `__all__` (line 47-50): change from `["DocumentProcessingWorkflow", "DocumentProcessingV7Workflow"]` to `["DocumentProcessingV7Workflow"]` (single item list, no trailing comma needed but keep it clean).

    KEEP intact: the module docstring (edit it to remove old workflow references if needed — line 7-10 mentions "top-level DocumentProcessingWorkflow handles two document paths"), the helper query activities `get_document_chunks_activity` and `get_prior_events_activity`, and `DocumentProcessingV7Workflow`. Update the module docstring to only describe the v7 workflow.

    **File 2: src/eth_pipeline/chunker.py**
    Remove three things:

    1. `class DocumentChunker` (lines 79-212) — the entire old chunker class with its `chunk()` method and `_offset_to_page` helper.
    2. `@dataclass class ChunkResult` (lines 55-72) — only used as return type of `DocumentChunker.chunk()`.
    3. `def chunk_document(...)` convenience function (lines 250-272) — only wraps `DocumentChunker`.

    KEEP: `@dataclass class DocumentChunk` (lines 25-52), `@dataclass class SmartChunk` (lines 342-366), `def distribute_balanced(...)` (lines 280-339), `class SmartChunker` (lines 376-491). Also remove the "Convenience function" header comments (lines 242-248, also note the double header — lines 242-249 and 250 both labeled "Convenience function").

    After removal, chunker.py should contain in order: (1) imports, (2) DocumentChunk dataclass, (3) SmartChunk dataclass, (4) distribute_balanced function, (5) SmartChunker class.
  </action>
  <verify>
    <automated>uv run python -c "from eth_pipeline.workflows import DocumentProcessingV7Workflow; print('V7 workflow OK')" && uv run python -c "from eth_pipeline.workflows import DocumentProcessingWorkflow" 2>&1 | grep -q "cannot import name" && echo "Old workflow correctly not importable" && uv run python -c "from eth_pipeline.chunker import SmartChunker, SmartChunk, DocumentChunk; print('Chunker OK')" && uv run python -c "from eth_pipeline.chunker import DocumentChunker" 2>&1 | grep -q "cannot import name" && echo "DocumentChunker correctly not importable"</automated>
  </verify>
  <done>DocumentProcessingWorkflow class removed from workflows.py (only DocumentProcessingV7Workflow remains); old activity imports cleaned; DocumentChunker, ChunkResult, chunk_document() removed from chunker.py; SmartChunker, SmartChunk, DocumentChunk, distribute_balanced retained; old symbols not importable</done>
</task>

<task type="auto">
  <name>Task 3: Clean up documents.py — remove old-table orphan cleanup + clear_document_events endpoint</name>
  <read_first>
    - src/eth_pipeline/api/routes/documents.py — study the `delete_document` function, especially the orphan-entity cleanup block (lines 1000-1260) and the `clear_document_events` endpoint (lines 1262-1372)
    - The `DELETE /documents/{document_id}` function needs the old-table cleanup surgically removed
    - The `DELETE /documents/{document_id}/events` endpoint must be removed entirely (it references dropped tables and the deleted EventsCleared model)
    - .planning/phases/38-cleanup/38-RESEARCH.md — review lines 127-155 (Recommended Project Structure: documents.py EDIT line)
  </read_first>
  <files>src/eth_pipeline/api/routes/documents.py</files>
  <action>
    Two surgical removals in src/eth_pipeline/api/routes/documents.py:

    **1. Clean `delete_document` function (lines ~1000-1260):**
    The function currently does: verify document exists → STEP 1: delete event_participant edges → STEP 1a: collect non-event entities via event_entity_link → STEP 1b: delete event_entity_link edges → STEP 1c: collect event_participant entities → [steps 2-7: delete references, events, event-type entities, orphan entities from reference, orphan entities from event_entity_link, orphan entities from event_participant] → delete document_chunk → delete document → return DocumentDeleted.

    Remove ALL old-table cleanup steps (steps 1 through 8c). Keep:
    - Document existence check (lines 1274-1291 area)
    - `DELETE FROM document_chunk WHERE document = $1` (shared table — keep)
    - `DELETE FROM document WHERE id = $1` (keep)
    - The error handling wrappers
    - The return statement — but change from `DocumentDeleted(document_id=document_id, document_deleted=True, orphaned_entities_cleaned=orphaned)` to `DocumentDeleted(document_id=document_id, document_deleted=True)` (no orphaned_entities_cleaned kwarg)

    Also remove the `orphaned = 0` counter initialization and all `orphaned += ...` increments. Remove the `import` of any removed symbols if they become unused (check if `EventsCleared` was imported at line 26 and if it's no longer used — if so, remove it).

    **2. Remove `clear_document_events` endpoint (lines 1262-1372):**
    Delete the entire endpoint block including:
    - The section comment `# ======================================================================= ... Clear document events endpoint =======================================================================`
    - The `@router.delete("/documents/{document_id}/events", response_model=EventsCleared)` decorator
    - The `async def clear_document_events(document_id: str) -> EventsCleared:` function

    This endpoint queries old tables (event_participant, event_entity_link, canonical_entity) and returns the now-deleted `EventsCleared` model — all invalid after Plan 38-01 and Plan 38-02.

    **Verification after edits:**
    - `uv run python -c "from eth_pipeline.api import app; from eth_pipeline.api.models import DocumentDeleted; d = DocumentDeleted(document_id='test', document_deleted=True); print('documents.py OK')"`
    - Confirm `DocumentDeleted` constructed with 2 args succeeds without the old third arg
  </action>
  <verify>
    <automated>uv run python -c "from eth_pipeline.api import app; from eth_pipeline.api.models import DocumentDeleted; d = DocumentDeleted(document_id='test', document_deleted=True); print('documents.py cleanup OK')"</automated>
  </verify>
  <done>documents.py delete_document no longer touches old tables (canonical_entity, event_participant, event_entity_link, reference); returns DocumentDeleted without orphaned_entities_cleaned; clear_document_events endpoint deleted; EventsCleared import removed if no longer used; FastAPI app starts without ImportError</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Temporal worker → activity imports | Worker starts and imports all registered activities from `activities/__init__.py` — stale imports cause worker crash |
| Python import system → deleted modules | Any remaining code that tries to import deleted activity modules raises ImportError at module load time |

## Assets
| Asset | Sensitivity |
|-------|-------------|
| DocumentProcessingV7Workflow | Production v7 pipeline — must remain importable and operational |
| chunk_document_activity | Chunking entry point — must remain importable and correctly route to SmartChunker |
| SmartChunker, SmartChunk, DocumentChunk | Core chunking infrastructure — must survive cleanup |
| get_document_chunks_activity, get_prior_events_activity | v7 helper activities defined in workflows.py — must remain importable |
| extract_events_v7_activity, store_events_v7_activity, resolve_references_v7_activity | v7 pipeline activities — must remain importable |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-01 | Denial of Service | Stale import in workflows.py or activities/__init__.py referencing deleted activity module | mitigate | All imports cleaned in the same commit as file deletions. Verify: `uv run python -c "from eth_pipeline.workflows import DocumentProcessingV7Workflow"` — full import chain exercised. |
| T-38-03-02 | Denial of Service | Removal of _db_params or _extract_query_results breaking v7 helper activities | mitigate | Explicitly verified — `get_document_chunks_activity` and `get_prior_events_activity` in workflows.py use both helpers. They are RETAINED. |
| T-38-03-03 | Tampering | Deleting chunk_document_activity (which the v7 system needs for initial chunking) | mitigate | chunk_document_activity is RETAINED — only the old DocumentChunker path is removed. The activity still works for v7 documents via SmartChunker. |
| T-38-03-04 | Denial of Service | Removing DocumentChunk dataclass that SmartChunk mirrors | mitigate | DocumentChunk dataclass is RETAINED per RESEARCH Q3 resolution. It is the canonical data model; SmartChunk mirrors its fields. Safe to keep. |
| T-38-03-05 | Denial of Service | documents.py delete_document broken after old-table cleanup removal | mitigate | Clean the function surgically — keep document existence check, document_chunk delete, document delete, and error handling. Test: `uv run python -c "from eth_pipeline.api import app"` at verify time. |
</threat_model>

<verification>
- `uv run python -c "from eth_pipeline.activities import chunk_document_activity, extract_events_v7_activity, store_events_v7_activity, resolve_references_v7_activity; print('OK')"` — v7 activities importable
- `uv run python -c "from eth_pipeline.workflows import DocumentProcessingV7Workflow; print('OK')"` — v7 workflow importable
- `uv run python -c "from eth_pipeline.chunker import SmartChunker, SmartChunk, DocumentChunk; print('OK')"` — chunker infrastructure intact
- `uv run python -c "from eth_pipeline.api import app; print('OK')"` — FastAPI app starts with cleaned documents.py
- `rg -l "DocumentChunker" src/eth_pipeline/` — returns empty (class removed)
- `rg -l "DocumentProcessingWorkflow" src/eth_pipeline/` — returns empty or only in comments (class removed)
- `rg -l "extract_events_activity\|resolve_entities_activity\|store_extraction_results_activity\|create_event_canonical_entities_activity" src/eth_pipeline/` — returns empty (old activities fully removed)
</verification>

<success_criteria>
- [ ] 5 old activity Python files deleted
- [ ] 2 orphan-cleanup scripts deleted
- [ ] activities/__init__.py — only v7+shared activity imports remain
- [ ] _common.py — _create_canonical_entity removed; _db_params/_extract_query_results/_get_blob_from_minio retained
- [ ] chunk_document.py — old DocumentChunker path removed; SmartChunker path retained
- [ ] workflows.py — DocumentProcessingWorkflow class removed; only DocumentProcessingV7Workflow remains
- [ ] workflows.py __all__ — updated to only list DocumentProcessingV7Workflow
- [ ] chunker.py — DocumentChunker, ChunkResult, chunk_document() removed; SmartChunker/SmartChunk/DocumentChunk/distribute_balanced retained
- [ ] documents.py — delete_document no longer touches old tables; returns DocumentDeleted without orphaned_entities_cleaned
- [ ] documents.py — clear_document_events endpoint deleted
- [ ] All v7 imports verify green (activities, workflows, chunker, api)
- [ ] Old symbols (extract_events_activity, DocumentChunker, DocumentProcessingWorkflow, etc.) raise ImportError
</success_criteria>

<output>
Create `.planning/phases/38-cleanup/38-03-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

### Deleted Files
| Artifact | Path | Description |
|----------|------|-------------|
| extract_events | `src/eth_pipeline/activities/extract_events.py` | Deleted — old whole-document LLM extraction |
| resolve_entities | `src/eth_pipeline/activities/resolve_entities.py` | Deleted — old entity resolution |
| resolve_entities_with_search | `src/eth_pipeline/activities/resolve_entities_with_search.py` | Deleted — old search-first entity resolution |
| store_extraction_results | `src/eth_pipeline/activities/store_extraction_results.py` | Deleted — old event/reference table insertion |
| create_event_canonical_entities | `src/eth_pipeline/activities/create_event_canonical_entities.py` | Deleted — old event-entity linking |
| cleanup_orphan_entities | `scripts/cleanup_orphan_entities.py` | Deleted — was targeting dropped canonical_entity table |
| cleanup_orphan_references | `scripts/cleanup_orphan_references.py` | Deleted — was targeting dropped reference table |

### Modified Files
| Artifact | Path | Changes |
|----------|------|---------|
| activities/__init__.py | `src/eth_pipeline/activities/__init__.py` | 5 old activity imports removed; `_create_canonical_entity` removed |
| _common.py | `src/eth_pipeline/activities/_common.py` | `_create_canonical_entity` function deleted (~24 lines) |
| chunk_document.py | `src/eth_pipeline/activities/chunk_document.py` | DocumentChunker import removed; old-path branch deleted |
| workflows.py | `src/eth_pipeline/workflows.py` | DocumentProcessingWorkflow class deleted (~203 lines); old activity imports removed; __all__ updated |
| chunker.py | `src/eth_pipeline/chunker.py` | DocumentChunker class + ChunkResult + chunk_document() deleted (~200 lines); SmartChunker retained |
| documents.py | `src/eth_pipeline/api/routes/documents.py` | Old-table orphan cleanup block deleted (~260 lines); clear_document_events endpoint deleted (~110 lines); DocumentDeleted() call simplified |