---
phase: 38-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/api/__init__.py
  - src/eth_pipeline/api/models.py
  - src/eth_pipeline/api.py
  - src/eth_pipeline/api/routes/entities.py
  - src/eth_pipeline/api/routes/references.py
  - src/eth_pipeline/api/routes/events.py
autonomous: true
requirements:
  - CLN-02

must_haves:
  truths:
    - "GET /entities, GET /references, old GET /events all return 404"
    - "No old model types (Entity*, Reference*, Merge*, Split*, old Event*) are importable from api.models"
    - "api.py and api/__init__.py do not re-export any deprecated symbols"
  artifacts:
    - path: "src/eth_pipeline/api/__init__.py"
      provides: "Router registration — only documents_router and events_v2_router remain"
      contains: "events_v2_router"
      excludes: "entities_router"
    - path: "src/eth_pipeline/api/models.py"
      provides: "Pydantic models — old types removed, v7 + shared types retained"
      excludes: "EntityListItem"
    - path: "src/eth_pipeline/api.py"
      provides: "Top-level re-exports — no deprecated symbols"
      excludes: "EntityListItem"
  key_links:
    - from: "api/__init__.py"
      to: "api/routes/"
      via: "app.include_router()"
      pattern: "app\\.include_router\\(events_v2_router\\)"
    - from: "api/models.py"
      to: "api/__init__.py"
      via: "import block export"
      pattern: "from eth_pipeline\\.api\\.models import"
---

<objective>
Delete 3 old API route files (entities.py, references.py, events.py), remove their router registrations from api/__init__.py, remove ~15 old Pydantic model classes from api/models.py, and clean all re-export chains in api/__init__.py and api.py. This unregisters /entities/*, /references/*, and the shadowed old /events/* paths.

Purpose: Fulfill CLN-02 (old API routes) by surgically removing every deprecated symbol path. No old route survives. No old model is importable.
Output: 3 files deleted, 3 files edited — all old API surface removed
</objective>

<execution_context>
@/home/u/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/u/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-cleanup/38-RESEARCH.md
@src/eth_pipeline/api/__init__.py
@src/eth_pipeline/api/models.py
@src/eth_pipeline/api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old route files + clean router imports/registrations in api/__init__.py</name>
  <read_first>
    - src/eth_pipeline/api/__init__.py — read the full file (87 lines) to see router import block (lines 77-87) and model re-export block (lines 12-43 — DO NOT EDIT model re-exports in this task, only router imports)
    - .planning/phases/38-cleanup/38-RESEARCH.md — review lines 157-181 (Pattern 1: Backward-Incompatible Route Removal) and lines 371-390 (Route Cleanup code example)
  </read_first>
  <files>
    src/eth_pipeline/api/__init__.py
    src/eth_pipeline/api/routes/entities.py
    src/eth_pipeline/api/routes/references.py
    src/eth_pipeline/api/routes/events.py
  </files>
  <action>
    Three operations, this exact order:

    1. DELETE these three files:
       - src/eth_pipeline/api/routes/entities.py
       - src/eth_pipeline/api/routes/references.py
       - src/eth_pipeline/api/routes/events.py

    2. EDIT src/eth_pipeline/api/__init__.py — In the router import block (lines 77-87), remove exactly these 3 import lines:
       - `from eth_pipeline.api.routes.entities import router as entities_router  # noqa: E402`
       - `from eth_pipeline.api.routes.references import router as references_router  # noqa: E402`
       - `from eth_pipeline.api.routes.events import router as events_router  # noqa: E402`

    3. In the same file, remove exactly these 3 include_router calls:
       - `app.include_router(entities_router)`
       - `app.include_router(references_router)`
       - `app.include_router(events_router)`

    After edits, the router section should have ONLY:
    - `from eth_pipeline.api.routes.documents import router as documents_router`
    - `from eth_pipeline.api.routes.events_v2 import router as events_v2_router`
    - `app.include_router(documents_router)`
    - `app.include_router(events_v2_router)`

    DO NOT edit the model re-export block (lines 12-43) — that is Task 2's responsibility.
    DO NOT edit the FastAPI app constructor or static directory mounting.
    DO NOT edit the `# noqa: E402` comment on the remaining import lines — keep the style consistent.
  </action>
  <verify>
    <automated>uv run python -c "from eth_pipeline.api import app; print('App loaded OK')"</automated>
  </verify>
  <done>3 route files deleted, 3 router imports removed from api/__init__.py, 3 include_router calls removed, old /entities /references /events paths return 404, FastAPI app starts without ImportError</done>
</task>

<task type="auto">
  <name>Task 2: Remove old model classes from api/models.py + clean re-exports in api/__init__.py and api.py</name>
  <read_first>
    - src/eth_pipeline/api/models.py — read the full file (840 lines) to identify all old model classes and the __all__ list at the bottom
    - src/eth_pipeline/api/__init__.py — read lines 12-43 (model re-export block)
    - src/eth_pipeline/api.py — read lines 12-34 (top-level re-export block)
    - .planning/phases/38-cleanup/38-RESEARCH.md — review lines 392-418 (Model Types to Remove) and lines 321-325 (Pitfall 3: Stale Model Re-exports)
  </read_first>
  <files>
    src/eth_pipeline/api/models.py
    src/eth_pipeline/api/__init__.py
    src/eth_pipeline/api.py
  </files>
  <action>
    THREE files edited simultaneously — all deletions synced to prevent ImportError from stale re-exports.

    **File 1: src/eth_pipeline/api/models.py**
    Remove these concrete model classes (entire class blocks + docstrings):
    - EntityDeleted (line ~204)
    - OrphanCleanupResponse (line ~210)
    - EntityListItem (line ~215)
    - EntityDetailReference (line ~231)
    - EntityDetailResponse (line ~243)
    - EntityListResponse (line ~254)
    - ReferenceListItem (line ~272)
    - ReferenceListResponse (line ~333)
    - EventListItem (line ~352 — the OLD EventListItem, NOT EventV2ListItem)
    - EventListResponse (line ~401 — the OLD EventListResponse, NOT EventListV2Response)
    - MergeRequest (line ~713)
    - MergeResponse (line ~728)
    - SplitPartition (line ~747)
    - SplitRequest (line ~757)
    - SplitResponse (line ~768)
    - EventsCleared (line ~147)

    Also remove these entries from the `__all__` list at the bottom (lines 803-840):
    EntityDeleted, OrphanCleanupResponse, EntityDetailReference, EntityDetailResponse, EntityListItem, EntityListResponse, EventListItem, EventListResponse, EventsCleared, MergeRequest, MergeResponse, ReferenceListItem, ReferenceListResponse, SplitPartition, SplitRequest, SplitResponse

    KEEP all these: APIInfo, DocumentCreated, DocumentDeleted, DocumentInput, DocumentListItem, DocumentListResponse, DocumentStatus, DocumentTokenUsage, DocumentUploadCreated, HealthResponse, ProcessingLogListItem, ProcessingLogListResponse, LlmCallLogListItem, LlmCallLogListResponse, ChunkTextResponse, EventV2ListItem, EventListV2Response, EventLocationDetail, EventParticipantDetail, EventRefDetail, EventV2DetailResponse

    **File 2: src/eth_pipeline/api/__init__.py — model re-export block (lines 12-43)**
    Remove these symbols from the `from eth_pipeline.api.models import (...)` block:
    EntityDetailReference, EntityDetailResponse, EntityListItem, EntityListResponse, EventsCleared, MergeRequest, MergeResponse, ReferenceListItem, ReferenceListResponse, SplitPartition, SplitRequest, SplitResponse

    KEEP these in the import: APIInfo, ChunkTextResponse, DocumentCreated, DocumentDeleted, DocumentInput, DocumentListItem, DocumentListResponse, DocumentStatus, DocumentUploadCreated, EventListV2Response, EventLocationDetail, EventParticipantDetail, EventRefDetail, EventV2DetailResponse, EventV2ListItem, HealthResponse, ProcessingLogListItem, ProcessingLogListResponse

    **File 3: src/eth_pipeline/api.py — top-level re-export block (lines 12-34)**
    Remove these symbols from the `from eth_pipeline.api import (...)` block:
    EntityListItem, EntityListResponse, EventsCleared, MergeRequest, MergeResponse, ReferenceListItem, ReferenceListResponse, SplitPartition, SplitRequest, SplitResponse

    KEEP: APIInfo, DocumentCreated, DocumentDeleted, DocumentInput, DocumentListItem, DocumentListResponse, DocumentStatus, DocumentUploadCreated, HealthResponse, ProcessingLogListItem, ProcessingLogListResponse, app
  </action>
  <verify>
    <automated>uv run python -c "from eth_pipeline.api import app; from eth_pipeline.api import EventV2ListItem, DocumentListItem; from eth_pipeline.api.models import EventV2DetailResponse; print('All imports OK')" && uv run python -c "from eth_pipeline.api import EntityListItem" 2>&1 | grep -q ImportError && echo "EntityListItem correctly not importable"</automated>
  </verify>
  <done>~15 old model classes deleted from models.py, model re-exports cleaned in api/__init__.py and api.py, __all__ list updated, no stale ImportError at startup, old symbols not importable, v7 and shared types still importable</done>
</task>

<task type="auto">
  <name>Task 3: Update DocumentDeleted model + remove EventsCleared model</name>
  <read_first>
    - src/eth_pipeline/api/models.py — read the DocumentDeleted class (lines 160-171) and EventsCleared class (lines 147-158)
    - .planning/phases/38-cleanup/38-RESEARCH.md — review lines 173 (DocumentDeleted change)
  </read_first>
  <files>src/eth_pipeline/api/models.py</files>
  <action>
    Two model edits in src/eth_pipeline/api/models.py:

    1. **DocumentDeleted class** (line ~160): Remove the `orphaned_entities_cleaned: int = 0` field and its docstring `"""Number of canonical_entities that were orphaned and removed."""`. After the canonical_entity table is dropped, this field is meaningless. The DocumentDeleted model should only have:
       - document_id: str
       - document_deleted: bool = True

    2. **EventsCleared class** (line ~147): Remove entirely — this model was removed from the re-export chain in Task 2's models.py step. Verify it is gone. If still present (was missed in Task 2), delete the entire class block now. Also remove 'EventsCleared' from __all__ if still present.

    These are the final model cleanups after Task 2's bulk removal. The DocumentDeleted model change is needed because documents.py constructs DocumentDeleted with `orphaned_entities_cleaned=orphaned` (Plan 38-03 cleans up the documents.py route code in its Task 3 — deleting the old-table cleanup block in `delete_document` and the `clear_document_events` endpoint).
  </action>
  <verify>
    <automated>uv run python -c "from eth_pipeline.api.models import DocumentDeleted; d = DocumentDeleted(document_id='test'); assert not hasattr(d, 'orphaned_entities_cleaned'); print('DocumentDeleted OK')"</automated>
  </verify>
  <done>DocumentDeleted has only document_id + document_deleted fields (orphaned_entities_cleaned removed), EventsCleared class deleted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | HTTP requests to /entities, /references, old /events — must return 404, not 500 |

## Assets
| Asset | Sensitivity |
|-------|-------------|
| documents.py, events_v2.py routes | Production API surface — must remain unchanged and functional |
| EventV2ListItem, EventV2DetailResponse, DocumentListItem, etc. | Pydantic models used by v7 API — must remain importable |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Denial of Service | Stale re-export in api.py referencing deleted model | mitigate | All three files (models.py, api/__init__.py, api.py) edited atomically. Verify with `uv run python -c "from eth_pipeline.api import app"` — app startup is the gate. |
| T-38-02-02 | Denial of Service | Remaining code importing deprecated symbols (e.g., documents.py importing EventsCleared) | mitigate | Plan 38-03 Task 3 handles documents.py cleanup — removes the `clear_document_events` endpoint and old-table cleanup in `delete_document`. The EventsCleared model still exists until Plan 38-02 Task 3 removes it. |
| T-38-02-03 | Information Disclosure | Old routes returning stale data if router is not properly unregistered | mitigate | Files deleted + imports/registrations removed — FastAPI physically cannot route to deleted modules. |
</threat_model>

<verification>
- `uv run python -c "from eth_pipeline.api import app"` — app starts without ImportError
- `uv run python -c "from eth_pipeline.api import EntityListItem"` — raises ImportError (confirms old symbols inaccessible)
- `uv run python -c "from eth_pipeline.api import EventV2ListItem, DocumentListItem"` — succeeds (confirms v7 types accessible)
- `curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/entities` — returns 404
- `curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/references` — returns 404
</verification>

<success_criteria>
- [ ] 3 old route files deleted
- [ ] api/__init__.py router section only has documents_router + events_v2_router
- [ ] ~15 old model classes removed from api/models.py
- [ ] api/__init__.py and api.py re-exports cleaned of old symbols
- [ ] DocumentDeleted model simplified (no orphaned_entities_cleaned)
- [ ] FastAPI app starts without ImportError
- [ ] Old routes return 404
</success_criteria>

<output>
Create `.planning/phases/38-cleanup/38-02-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

### Deleted Files
| Artifact | Path | Description |
|----------|------|-------------|
| entities route | `src/eth_pipeline/api/routes/entities.py` | Deleted — old entity list/detail/merge/split routes |
| references route | `src/eth_pipeline/api/routes/references.py` | Deleted — old reference list route |
| events route (old) | `src/eth_pipeline/api/routes/events.py` | Deleted — old event clear route |

### Modified Files
| Artifact | Path | Description |
|----------|------|-------------|
| api/__init__.py | `src/eth_pipeline/api/__init__.py` | Router imports reduced to documents + events_v2; model re-exports cleaned |
| api/models.py | `src/eth_pipeline/api/models.py` | ~15 old Pydantic classes removed; DocumentDeleted simplified; __all__ updated |
| api.py | `src/eth_pipeline/api.py` | Top-level re-exports cleaned of old symbols |

### Removed Symbols
| Symbol | Type | Former Location |
|--------|------|----------------|
| EntityDetailReference | Pydantic | api/models.py |
| EntityDetailResponse | Pydantic | api/models.py |
| EntityListItem | Pydantic | api/models.py |
| EntityListResponse | Pydantic | api/models.py |
| EntityDeleted | Pydantic | api/models.py |
| OrphanCleanupResponse | Pydantic | api/models.py |
| MergeRequest | Pydantic | api/models.py |
| MergeResponse | Pydantic | api/models.py |
| SplitPartition | Pydantic | api/models.py |
| SplitRequest | Pydantic | api/models.py |
| SplitResponse | Pydantic | api/models.py |
| EventListItem (old) | Pydantic | api/models.py |
| EventListResponse (old) | Pydantic | api/models.py |
| EventsCleared | Pydantic | api/models.py |
| ReferenceListItem | Pydantic | api/models.py |
| ReferenceListResponse | Pydantic | api/models.py |
| orphaned_entities_cleaned (field) | Pydantic field | api/models.py → DocumentDeleted |
