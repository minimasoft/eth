---
phase: 38-cleanup
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/eth_pipeline/static/index.html
autonomous: true
requirements:
  - CLN-02

must_haves:
  truths:
    - "Entidades and Referencias tabs are absent from the top nav bar"
    - "Only Cargar, Documentos, Registros, Eventos tabs remain in navigation"
    - "No console errors when switching among remaining tabs"
    - "#tab-entities and #tab-references HTML sections do not exist in the DOM"
  artifacts:
    - path: "src/eth_pipeline/static/index.html"
      provides: "Single-page app — entity/reference code removed"
      excludes: "tab-btn-entities"
  key_links:
    - from: "nav buttons"
      to: "sections object"
      via: "onTabClick event handler → sections[tabName] lookup"
      pattern: "sections\\[tabName\\]"
    - from: "Entity tab JS"
      to: "API endpoints /entities"
      via: "fetchEntities → fetch('/entities?...')"
      pattern: "fetch\\('/entities"
    - from: "Reference tab JS"
      to: "API endpoints /references"
      via: "fetchReferences → fetch('/references?...')"
      pattern: "fetch\\('/references"
---

<objective>
Remove all "Entidades" and "Referencias" tab code from `static/index.html` — HTML sections, nav buttons, sections object entries, tab-switch references, entity-specific JavaScript functions, reference-specific JavaScript functions, event listeners, pagination, detail panels, and recycle buttons. The remaining tabs (Cargar, Documentos, Registros, Eventos) must function without console errors.

Purpose: Fulfill the UI half of CLN-02 — no deprecated entity/reference UI code survives. After Phase 37's Eventos tab replaces their functionality, these tabs are dead weight with broken API dependencies (routes deleted in Plan 38-02).

Deletion order follows RESEARCH.md lines 327-331 (Pitfall 4: Incomplete UI Cleanup): HTML sections → nav buttons → sections object → onTabClick → entity JS → reference JS.

Output: ~500 lines removed from a single file (index.html); 2 nav buttons gone; 2 tab sections gone; all associated JS deleted
</objective>

<execution_context>
@/home/u/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/u/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-cleanup/38-RESEARCH.md
@src/eth_pipeline/static/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove entity/reference HTML sections, nav buttons, and sections object entries</name>
  <read_first>
    - src/eth_pipeline/static/index.html — read the full file (3640 lines) to identify all entity/reference code blocks
    - Key sections to locate:
      - Nav buttons: lines 1059-1060 (`tab-btn-entities` and `tab-btn-references`)
      - #tab-entities section: starts at `<section id="tab-entities">` (line ~1137) through `</section>` before `#tab-references` (line ~1233)
      - #tab-references section: starts at `<section id="tab-references">` (line ~1235) through `</section>` before next section (line ~1280)
      - sections object: lines 1528-1529 (`entities:` and `references:` entries)
      - Events tab: there may be entity/reference columns in the `#tab-eventos` section (line ~1114 `col-refs`, line ~1115 `col-ents`) — check if these are in the Eventos tab and whether they reference entity/reference APIs
  </read_first>
  <files>src/eth_pipeline/static/index.html</files>
  <action>
    Four removal operations, in this exact deletion order (Pitfall 4 — RESEARCH.md lines 327-331):

    **Step 1 — Remove HTML sections for #tab-entities and #tab-references:**
    Delete the entire `<section id="tab-entities" ...>` block — from `<section id="tab-entities" role="tabpanel" ...>` at ~line 1137 through its closing `</section>` tag just before `<section id="tab-references" ...>`. This includes the entity list table, entity detail panel, entity search input, pagination elements, the "Sin referencias" empty state, and all entity HTML.

    Delete the entire `<section id="tab-references" ...>` block — from `<section id="tab-references" role="tabpanel" ...>` at ~line 1235 through its closing `</section>` tag. This includes the reference list table, reference detail panel, reference search input, pagination elements, and all reference HTML.

    **Step 2 — Remove nav buttons:**
    Delete these two lines from the `<nav role="tablist">` block (lines 1059-1060):
    ```html
    <button role="tab" aria-selected="false" aria-controls="tab-entities" id="tab-btn-entities" data-tab="entities">Entidades</button>
    <button role="tab" aria-selected="false" aria-controls="tab-references" id="tab-btn-references" data-tab="references">Referencias</button>
    ```

    **Step 3 — Remove sections object entries:**
    In the `sections` object (line ~1525-1531), remove these two key-value pairs:
    ```javascript
    entities: document.getElementById('tab-entities'),
    references: document.getElementById('tab-references'),
    ```
    The resulting `sections` object should only have: `upload`, `documents`, `logs`, `eventos`.

    **Step 4 — Check #tab-eventos for entity/reference column references:**
    Lines ~1114-1115 reference `col-refs` and `col-ents` classes in the event list table header — these are old entity/reference columns in the event list. If they reference deleted APIs (entities/references), remove those `<th>` elements. If they are populated by v7 API (event_v2 data with inline reference/participant info), keep them. Check the Eventos tab JS code to determine — if `renderEventsList` or similar function populates these columns via v7 event data (participant count, etc.), they are valid. If they call `fetchEntities` or `fetchReferences` APIs, remove them.
  </action>
  <verify>
    <automated>uv run python -c "from eth_pipeline.api import app; print('FastAPI app OK')" && rg -c 'tab-btn-entities|tab-btn-references|tab-entities|tab-references' src/eth_pipeline/static/index.html | grep -v '^#' | awk -F: '{sum+=$2} END {if (sum==0) print "PASS: 0 entity/reference references"; else print "FAIL: found " sum " references"}'</automated>
  </verify>
  <done>HTML sections for #tab-entities and #tab-references deleted; nav buttons for Entidades and Referencias removed; sections object cleaned of entities/references entries; Eventos tab header verified; grep returns zero remaining references to old tab IDs and nav buttons</done>
</task>

<task type="auto">
  <name>Task 2: Remove all entity/reference JavaScript functions and event listeners</name>
  <read_first>
    - src/eth_pipeline/static/index.html — read the JavaScript block (lines ~1500+ through end of file) focusing on entity/reference functions
    - Key functions to remove: fetchEntities, renderEntities, fetchEntitiesPage, recycleEntities, showEntityDetail, closeEntityDetail, fetchEntityDetailReferences, fetchReferences, renderReferences, fetchReferencesPage
    - Event listeners: entity search input, entity refresh button, entity recycle button, reference search input, reference refresh button, entity pagination listeners, reference pagination listeners
    - Entity detail panel JS: showEntityDetail, closeEntityDetail, fetchEntityDetailReferences, all DOM references to entity-detail-* elements
    - Reference detail JS: any reference detail panel code (if separate from entity detail)
    - Check: do any remaining tabs (Eventos, Registros, Documentos, Cargar) call into entity/reference functions? If so, remove those call sites too.
  </read_first>
  <files>src/eth_pipeline/static/index.html</files>
  <action>
    Delete ALL entity-specific and reference-specific JavaScript. The exact line ranges will be identified by reading the file — locate and remove:

    **Entity JavaScript (all of the following):**
    - `fetchEntities()` function — fetches from `/entities?...` endpoint (deleted in Plan 38-02)
    - `renderEntities()` function — renders the entity list table
    - `fetchEntitiesPage(page)` — entity pagination
    - `recycleEntities()` function — calls orphan cleanup (deleted scripts)
    - `showEntityDetail(entityId)` function — opens entity detail panel
    - `closeEntityDetail()` function — closes entity detail panel  
    - `fetchEntityDetailReferences(entityId)` function — fetches references for entity detail
    - Entity search input event listener (`#entity-search` on `input` or `keyup`)
    - Entity refresh button listener (`#entity-refresh-btn`)
    - Entity recycle button listener (`#entity-recycle-btn`)
    - Entity detail panel close button listener
    - Entity pagination listeners
    - Any `ref-count-link` click handlers that trigger entity detail
    - The banner messages related to entity operations (e.g., "Error al obtener entidades", "Error al cargar entidades", "Error al reciclar entidades")

    **Reference JavaScript (all of the following):**
    - `fetchReferences()` function — fetches from `/references?...` endpoint (deleted in Plan 38-02)
    - `renderReferences()` function — renders the reference list table
    - `fetchReferencesPage(page)` — reference pagination
    - Reference search input event listener (`#ref-search`)
    - Reference refresh button listener (`#ref-refresh-btn`)
    - Reference pagination listeners
    - Reference detail panel JS (if separate from entity detail)
    - Banner messages related to reference operations

    **Also remove/check:**
    - Any global DOM element references (e.g., `const entitySearch = document.getElementById('entity-search')` — remove these)
    - If the Eventos tab has event listeners or inline handlers that reference entity/reference functions, remove those hooks but preserve the Eventos tab functionality
    - If there's a `deleteEntity` or `mergeEntities` or `splitEntity` function, remove those too

    **Do NOT remove:**
    - Tab switching infrastructure (`switchTab`, `onTabClick`, `sections` object — Task 1 handles sections object cleanup; Task 2 only removes the entries)
    - Eventos tab JS (fetchEventsList, renderEventsList, event detail modal, reference navigation in eventos)
    - Documentos tab JS (fetchDocuments, upload, etc.)
    - Registros/Logs tab JS (processing logs, LLM calls)
    - Cargar tab JS
    - Header, CSS styles, shared utilities
    - The `sections` object itself (cleaned by Task 1)

    **Verification after all JS removals:**
    - Grep for remaining entity/reference API calls: `rg "fetch\('/entities\|fetch\('/references" src/eth_pipeline/static/index.html` should return empty
    - Grep for remaining entity/reference function names: `rg "fetchEntities\|renderEntities\|recycleEntities\|showEntityDetail\|closeEntityDetail\|fetchReferences\|renderReferences" src/eth_pipeline/static/index.html` should return empty
  </action>
  <verify>
    <automated>rg -c "fetch\('/entities|fetch\('/references|fetchEntities|renderEntities|recycleEntities|showEntityDetail|closeEntityDetail|fetchReferences|renderReferences|entity-search|ref-search|entity-refresh-btn|ref-refresh-btn|entity-recycle-btn" src/eth_pipeline/static/index.html | grep -v '^#' | awk -F: '{sum+=$2} END {if (sum==0) print "PASS: 0 entity/reference JS references"; else print "FAIL: found " sum " references"}'</automated>
  </verify>
  <done>All entity JavaScript functions deleted; all reference JavaScript functions deleted; all entity/reference event listeners removed; grep returns zero references to old entity/reference JS identifiers; Eventos/Documentos/Registros/Cargar tabs remain functional</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — remaining tabs work, no console errors</name>
  <what-built>All entity and reference HTML sections, nav buttons, and JavaScript have been removed. The Eventos tab (from Phase 37) should serve as the replacement for viewing events, participants, locations, and references.</what-built>
  <how-to-verify>
    1. Start the application: `uv run uvicorn eth_pipeline.api:app --host 0.0.0.0 --port 8000`
    2. Open the app in a browser at `http://localhost:8000`
    3. Verify the nav bar has exactly: **Cargar**, **Documentos**, **Registros**, **Eventos** (no "Entidades", no "Referencias")
    4. Click each remaining tab — confirm no console errors appear:
       - **Cargar** tab: upload area renders correctly
       - **Documentos** tab: document list loads without errors
       - **Registros** tab: processing logs and LLM calls sub-tabs work
       - **Eventos** tab: event list loads, detail modal opens, reference navigation works
    5. Open browser DevTools Console (F12) — verify zero JavaScript errors
    6. Confirm no `404` network errors from the app trying to call `/entities` or `/references` APIs
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the UI cleanup is complete, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser DOM → JavaScript engine | `document.getElementById()` calls on removed elements return `null` — null-safe code must not crash |
| JavaScript → API | fetch() calls to `/entities` and `/references` must no longer exist (routes deleted in Plan 38-02) |

## Assets
| Asset | Sensitivity |
|-------|-------------|
| Eventos tab functionality | Production UI — must work exactly as before |
| Documentos, Cargar, Registros tabs | Production UI — must work exactly as before |
| Tab switching infrastructure | Shared JS used by all tabs — must not break |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Denial of Service | JavaScript error from `document.getElementById('tab-entities')` returning null after HTML removed | mitigate | Remove `sections.entities` and `sections.references` entries (Task 1, Step 3) BEFORE removing JS code. This prevents the `switchTab` function from attempting to activate non-existent sections. |
| T-38-04-02 | Denial of Service | Remaining JS code calling `fetchEntities()` or `fetchReferences()` after function deletion | mitigate | Complete audit — all entity/reference JS functions, event listeners, and call sites removed. Grep verification (Task 2 verify) confirms zero remaining references. |
| T-38-04-03 | Denial of Service | CSS styles referencing `#tab-entities`, `#tab-references`, or entity/reference class names | mitigate | CSS remains — unused selectors on removed elements are harmless (no visual effect, no JS errors). Accept this minor dead CSS as a cosmetic non-blocker. |
| T-38-04-04 | Denial of Service | Entity/reference columns in Eventos tab table header (`col-refs`, `col-ents`) referencing broken APIs | mitigate | Task 1 Step 4 checks these columns. If they call entity/reference APIs, remove them. If they use v7 event data (participant count, etc.), keep them. The Eventos tab is the replacement for these columns. |
</threat_model>

<verification>
- Grep: `rg -c "tab-btn-entities|tab-btn-references" src/eth_pipeline/static/index.html` → 0 (nav buttons gone)
- Grep: `rg -c "tab-entities|tab-references" src/eth_pipeline/static/index.html` → 0 (HTML sections gone)
- Grep: `rg "fetch\('/entities|fetch\('/references" src/eth_pipeline/static/index.html` → empty (no API calls to deleted routes)
- Grep: `rg "fetchEntities|renderEntities|recycleEntities|showEntityDetail|closeEntityDetail|fetchReferences|renderReferences" src/eth_pipeline/static/index.html` → empty (no JS functions)
- Browser: nav has 4 tabs (Cargar, Documentos, Registros, Eventos); all function without console errors
</verification>

<success_criteria>
- [ ] Nav bar has exactly 4 tabs: Cargar, Documentos, Registros, Eventos
- [ ] No Entidades tab, no Referencias tab anywhere in the page
- [ ] No entity/reference HTML sections in the DOM
- [ ] No entity/reference JavaScript functions remaining
- [ ] No fetch() calls to /entities or /references APIs
- [ ] Cargar, Documentos, Registros (processing logs + LLM calls), and Eventos tabs all work
- [ ] Zero JavaScript console errors
- [ ] Zero 404 network errors from old API calls
</success_criteria>

<output>
Create `.planning/phases/38-cleanup/38-04-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

### Modified Files
| Artifact | Path | Changes |
|----------|------|---------|
| index.html | `src/eth_pipeline/static/index.html` | ~500 lines removed: #tab-entities section (~96 lines), #tab-references section (~45 lines), 2 nav buttons, sections object entries, all entity JS functions, all reference JS functions, all related event listeners |

### Removed UI Elements
| Element | Type | Description |
|---------|------|-------------|
| #tab-btn-entities | Nav button | "Entidades" tab in nav bar |
| #tab-btn-references | Nav button | "Referencias" tab in nav bar |
| #tab-entities | HTML section | Entity list, detail panel, search, pagination |
| #tab-references | HTML section | Reference list, search, pagination |
| fetchEntities() | JS function | Fetches entity list from /entities API |
| renderEntities() | JS function | Renders entity table rows |
| recycleEntities() | JS function | Calls orphan entity cleanup |
| showEntityDetail() | JS function | Opens entity detail panel |
| fetchReferences() | JS function | Fetches reference list from /references API |
| renderReferences() | JS function | Renders reference table rows |