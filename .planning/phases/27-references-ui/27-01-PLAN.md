---
phase: 27-references-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eth_pipeline/api/models.py
  - src/eth_pipeline/api/routes/references.py
autonomous: true
requirements: [REFS-02]

must_haves:
  truths:
    - "GET /references returns page_offset_start and page_offset_end for every reference"
    - "GET /references returns context_excerpt for every reference when document text_content and span data are available"
    - "context_excerpt shows ~80 chars of surrounding text with verbatim span centered, or null when text_content is not available"
  artifacts:
    - path: "src/eth_pipeline/api/models.py"
      provides: "ReferenceListItem with page_offset_start, page_offset_end, context_excerpt fields"
      min_lines: 0
      contains: "page_offset_start: int | None"
    - path: "src/eth_pipeline/api/routes/references.py"
      provides: "Computed context_excerpt and mapped page_offset fields in list_references response"
      contains: "context_excerpt"
  key_links:
    - from: "api/routes/references.py"
      to: "api/models.py"
      via: "ReferenceListItem import"
      pattern: "ReferenceListItem"
    - from: "api/routes/references.py"
      to: "document.text_content"
      via: "SELECT d.text_content"
      pattern: "d\\.text_content"
---

<objective>
Add `page_offset_start`, `page_offset_end`, and `context_excerpt` to the `GET /references` API response model and query.

**Purpose:** The frontend needs these fields to render the "Página/Offset" provenance column and the "Contexto" context excerpt column in the References tab table. `page_offset_start`/`end` are stored in the `reference` table but not exposed in the API response. `context_excerpt` does not exist and must be computed at query time from the document's `text_content` and the reference's `span_start`/`span_end`.

**Output:**
- Updated `ReferenceListItem` model with 3 new optional fields
- Updated `list_references` SQL query to fetch `d.text_content` and compute `context_excerpt`
</objective>

<execution_context>
@/home/u/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/u/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/eth_pipeline/api/models.py
@src/eth_pipeline/api/routes/references.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add page_offset_start, page_offset_end, context_excerpt to ReferenceListItem model</name>
  <files>src/eth_pipeline/api/models.py</files>
  <action>
    Add three optional fields to the `ReferenceListItem` class (after `page_number` at line 286):

    1. `page_offset_start: int | None = None` — COMMENT 'Character offset within the page where the verbatim span starts (0-based).'
    2. `page_offset_end: int | None = None` — COMMENT 'Character offset within the page where the verbatim span ends (exclusive).'
    3. `context_excerpt: str | None = None` — COMMENT 'Excerpt of surrounding text (~80 chars before and after) with verbatim text centered, or null if document text_content is unavailable.'

    Add a docstring-style comment for each. The order after `page_number` should be: `page_offset_start`, `page_offset_end`, then `context_excerpt`.
  </action>
  <verify>
    <automated>grep -n "page_offset_start" src/eth_pipeline/api/models.py</automated>
  </verify>
  <done>ReferenceListItem has page_offset_start, page_offset_end, context_excerpt as optional nullable fields</done>
</task>

<task type="auto">
  <name>Task 2: Compute context_excerpt and map page_offset fields in list_references query</name>
  <files>src/eth_pipeline/api/routes/references.py</files>
  <action>
    Modify `list_references` to:

    1. **Add `d.text_content` to the data SQL SELECT** — append `d.text_content AS doc_text_content` to the SELECT clause at line 72-79.

    2. **Compute `context_excerpt`** — After the `for record in data_result` loop starts (line 102), compute context_excerpt for each record:

       ```
       context_excerpt = None
       doc_text = record.get("doc_text_content")
       span_start = record.get("span_start")
       span_end = record.get("span_end")
       if doc_text and span_start is not None and span_end is not None:
           ctx_before = 80
           ctx_after = 80
           start = max(0, span_start - ctx_before)
           end = min(len(doc_text), span_end + ctx_after)
           excerpt = doc_text[start:end]
           # If the excerpt doesn't start at the beginning of text, prefix with "..."
           if start > 0:
               excerpt = "..." + excerpt
           # If the excerpt doesn't end at the end of text, suffix with "..."
           if end < len(doc_text):
               excerpt = excerpt + "..."
           context_excerpt = excerpt
       ```

       This gives ~80 chars of text on each side of the verbatim span. The frontend will bold the verbatim portion within this excerpt.

    3. **Map new fields to ReferenceListItem** — Add to the `ReferenceListItem(...)` constructor call (lines 103-120):
       - `page_offset_start=record.get("page_offset_start")`
       - `page_offset_end=record.get("page_offset_end")`
       - `context_excerpt=context_excerpt`

    The `page_offset_start` and `page_offset_end` fields already come through `r.*` in the SELECT — they just need to be mapped to the model.
  </action>
  <verify>
    <automated>python -c "from eth_pipeline.api.models import ReferenceListItem; from eth_pipeline.api.routes.references import list_references; print('OK')" 2>&1</automated>
  </verify>
  <done>list_references returns page_offset_start, page_offset_end, context_excerpt in each ReferenceListItem</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→frontend | Computed context_excerpt derived from document text_content crosses the format boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | context_excerpt computation | accept | Excerpt is only ~240 chars of content already accessible via GET /documents/{id} — no new disclosure surface |
</threat_model>

<verification>
1. Run `python -c "from eth_pipeline.api.models import ReferenceListItem; print(ReferenceListItem.model_json_schema())"` and verify page_offset_start, page_offset_end, context_excerpt appear in the schema
2. Run `PYTHONPATH=src pytest src/eth_pipeline/api/tests/ -x -k "reference" -q` to confirm no existing tests break
</verification>

<success_criteria>
1. `ReferenceListItem` model schema includes `page_offset_start`, `page_offset_end`, `context_excerpt`
2. A reference with valid span_start/span_end and document text_content returns a non-null context_excerpt
3. page_offset fields match the values stored in the DB for each reference
</success_criteria>

<output>
Create `.planning/phases/27-references-ui/27-01-SUMMARY.md` when done
</output>
